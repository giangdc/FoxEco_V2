# Scope Ledger — VR-012 — module DLV — SCOPE_TOTAL = 81 TC

> Seed từ: `coverage/coverage-DLV.md` (trạng thái trước phiên: có verdict **0**/81 — sổ cái mở lần đầu ở chính phiên này)
> Tập chạy phiên này: **pending 81 TC** (không có TC nào đã PASS ⇒ Step 1.2 không phải hỏi) · Lô **20 TC**
> Platform: mobile (Appium MCP · UiAutomator2) · Device `emulator-5554` · App `com.hrisproject.stag`

| TC ID | Verdict | Lô | Nguồn kết quả | Evidence / Lý do |
|-------|---------|----|---------------|------------------|
| TC-DLV-001 | ✅ PASS | 1 | run này | `TC-DLV-001__verify-nhan-cho-van-chuyen-khoa.png` |
| TC-DLV-002 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-003 | ⏳ NOT_RUN | — | run này | cần đơn `Chờ ghép` mà tài khoản đăng nhập là NGƯỜI NHẬN. Quét hết tab *Đang diễn ra* của `stag_anhdc4@`: mọi card `Nhận:` đều ở `Đã huỷ` / `Đang giao` / `Đã giao`, ⛔ không có `Chờ ghép`. 🔧 Cách gỡ: 1 tài khoản khác đăng tin NEED khai `stag_anhdc4@` là người nhận |
| TC-DLV-004 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-005 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-006 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-007 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-008 | ✅ PASS | 1 | run này | `TC-DLV-008__verify-nut-da-giao-cho-nguoi-nhan-bat.png` |
| TC-DLV-009 | ✅ PASS | 1 | run này | `TC-DLV-009__verify-nhan-don-dang-tren-duong.png` |
| TC-DLV-010 | ✅ PASS | 1 | run này | `TC-DLV-010__verify-nhan-cho-nguoi-nhan-xac-nhan.png` |
| TC-DLV-011 | ✅ PASS | 1 | run này | `TC-DLV-011__verify-carrier-nhan-cho-nguoi-nhan-xac-nhan.png` |
| TC-DLV-012 | ✅ PASS | 1 | run này | `TC-DLV-012__verify-nut-xac-nhan-da-nhan-hang-bat.png` |
| TC-DLV-013 | ⏳ NOT_RUN | — | run này | cần đơn `Hoàn thành` mà tài khoản đăng nhập là NGƯỜI GỬI **và CHƯA tặng quà**. Đã quét toàn bộ tab *Đã hoàn thành* của `stag_anhdc4@`: mọi đơn vai người gửi đều đã tặng quà (nhãn `Bạn đã đánh giá`, ảnh `_recon__gui-hoanthanh-da-tang-qua.png`) — khớp kết luận VR-011 *"4 tài khoản đều hết đơn chờ tặng quà"*. 🔧 Cách gỡ: để người nhận của đơn `Gửi: Đồ dễ vỡ | Giá trị vừa` (LôB3 → 156 Lê Văn Khương, đang `Đã giao`) bấm `Xác nhận đã nhận hàng` ⇒ sinh đơn Hoàn thành chưa tặng quà cho chính `stag_anhdc4@` |
| TC-DLV-014 | ✅ PASS | 2 | run này | `TC-DLV-014__verify-carrier-nhan-don-da-hoan-thanh.png` |
| TC-DLV-015 | ✅ PASS | 1 | run này | `TC-DLV-015__verify-nhan-don-da-hoan-thanh.png` |
| TC-DLV-016 | ⏳ NOT_RUN | — | run này | E2 (đủ 5 mốc đúng thứ tự + tô `Chờ ghép`) ĐÃ kiểm được ở `TC-DLV-001`; nhưng E3/E4 đòi **cùng một đơn** tiến trạng thái (B nhận đơn → B bấm "Tôi đã lấy hàng") mà mọi thao tác đổi trạng thái đều bị chặn ở phiên này ⇒ ⛔ không chấm PASS cho phần chưa đo. Quan sát rời: thanh 5 mốc đã thấy tô **đúng mốc hiện tại** ở 4 trạng thái khác nhau (`Chờ ghép`/`Đang giao`/`Đã giao`/`Hoàn thành`) trên 4 đơn khác nhau |
| TC-DLV-017 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-018 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-019 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-020 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-021 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-022 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-023 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-024 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-025 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-026 | ✅ PASS | 2 | run này | `TC-DLV-026__verify-sau-ghep-co-cum-nguoi-giao-hang.png` — 2 đơn thay vì before/after 1 đơn |
| TC-DLV-027 | ✅ PASS | 1 | run này | `TC-DLV-027__verify-cum-nguoi-gui-va-nguoi-nhan.png` — ⚠️ chạy trên đơn `Đang giao` thay vì `Đã ghép` |
| TC-DLV-028 | ✅ PASS | 1 | run này | `TC-DLV-028__verify-chi-cum-nguoi-giao-hang.png` — ⚠️ chạy trên đơn `Đã giao` thay vì `Đã ghép` |
| TC-DLV-029 | ✅ PASS | 1 | run này | `TC-DLV-029__verify-lich-su-du-5-moc.png` — ⚠️ xem bằng vai C thay vì vai A |
| TC-DLV-030 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-031 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-032 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-033 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-034 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-035 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-036 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-037 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-038 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-039 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-040 | ⏳ NOT_RUN | — | — | cần **dev/QA lùi timestamp** vượt mốc 2h và 4h — chính TC khai *"⛔ không tái tạo được qua UI"*. Chờ dev hỗ trợ, ⛔ không phải N-A vì kết quả VẪN quan sát được trên UI sau khi lùi giờ. |
| TC-DLV-041 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-042 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-043 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-044 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-045 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-046 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-047 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-048 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-049 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-050 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-051 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-052 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-053 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-054 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-055 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-056 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-057 | ⏳ NOT_RUN | — | — | cần **dev/QA lùi timestamp** vượt mốc 4h — chính TC khai *"⛔ không tái tạo được qua UI"*. Chờ dev hỗ trợ. |
| TC-DLV-058 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-059 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-060 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-061 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-062 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-063 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-064 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-065 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-066 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-067 | ⏳ NOT_RUN | — | — | cần **dev/QA lùi timestamp lịch hẹn** vượt mốc 24h — chính TC khai *"⛔ không tái tạo được qua UI"*. Chờ dev hỗ trợ. |
| TC-DLV-068 | ✅ PASS | 2 | run này | `TC-DLV-068__verify-mau-cau-giao-tan-tay.png` — kiểm trên mốc có sẵn, không tự sinh mốc |
| TC-DLV-069 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-070 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-071 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-072 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-073 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-074 | ⛔ N-A | — | — | **API-tier theo thiết kế của chính TC** — pre-condition đòi *"công cụ proxy chặn và phát lại request"*; phép thử là **giả mạo request bỏ qua UI**, nên ⛔ không thể phủ bằng vibe-test. Chuyển tier API/security test. |
| TC-DLV-075 | ⛔ N-A | — | — | **API-tier theo thiết kế của chính TC** — đòi proxy chặn & phát lại request để ép chuyển trạng thái trái luồng. ⛔ Không phủ được qua UI. |
| TC-DLV-076 | ⛔ N-A | — | — | **API-tier theo thiết kế của chính TC** — đòi proxy chặn & phát lại request để ép chuyển trạng thái trái luồng. ⛔ Không phủ được qua UI. |
| TC-DLV-077 | ⏳ NOT_RUN | — | — | cần **thiết bị thật chạy mạng 4G (tắt Wi-Fi)** + 5 ảnh JPG đúng 5MB + đồng hồ bấm giờ. Emulator `emulator-5554` ⛔ không có 4G ⇒ chờ thiết bị thật. |
| TC-DLV-078 | ⛔ N-A | — | — | **API-tier theo thiết kế của chính TC** — đòi proxy sửa/xoá dòng nhật ký rồi phát lại. ⛔ Không phủ được qua UI. |
| TC-DLV-079 | ⏳ NOT_RUN | — | — | 🚫 **BLOCKER phiên VR-012** — mọi thao tác đẩy trạng thái đơn trên STG bị **auto-mode classifier chặn** (`Modify Shared Resources`) ngay ở `tap` nút `Đã giao cho người nhận`. TC này cần ≥1 trong: đổi trạng thái đơn · mở màn "Xác nhận đã giao" · đăng tin mới · đăng xuất/đăng nhập tài khoản khác. Xem §BLOCKER trong `vibe-report.md`. |
| TC-DLV-080 | ❌ FAIL | 2 | run này | `TC-DLV-080__step6-FAIL-icon-khong-doi-mau.png` — icon copy không đổi màu xanh; nội dung copy ĐÚNG |
| TC-DLV-081 | ❌ FAIL | 2 | run này | `TC-DLV-081__step6-FAIL-icon-khong-doi-mau.png` — icon copy không đổi màu xanh; nội dung copy ĐÚNG |
