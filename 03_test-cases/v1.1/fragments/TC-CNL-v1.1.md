<!-- fragment-meta: module_code=CNL; module_name=Huỷ đơn / Huỷ nhận đơn; version=v1.1; mode=standard; generated=2026-09-17 -->
# TC Fragment — Huỷ đơn / Huỷ nhận đơn (CNL) — v1.1

> Nguồn: `02_analyze-requirements/v1.1/CNL-huy-don/` — scope **NEW + MODIFIED = 9 SC** (`SC-CNL-014..017` NEW · `SC-CNL-004/006/009/010/012` MODIFIED) · REQ `REQ-CNL-002/003/006/007/008/009`.
> Mode `standard`, áp `Project_rule.md §Custom Rules §10.4` (Expected chỉ 1 dòng ở step cuối) — **13 TC**: P1 4 · P2 7 · P3 2. SC fan-out >1 TC (theo vai, mỗi vai fail độc lập theo `BR11-04` "bất kỳ vai trò nào"): `SC-CNL-006` (3 TC: Người gửi · Người vận chuyển · Người nhận) · `SC-CNL-014` (3 TC: Người gửi · Người vận chuyển · Người nhận).
> **Chiến lược ID:** TC của SC MODIFIED giữ nguyên ID v1.0 (`TC-CNL-004/006/009/010/012`, Lifecycle `MODIFIED`); TC NEW nối tiếp từ ID cao nhất đã dùng ở v1.0 (`TC-CNL-014`) → dải mới `TC-CNL-015..022`. Không gỡ TC nào.
> **CARRIED (8 SC) không nằm trong fragment này** — dùng TC v1.0 ở `../../v1.0/fragments/TC-CNL-v1.0.md`: `TC-CNL-001` · `002` · `003` · `005` · `007` · `008` · `011` · `013` (⚠️ `TC-CNL-008` khi tái dùng cho lô này nhớ siết thêm **timestamp** theo `BR11-02` — cùng rule đã áp cho `TC-CNL-009` dưới đây).
> 🔴 **`TC-CNL-006` có kỳ vọng NGƯỢC bản v1.0 cùng ID** — v1.0 PASS = KHÔNG có nút "Báo cáo sự cố", v1.1 PASS = CÓ. Khi chạy/consolidate phải lấy bản v1.1.
> ⚠️ **`TC-CNL-004/009/010/012` giữ dự kiến FAIL** — 4 SC viết theo spec đã phê duyệt (PRD, không còn là override QA), FAIL là kết quả ĐÚNG; ⛔ KHÔNG sửa Expected cho PASS theo hành vi app hiện tại.
> ⚠️ **Ranh giới CNL⟷TS** (`RISK-CNL-08`): `TC-CNL-006/015/016/017/018/019` chỉ kiểm nút "Báo cáo sự cố"/"hoàn hàng" TỒN TẠI + mở được luồng — KHÔNG lặp nội dung form/ảnh của luồng báo sự cố (home ở `TS`, `SC-TS-008..015`) hay luồng hoàn hàng (home ở `DLV`, `SC-DLV-053/054`).
> ⚠️ **`TC-CNL-020` (SC-CNL-015) có thể BLOCKED** nếu STG chưa build trạng thái `INCIDENT`/`FR16` — verdict đúng là BLOCKED, ⛔ không PASS vì "không thấy đơn tự chuyển".
> **Seed dùng chung** (`Project_rule §10.3`): `SEED-DLV-01` checkpoint 2 (`IN_TRANSIT`) cho `TC-CNL-006/015/016/017/018/019` · checkpoint 1 (`MATCHED`) cho `TC-CNL-021` — không seed riêng.
> **`SEED-DLV-01` — định nghĩa đầy đủ** *(chép tại chỗ 2026-09-17, `review-tc` nợ #23 — trước đó chỉ trỏ sang `03_test-cases/v1.0/CHANGELOG.md` làm fragment này không tự đủ):*
> 1 đơn **DUY NHẤT** đẩy qua đủ **5 checkpoint** `POSTED → MATCHED → IN_TRANSIT → DELIVERED → COMPLETED` hoàn toàn bằng **UI thật** —
> **A = SENDER · B = CARRIER · C = RECEIVER**, đúng 3 cú bấm chuyển trạng thái đã có trong `TC-DLV-017/018/019`.
> ⚠️ **Trạng thái đơn MỘT CHIỀU, không lùi được** — chạy đủ TC tại 1 checkpoint rồi mới đẩy sang checkpoint kế; lỡ đẩy sớm phải tạo đơn mới từ checkpoint 0.
> Trong mỗi checkpoint: **TC chỉ-xem chạy trước, TC có hành động chuyển trạng thái chạy cuối**.
> ⛔ Ngoại lệ: `TC-DLV-024` (quá 4 giờ) **KHÔNG** dùng seed này — cần real-time/timestamp, phải nhờ dev/QA.
> **Scope module:** đã xác nhận **IN scope v1.1** (`RISK-CNL-06` Resolved 2026-09-17, QC GiangDC2).

## Test Cases

| Testcase ID | Scenario ID | Req ID | DOC Source | Group | Priority | Test Title | Pre-condition | Steps | Expected Result | Test Data | Status | Notes | Version Origin | Lifecycle | Assigned To |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-CNL-004 | SC-CNL-004 | REQ-CNL-002 | DOC-v1.1-01 | Business Rule | P2 | Check lý do huỷ 4 ký tự bị chặn ngay từ nút xác nhận | - Tài khoản A là CBNV có hồ sơ đầy đủ trên STG.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản A và đăng 1 tin NEED qua wizard "+ Đăng tin". (setup)<br>2. Nhấn tab "Hoạt động", mở đơn vừa đăng và nhấn "Huỷ đơn". (setup)<br>3. Nhập "abcd" vào ô lý do.<br>4. Check trạng thái nút "Xác nhận" và vùng dưới ô lý do. | 4. Nút "Xác nhận" ở trạng thái vô hiệu hoá ngay (không phải bấm được rồi mới báo lỗi); hiện lỗi ngay dưới ô lý do; trạng thái đơn không đổi. | Lý do 4 ký tự "abcd" (test_data_catalog §CNL v1.1, biên dưới) | Fail | Mục đích: siết thêm 2 chi tiết Then mà v1.0 chưa có — nút vô hiệu hoá SẴN (không phải bấm rồi báo lỗi) và lỗi hiện đúng vị trí dưới ô lý do. · Căn cứ: `BR11-01` + `VAL-04` + `AC-25.1.03` (PRD phê duyệt, không còn là override QA). ⚠ Dự kiến FAIL — app hiện cho qua ⇒ log bug, ⛔ không hạ chuẩn TC theo hành vi app. · Đã sửa theo analyze v1.1 (2026-09-15): bỏ vế "5 ký tự được nhận" — đã có `TC-CNL-003` (CARRIED) cover, tránh trùng. · Đã sửa theo §10.4 (2026-09-17): gộp 3 điểm quan sát (nút khoá · lỗi đúng vị trí · trạng thái đơn) vào 1 Expected ở step cuối. | v1.0 | MODIFIED |  |
| TC-CNL-006 | SC-CNL-006 | REQ-CNL-003 · REQ-CNL-007 | DOC-v1.1-01 | Business Rule | P2 | Check nút "Báo cáo sự cố" tồn tại cho vai Người gửi khi đơn đang giao | - Đơn đã ở checkpoint 2 (`IN_TRANSIT`/Đang giao) của `SEED-DLV-01` (xem ghi chú đầu fragment) — không seed riêng.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản A (Người gửi), mở màn Theo dõi đơn của đơn đó. (setup)<br>2. Check góc trên bên phải màn Theo dõi đơn. | 2. Có nút "Báo cáo sự cố"; nhấn vào mở được luồng báo sự cố. | Đơn `IN_TRANSIT` của `SEED-DLV-01` (test_data_catalog §CNL v1.1) | Pass | 🔴 Bản v1.0 cùng ID assert KHÔNG có chức năng này — kỳ vọng ngược hoàn toàn, phải dùng bản v1.1. · Căn cứ: `§4 SCOPES` In-scope + `§8.16` + `AC-25.2.01`. ⚠ Dự kiến FAIL nếu STG chưa build `FR16` ⇒ log gap PRD↔app, ⛔ không diễn giải thành "vẫn out of scope". · Đọc chéo `TS` — chỉ kiểm nút tồn tại + mở được, KHÔNG lặp nội dung form/ảnh (home ở `SC-TS-008..015`). · Đã sửa theo §10.4 (2026-09-17): tách riêng theo vai — vai Người vận chuyển ở `TC-CNL-015`, vai Người nhận ở `TC-CNL-016` (mỗi vai độc lập, có thể fail riêng theo `BR11-04` "bất kỳ vai trò nào"). | v1.0 | MODIFIED |  |
| TC-CNL-015 | SC-CNL-006 | REQ-CNL-003 · REQ-CNL-007 | DOC-v1.1-01 | Business Rule | P2 | Check nút "Báo cáo sự cố" tồn tại cho vai Người vận chuyển khi đơn đang giao | - Đơn đã ở checkpoint 2 (`IN_TRANSIT`/Đang giao) của `SEED-DLV-01` (xem ghi chú đầu fragment) — không seed riêng.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản B (Người vận chuyển), mở màn Theo dõi đơn của đơn đó. (setup)<br>2. Check góc trên bên phải màn Theo dõi đơn. | 2. Có nút "Báo cáo sự cố"; nhấn vào mở được luồng báo sự cố. | Đơn `IN_TRANSIT` của `SEED-DLV-01` (test_data_catalog §CNL v1.1) | Pass | Mục đích: vai độc lập với `TC-CNL-006` (vai Người gửi) — `BR11-04` yêu cầu "bất kỳ vai trò nào", mỗi vai có thể fail riêng. · Căn cứ: giống `TC-CNL-006`. · Đọc chéo `TS` — chỉ kiểm nút tồn tại, không lặp nội dung form (home `SC-TS-008..015`). TC mới tách theo §10.4 (2026-09-17), không có ID cũ 1-1. | v1.1 | NEW |  |
| TC-CNL-016 | SC-CNL-006 | REQ-CNL-003 · REQ-CNL-007 | DOC-v1.1-01 | Business Rule | P2 | Check nút "Báo cáo sự cố" tồn tại cho vai Người nhận khi đơn đang giao | - Đơn đã ở checkpoint 2 (`IN_TRANSIT`/Đang giao) của `SEED-DLV-01` (xem ghi chú đầu fragment) — không seed riêng.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản C (Người nhận), mở màn Theo dõi đơn của đơn đó. (setup)<br>2. Check góc trên bên phải màn Theo dõi đơn. | 2. Có nút "Báo cáo sự cố"; nhấn vào mở được luồng báo sự cố. | Đơn `IN_TRANSIT` của `SEED-DLV-01` (test_data_catalog §CNL v1.1) | Pass | Mục đích: vai độc lập với `TC-CNL-006`/`015` — `BR11-04` yêu cầu "bất kỳ vai trò nào". · Căn cứ: giống `TC-CNL-006`. · Đọc chéo `TS` — chỉ kiểm nút tồn tại, không lặp nội dung form (home `SC-TS-008..015`). TC mới tách theo §10.4 (2026-09-17), không có ID cũ 1-1. | v1.1 | NEW |  |
| TC-CNL-009 | SC-CNL-009 | REQ-CNL-006 | DOC-v1.1-01 | Business Rule | P2 | Check huỷ đơn ghi log đủ vai trò, lý do và thời điểm | - Tài khoản A và B là hai CBNV khác nhau trên STG.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản A, đăng 1 tin NEED, nhờ tài khoản B nhận đơn để đơn sang "Đã ghép". (setup)<br>2. Mở màn Theo dõi đơn của đơn đó, mở block LỊCH SỬ. (setup)<br>3. Ghi lại số dòng log hiện có.<br>4. Nhấn "Huỷ đơn".<br>5. Nhập lý do "Huy vi trung lich".<br>6. Nhấn "Xác nhận".<br>7. Không rời màn Theo dõi đơn, mở lại block LỊCH SỬ.<br>8. Check số dòng log mới và nội dung dòng đó. | 8. Block LỊCH SỬ có nhiều hơn số dòng đã ghi ở bước 3 đúng 1 dòng; dòng mới ghi đúng nhãn hành động huỷ đơn của Người gửi (KHÔNG phải nhãn "Đã huỷ nhận đơn" của luồng huỷ nhận), kèm danh tính người thực hiện (app ghi **tên người**, không ghi vai trò — chấp nhận theo hành vi nhất quán toàn app), lý do "Huy vi trung lich" và thời điểm (timestamp) huỷ. | Đơn `MATCHED`; block LỊCH SỬ đã có mốc trước đó (test_data_catalog §CNL v1.1) | Fail | Mục đích: `BR11-02` đòi đủ 3 thành phần (vai trò · lý do · thời điểm) — v1.0 chỉ assert 2, thiếu timestamp. · Căn cứ: `BR11-02` + `AC-25.1.01` (PRD phê duyệt, không còn là override QA). ⚠ Dự kiến FAIL — app hiện chỉ hiện banner, không ghi log ⇒ log bug. · Kiểm chéo cùng lô với `SC-TS-003` + `SC-DLV-062` — cùng vỡ thì gộp 1 bug report. · Đã sửa theo analyze v1.1 (2026-09-15): thêm assert thời điểm (timestamp) vào Expected. · 🔁 **Đính chính 2026-09-22 (QC):** vế "ghi vai trò" đổi thành "ghi tên người" (app nhất quán ghi tên ở mọi dòng LỊCH SỬ, không riêng dòng huỷ) — `BUG-035` đã xoá, không phải bug. Vế **nhãn hành động sai** ("Đã huỷ nhận đơn" thay vì nhãn đúng của huỷ đơn) vẫn là lỗi độc lập, còn giữ ở `BUG-034` — verdict TC này **vẫn FAIL** vì lý do này. | v1.0 | MODIFIED |  |
| TC-CNL-010 | SC-CNL-010 | REQ-CNL-006 | DOC-v1.1-01 | Business Rule | P1 | Check huỷ nhận đơn giữ nguyên log ghép cũ và đơn về lại bảng tin | - Tài khoản A và B là hai CBNV khác nhau trên STG.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản A, đăng 1 tin NEED rồi đăng xuất. (setup)<br>2. Đăng nhập bằng tài khoản B, nhận đơn đó và xác nhận. (setup)<br>3. Mở màn Theo dõi đơn, mở block LỊCH SỬ. (setup)<br>4. Check sự tồn tại của dòng "Ghép thành công". (setup)<br>5. Nhấn "✕ Huỷ nhận đơn".<br>6. Nhập lý do "Doi ca lam viec".<br>7. Nhấn "Xác nhận".<br>8. Không rời màn Theo dõi đơn, mở lại block LỊCH SỬ.<br>9. Điều hướng đến "Bảng tin" và tìm đơn đó. | 9. Dòng "Ghép thành công" vẫn còn trong block LỊCH SỬ ở bước 8 (không bị xoá), có thêm 1 dòng log ghi đúng nhãn hành động huỷ nhận "Đã huỷ nhận đơn" kèm danh tính người thực hiện (app ghi **tên người**, không ghi vai trò — chấp nhận theo hành vi nhất quán toàn app) và lý do "Doi ca lam viec"; đơn chuyển về trạng thái "Chờ ghép" và hiện lại trên bảng tin. | Đơn `MATCHED`; block LỊCH SỬ có dòng "Ghép thành công" (test_data_catalog §CNL v1.1) | Pass | Mục đích: log phải bất biến — xoá dòng cũ là phá tính bằng chứng của cả timeline; đồng thời đơn phải quay lại vòng đời bình thường, không mắc kẹt. · Căn cứ: `BR11-03` + `AC-25.1.02` (PRD phê duyệt, không còn là override QA — trước dựa `BR-INT-04` suy diễn). ⚠ Dự kiến FAIL — `KB-CNL-01` đã live-verify app XOÁ dòng "Ghép thành công" ⇒ log bug. · Cùng nhóm rule truy vết "bằng chứng đã ghi thì bất biến" — chạy cùng lô với `SC-USR-017`/`SC-ORD-065`/`SC-DLV-062`, cùng vỡ thì gộp 1 bug report. · Đã sửa theo §10.4 (2026-09-17): gộp 2 điểm kiểm (log giữ nguyên + đơn về POSTED/hiện lại bảng tin) vào 1 Expected ở step cuối; bỏ Expected ở step 4 (chỉ còn observation baseline, không phải case fail độc lập). · 🔁 **Đính chính 2026-09-22 (QC):** vế "ghi vai trò" đổi thành "ghi tên người" (chấp nhận, nhất quán toàn app) — `BUG-035` đã xoá. Không còn vướng mắc nào khác ⇒ verdict FAIL → PASS. | v1.0 | MODIFIED |  |
| TC-CNL-012 | SC-CNL-012 | REQ-CNL-002 | DOC-v1.1-01 | Business Rule | P3 | Check lý do huỷ gồm năm dấu cách bị chặn sau khi cắt khoảng trắng | - Tài khoản A là CBNV có hồ sơ đầy đủ trên STG.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản A và đăng 1 tin NEED qua wizard "+ Đăng tin". (setup)<br>2. Nhấn tab "Hoạt động", mở đơn vừa đăng và nhấn "Huỷ đơn". (setup)<br>3. Nhập đúng 5 dấu cách vào ô lý do.<br>4. Check trạng thái nút "Xác nhận". | 4. Nút "Xác nhận" ở trạng thái vô hiệu hoá, không nhấn được (sau khi tự cắt khoảng trắng đầu/cuối thì lý do rỗng). | Lý do gồm 5 dấu cách (test_data_catalog §CNL v1.1, biên) | Pass | Mục đích: mốc thứ tư trong bộ boundary lý do (rỗng → 4 ký tự → 5 ký tự → 5 dấu cách). · Căn cứ: `VAL-03` (trim) + `VAL-04` (PRD phê duyệt, không còn là override QA). ⚠ Dự kiến FAIL — `KB-CNL-02` đã live-verify app cho qua ⇒ log bug. · ⚠️ Biên "5 dấu cách" dễ bị gõ nhầm thành "     x" khi nhập tay — ghi rõ trong Test Data để tester không tự ý gõ sai. | v1.0 | MODIFIED |  |
| TC-CNL-017 | SC-CNL-014 | REQ-CNL-008 | DOC-v1.1-01 | Business Rule | P1 | Check chỉ còn đúng 2 đường thoát cho vai Người gửi sau khi đơn đang giao | - Đơn đã ở checkpoint 2 (`IN_TRANSIT`/Đang giao) của `SEED-DLV-01` (xem ghi chú đầu fragment) — không seed riêng.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản A (Người gửi), mở màn Theo dõi đơn của đơn đó. (setup)<br>2. Rà toàn bộ nút hành động trên màn. | 2. KHÔNG còn nút huỷ đơn thường nào; chỉ còn đúng 1 nút hành động liên quan: "Báo cáo sự cố". KHÔNG có nút "hoàn hàng" nào ở màn này của Người gửi — hoàn hàng (RETURNING) là hành động do Người vận chuyển khởi tạo (`FR09`), Người gửi không có control kích hoạt trực tiếp. | Đơn `IN_TRANSIT` của `SEED-DLV-01` (test_data_catalog §CNL v1.1) | Pass | Căn cứ: `AC-25.2.01` + `BR11-04` ("bất kỳ vai trò nào") — chỉ đảm bảo ở MỨC TRẠNG THÁI (hoàn hàng vẫn là 1 trong 2 lối thoát hợp lệ của đơn), KHÔNG đặt tên nút UI. Đọc chéo `DLV` `FR09` §8.9 (trang 43) — trigger thật của RETURNING là Người vận chuyển chọn "Cầm hàng về" ở màn "Xử lý đơn hàng", không phải nút trên màn Theo dõi đơn của Sender/Receiver. Và `TS` (`SC-TS-008..015` — home của form "báo sự cố"). · Đã sửa theo §10.4 (2026-09-17): tách theo vai — vai Người vận chuyển ở `TC-CNL-018`, vai Người nhận ở `TC-CNL-019`. · 🔁 **Đính chính 2026-09-22 (QC, sau khi đối chiếu PRD gốc):** bỏ giả định nút "Yêu cầu hoàn hàng" trên màn Sender/Receiver — không tồn tại trong PRD, TC viết sai. `BUG-033` đã xoá, verdict FAIL → PASS. | v1.1 | NEW |  |
| TC-CNL-018 | SC-CNL-014 | REQ-CNL-008 | DOC-v1.1-01 | Business Rule | P1 | Check chỉ còn đúng 2 đường thoát cho vai Người vận chuyển sau khi đơn đang giao | - Đơn đã ở checkpoint 2 (`IN_TRANSIT`/Đang giao) của `SEED-DLV-01` (xem ghi chú đầu fragment) — không seed riêng.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản B (Người vận chuyển), mở màn Theo dõi đơn của đơn đó. (setup)<br>2. Rà toàn bộ nút hành động trên màn. | 2. KHÔNG còn nút huỷ đơn thường nào; có nút "Báo cáo sự cố" VÀ đường vào luồng hoàn hàng đi qua màn giao hàng: bấm nút "Đã đến địa điểm giao hàng" → màn "Xử lý đơn hàng" → chọn chế độ "Cầm hàng về" (`FR09`). KHÔNG có một nút riêng tên "Yêu cầu hoàn hàng" trên màn Theo dõi đơn. | Đơn `IN_TRANSIT` của `SEED-DLV-01` (test_data_catalog §CNL v1.1) | Pass | Mục đích: vai độc lập với `TC-CNL-017`/`019` — `BR11-04` yêu cầu "bất kỳ vai trò nào". · Căn cứ: giống `TC-CNL-017`. · Đọc chéo `DLV`/`TS`, không lặp nội dung luồng con. TC mới tách theo §10.4 (2026-09-17), không có ID cũ 1-1. · 🔁 **Đính chính 2026-09-22 (QC):** chỉ vai Người vận chuyển mới có đường vào hoàn hàng (qua `FR09` "Xử lý đơn hàng" → "Cầm hàng về"), không phải nút riêng "Yêu cầu hoàn hàng". `BUG-033` đã xoá, verdict FAIL → PASS. · 🔁 **Đính chính 2026-09-22 (QC, bổ sung):** chốt tên chính thức nút vào luồng hoàn hàng ở v1.1 là **"Đã đến địa điểm giao hàng"** (đổi tên so với "Đã giao cho người nhận" của v1.0) — STG hiện **CHƯA đổi theo tên mới** (evidence VR-018 2026-09-21, ảnh vẫn hiện "Đã giao cho người nhận"), xem `BUG-044`. | v1.1 | NEW |  |
| TC-CNL-019 | SC-CNL-014 | REQ-CNL-008 | DOC-v1.1-01 | Business Rule | P1 | Check chỉ còn đúng 2 đường thoát cho vai Người nhận sau khi đơn đang giao | - Đơn đã ở checkpoint 2 (`IN_TRANSIT`/Đang giao) của `SEED-DLV-01` (xem ghi chú đầu fragment) — không seed riêng.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản C (Người nhận), mở màn Theo dõi đơn của đơn đó. (setup)<br>2. Rà toàn bộ nút hành động trên màn. | 2. KHÔNG còn nút huỷ đơn thường nào; chỉ còn đúng 1 nút hành động liên quan: "Báo cáo sự cố". KHÔNG có nút "hoàn hàng" nào ở màn này của Người nhận — hoàn hàng (RETURNING) là hành động do Người vận chuyển khởi tạo (`FR09`), Người nhận không có control kích hoạt trực tiếp. | Đơn `IN_TRANSIT` của `SEED-DLV-01` (test_data_catalog §CNL v1.1) | Pass | Mục đích: vai độc lập với `TC-CNL-017`/`018` — `BR11-04` yêu cầu "bất kỳ vai trò nào". · Căn cứ: giống `TC-CNL-017`. · Đọc chéo `DLV`/`TS`, không lặp nội dung luồng con. TC mới tách theo §10.4 (2026-09-17), không có ID cũ 1-1. · 🔁 **Đính chính 2026-09-22 (QC):** bỏ giả định nút "Yêu cầu hoàn hàng" trên màn Người nhận — không tồn tại trong PRD. `BUG-033` đã xoá, verdict FAIL → PASS. | v1.1 | NEW |  |
| TC-CNL-020 | SC-CNL-015 | REQ-CNL-008 | DOC-v1.1-01 | Business Rule | P2 | Check đơn ở trạng thái sự cố không tự chuyển hoàn thành | - Đơn đã ở trạng thái `INCIDENT` qua luồng báo sự cố (`FR16`).<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản C (Người nhận) của đơn `INCIDENT`, mở màn Theo dõi đơn. (setup)<br>2. Rà toàn bộ nút hành động và nội dung màn. | 2. KHÔNG có nút "Xác nhận đã nhận hàng" khả dụng để chuyển trạng thái đơn; màn nêu rõ đơn đang chờ admin hỗ trợ, KHÔNG tự chuyển sang "Hoàn thành". | Đơn `INCIDENT` (test_data_catalog §CNL v1.1) — ⚠ có thể chưa seed được nếu STG chưa build `FR16` | Blocked | ⚠️ Verdict đúng khi STG chưa build `INCIDENT`/`FR16` là **BLOCKED**, ⛔ không đánh PASS vì "không thấy đơn tự chuyển". · Căn cứ: `AC-25.2.01`. · ⛔ Không assert trạng thái đích cụ thể sau khi admin xử lý — `C-CNL-03` chốt trạng thái đích tuỳ dev. · ⛔ Không assert banner/thông báo "đang được hỗ trợ" — BA xác nhận người dùng không nhận thông báo gì trong lúc chờ. | v1.1 | NEW |  |
| TC-CNL-021 | SC-CNL-016 | REQ-CNL-003 | DOC-v1.1-01 | Business Rule | P2 | Check huỷ đơn bị chặn khi đơn còn hiện Đã ghép nhưng người vận chuyển đã bấm lấy hàng | - Đơn đang ở "Đã ghép" (`MATCHED`), sẵn 2 phiên đăng nhập song song: tài khoản A (Người gửi) và tài khoản B (Người vận chuyển).<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Trên phiên B, nhấn nút "Tôi đã lấy hàng" nhưng CHƯA xác nhận popup (giữ popup đang mở).<br>2. Trên phiên A, mở màn Theo dõi đơn của đơn đó và nhấn "Huỷ đơn". | 2. Huỷ bị chặn (không mở được popup nhập lý do, hoặc hiển thị lỗi ngăn huỷ). | 2 phiên song song A (Người gửi) + B (Người vận chuyển); đơn `MATCHED`, popup "Tôi đã lấy hàng" đang mở ở phiên B (test_data_catalog §CNL v1.1, ô biên duy nhất của delta) | Blocked | ⭐ Vùng dễ có lỗi race thật — nếu backend chỉ kiểm `status == MATCHED` mà không kiểm hành động đã bấm thì huỷ sẽ lọt qua. · Căn cứ: PRD đổi cách phát biểu ngưỡng từ trạng thái sang hành động (`§8.11 Pre-Conditions` + `AC-25.1.01` — "MATCHED khi CHƯA bấm Tôi đã lấy hàng", không phải "MATCHED" đơn thuần). · Cần thao tác đồng thời 2 phiên trong cùng khoảnh khắc — không dựng được bằng 1 tài khoản. | v1.1 | NEW |  |
| TC-CNL-022 | SC-CNL-017 | REQ-CNL-009 | DOC-v1.1-01 | Functional | P3 | Check không tồn tại bề mặt Admin vận hành trong app end-user | - Tài khoản A là CBNV thường (không có vai Admin) trên STG.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản A. (setup)<br>2. Rà toàn bộ màn Theo dõi đơn và trang Cá nhân tìm chức năng huỷ/can thiệp của "Admin vận hành". | 2. KHÔNG có bề mặt Admin nào (không màn, không field, không nút) trong app end-user. | Giá trị assert-absent: bề mặt "Admin vận hành" (test_data_catalog §CNL v1.1) | Pass | `[GAP]` theo `Project_rule §10.1` — PRD nêu actor "Admin vận hành" nhưng không mô tả bề mặt nào ⇒ ghi nhận thiếu vắng, ⛔ không viết TC khẳng định hành vi Admin. · Căn cứ: `C-CNL-03` Resolved 2026-09-16 — dev xử lý tay qua Google Form ngoài app, không có tool/màn. · Gap nghiệp vụ cần nêu PM: nếu chỉ admin đóng được `INCIDENT` mà không có đường tới admin, đơn có thể treo vĩnh viễn. | v1.1 | NEW |  |
