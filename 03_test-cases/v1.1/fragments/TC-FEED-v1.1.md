<!-- fragment-meta: module_code=FEED; module_name=Bảng tin & Chi tiết tin; version=v1.1; mode=standard; generated=2026-09-17 -->
# TC Fragment — Bảng tin & Chi tiết tin (FEED) — v1.1

> ⚠️ **Ngoại lệ kiến trúc:** module `FEED` **không có thư mục `02_analyze-requirements/v1.1/FEED-bang-tin/`** — PRD v1.1 có chạm bề mặt Bảng tin nhưng thay đổi được **sửa tại chỗ** trong `02_analyze-requirements/v1.0/FEED-bang-tin/` (xem `CHANGELOG.md §1` dòng 2026-09-16/17, `v1.1/MEMORY.md §Module Summary`). Nguồn: `../../../02_analyze-requirements/v1.0/FEED-bang-tin/` — toàn bộ 15 SC (`SC-FEED-001..015`) đều giữ Lifecycle **NEW** ở tầng phân tích (chưa từng tách version); ở tầng TC, fragment này chỉ generate cho phần **thật sự có nội dung đổi**.
> Mode `standard`, áp `Project_rule.md §Custom Rules §10.4` (Expected chỉ 1 dòng ở step cuối) — **5 TC**: P1 0 · P2 2 · P3 3.
> **Chiến lược ID:** 4 TC ứng với 4 SC vừa đổi Then giữ nguyên ID v1.0 (`TC-FEED-002/007/009/013`, Lifecycle `MODIFIED`); 1 TC ứng SC hoàn toàn mới nối tiếp ID cao nhất đã dùng ở v1.0 (`TC-FEED-014`) → `TC-FEED-015`.
> **10 SC không đổi (`SC-FEED-001/003/004/005/006/008/010/011/012/014`) không nằm trong fragment này** — dùng TC v1.0 ở `../../v1.0/fragments/TC-FEED-v1.0.md` nguyên trạng, gồm cả `TC-FEED-010`/`011` (2 bug P1 đã biết, chưa log — xem CHANGELOG §3 Nợ #1 của FEED).
> ⛔ **Cập nhật quan trọng chưa kịp write-back đầy đủ ở `CHANGELOG.md §3`/`risk_assessment.md` của FEED:** `C-FEED-01/02/03/04/05` đều đã **Resolved** (mới nhất `C-FEED-05` 2026-09-17) — fragment này áp thẳng bản Resolved.
> **Test data:** nhãn "Loại hàng" dùng **`Tài liệu`** (không phải "Giấy tờ, hồ sơ" — `C-ORD-09` Resolved); văn phòng dùng cho `TC-FEED-009` phải nằm NGOÀI danh sách lỗi toạ độ của `C-FEED-05` (399 `MISSING`/61 thiếu lat-lng/34 ngoài VN, xem `DOC-v1.1-04` = `location_address_catalog.xlsx`), văn phòng dùng cho `TC-FEED-015` phải nằm TRONG danh sách đó.

## Test Cases

| Testcase ID | Scenario ID | Req ID | DOC Source | Group | Priority | Test Title | Pre-condition | Steps | Expected Result | Test Data | Status | Notes | Version Origin | Lifecycle | Assigned To |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-FEED-002 | SC-FEED-002 | REQ-FEED-001 | DOC-v1.0-02 | UI | P2 | Check card tin ở Bảng tin đủ thành phần và không có nút CTA | - Tài khoản B là CBNV có hồ sơ đầy đủ trên STG.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản B, nhấn tab "Bảng tin". (setup)<br>2. Check lần lượt các thành phần và rà toàn bộ card tin đầu tiên tìm nút "Tôi mang giúp được". | 2. Card hiển thị đủ: icon/ảnh hàng, dòng loại hàng kèm giá trị hàng, thời gian đăng, dòng "Nhận:"/"Giao:" dạng rút gọn, khung giờ mong muốn; card KHÔNG có nút "Tôi mang giúp được" ở bất kỳ đâu. | 1 tin NEED `POSTED` có khung giờ, loại hàng "Tài liệu" (test_data_catalog §FEED) |  | Mục đích: hết gap CTA — đổi từ "cố ý không assert vị trí CTA" (v1.0) sang assert cứng phủ định "card KHÔNG có CTA". · Căn cứ: `C-FEED-01(a)` Resolved 2026-09-16 — vibe-check demo xác nhận CTA chỉ có ở màn Chi tiết tin. Badge "Tin của bạn" verify riêng ở `TC-FEED-003/004` (thành phần có điều kiện). · Đã sửa tại chỗ 2026-09-16: thêm assert phủ định CTA vào Expected. | v1.0 | MODIFIED |  |
| TC-FEED-007 | SC-FEED-007 | REQ-FEED-004 | DOC-v1.0-01 · DOC-v1.0-02 | UI | P2 | Check phần dưới Chi tiết tin đủ lộ trình, khung giờ, tên người gửi và CTA | - Tài khoản B là CBNV có hồ sơ đầy đủ trên STG.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản B, nhấn tab "Bảng tin" và nhấn 1 tin do tài khoản khác đăng. (setup)<br>2. Cuộn xuống phần dưới màn Chi tiết tin.<br>3. Check lần lượt các thành phần. | 3. Hiển thị đủ: khối Lộ trình gồm điểm lấy hàng và điểm giao hàng đầy đủ như data đã đăng kèm khung "Bản đồ · ~X km", khung giờ mong muốn, cụm Người gửi có đúng họ tên (KHÔNG có số điện thoại), và nút "Tôi mang giúp được". | 1 tin NEED `POSTED` của tài khoản khác (test_data_catalog §FEED) |  | Mục đích: bổ sung assert tên người gửi hiển thị trước ghép (thành phần tồn tại) — phân biệt với `TC-FEED-010` (cùng cụm nhưng assert SĐT KHÔNG hiển thị). · Căn cứ: `C-FEED-04` Resolved 2026-09-17 — trước ghép chỉ ẩn SĐT, tên + địa chỉ đầy đủ hiện bình thường. ⛔ KHÔNG log bug nếu tên/địa chỉ hiện đầy đủ trước ghép — đó là hành vi ĐÚNG. · Đã sửa tại chỗ 2026-09-17: thêm "có đúng họ tên" + "điểm Lấy/Giao đầy đủ như data đã đăng" vào Expected. | v1.0 | MODIFIED |  |
| TC-FEED-009 | SC-FEED-009 | REQ-FEED-005 | DOC-v1.0-02 | UI | P3 | Check khung bản đồ ở Chi tiết tin là ảnh tĩnh có vẽ tuyến thật | - Tin NEED có điểm lấy hàng và điểm giao hàng đều thuộc văn phòng CÓ đủ toạ độ (lat/lng) trong `location_address_catalog.xlsx` — chọn văn phòng NGOÀI danh sách lỗi `C-FEED-05`.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản B, nhấn tab "Bảng tin" và nhấn tin đó để mở màn Chi tiết tin. (setup)<br>2. Check khung "Bản đồ · ~X km". | 2. Khung hiển thị ảnh bản đồ **tĩnh có vẽ tuyến** từ điểm lấy tới điểm giao (không phải khung trống/placeholder); có dòng "~X km" (không assert giá trị số cụ thể). | Tin NEED có điểm lấy/giao thuộc văn phòng đủ toạ độ (test_data_catalog §FEED) |  | Mục đích: ĐẢO kết luận v1.0 — trước assert "placeholder tĩnh, không có bản đồ" (`C-FEED-01(b)` Open), nay assert "CÓ bản đồ thật, ảnh tĩnh vẽ tuyến". · Căn cứ: `C-FEED-01(b)` + `C-FEED-02(c)` Resolved 2026-09-17. ⛔ Không test zoom/pan — ảnh tĩnh, không phải bản đồ tương tác. · Đã sửa tại chỗ 2026-09-17: đảo hoàn toàn Expected + Pre-condition (cần văn phòng có toạ độ hợp lệ, khác v1.0 không cần điều kiện gì), bỏ 2 bước test zoom/pan của v1.0. | v1.0 | MODIFIED |  |
| TC-FEED-013 | SC-FEED-013 | REQ-FEED-009 | DOC-v1.1-01 · DOC-v1.0-06 | UI | P3 | Check empty state Bảng tin đúng text và chỉ 1 danh sách không tab | - Tài khoản B là CBNV có hồ sơ đầy đủ trên STG. | 1. Nhờ dev/QA chuẩn bị môi trường không còn tin NEED ở trạng thái "Chờ ghép". (setup)<br>2. Đăng nhập bằng tài khoản B, nhấn tab "Bảng tin". (setup)<br>3. Check vùng danh sách, text hiển thị, nút CTA, và cấu trúc màn (có tab con hay không). | 3. Vùng danh sách hiện đúng chuỗi "Chưa có tin nào", có câu gợi ý **mở rộng khu vực hoặc đăng tin**, kèm nút CTA "Đăng tin"; màn Bảng tin chỉ có **1 danh sách duy nhất, không có tab con nào**. | 0 tin cộng đồng (test_data_catalog §FEED, biên) |  | Mục đích: hết `[GAP]` — text empty state đã chốt (`EMP-04`, home ở `ACT`) + xác nhận cấu trúc 1 danh sách (không phải 2 tab như nghi vấn cũ). · Căn cứ: `C-FEED-03` Resolved 2026-09-17 (1 danh sách, không tab/lọc) + `DOC-v1.1-01 §8.17.1 EMP-04` + `C-HOME-04` Resolved vòng 3 2026-09-17 (BA: *"giữ nguyên nhé"* ⇒ `EMP-04` giữ nguyên chữ PRD dù Bảng tin load toàn quốc). ℹ️ PRD không nói "gợi ý mở rộng khu vực" là nút hay chữ ⇒ assert **nội dung chữ**, ghi nhận dạng hiển thị thực tế, không FAIL vì hình thức. · Đã sửa tại chỗ 2026-09-17: thay "chỉ assert phần suy được" bằng assert cứng text + cấu trúc 1 danh sách; bổ sung assert câu gợi ý mở rộng khu vực sau khi `C-HOME-04` đóng. | v1.0 | MODIFIED |  |
| TC-FEED-015 | SC-FEED-015 | REQ-FEED-005 | DOC-v1.1-04 | UI | P3 | Check khung bản đồ hiện placeholder và 0km khi văn phòng thiếu toạ độ | - Tin NEED có điểm lấy hoặc điểm giao là văn phòng THIẾU lat/lng trong `location_address_catalog.xlsx` (theo danh sách lỗi đã xác nhận ở `C-FEED-05`).<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập bằng tài khoản B, nhấn tab "Bảng tin" và nhấn tin đó để mở màn Chi tiết tin. (setup)<br>2. Check khung "Bản đồ · ~X km" và tổng thể bố cục màn. | 2. Khung hiển thị placeholder kèm thông báo (không phải ảnh bản đồ vẽ tuyến); khoảng cách hiện đúng "0km"; màn không vỡ layout. | Văn phòng thiếu lat/lng theo `location_address_catalog.xlsx` (test_data_catalog §FEED — BA cấp data khi test) |  | Mục đích: SC mới — nhánh lỗi data khi văn phòng thiếu toạ độ, đối chứng với `TC-FEED-009` (văn phòng đủ toạ độ). · Căn cứ: `C-FEED-02` + `C-FEED-05` Resolved 2026-09-17 (BA: thiếu toạ độ là lỗi data STG, không phải bug app). ⚠️ BA đánh giá "có thể không cần viết TC" vì là lỗi data — vẫn viết để có coverage nhánh lỗi (đánh P3); text thông báo cụ thể + nhánh "chỉ 1 trong 2 điểm thiếu" chưa chốt, Expected chỉ assert phần đã chốt (placeholder + "0km" + không vỡ layout). | v1.1 | NEW |  |
