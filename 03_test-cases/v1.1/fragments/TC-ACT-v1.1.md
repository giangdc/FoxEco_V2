<!-- fragment-meta: module_code=ACT; module_name=Hoạt động (Đơn của tôi); version=v1.1; mode=standard; generated=2026-09-17 -->
# TC Fragment — Hoạt động (Đơn của tôi) (ACT) — v1.1

> Nguồn: `02_analyze-requirements/v1.1/ACT-hoat-dong/` — scope delta = **3 SC NEW** (`SC-ACT-015/016/017`) + **6 SC MODIFIED** (`SC-ACT-001/005/008/012/013/014`), 8 SC CARRIED.
> Mode `standard` — **10 TC**: P1 0 · P2 6 · P3 4. 6 TC MODIFIED **giữ nguyên ID v1.0** (`TC-ACT-001/005/008/012/013/014`, map 1-1 sạch); 4 TC NEW nối tiếp `TC-ACT-014` → **`TC-ACT-015..018`**.
> CARRIED (`SC-ACT-002/003/004/006/007/009/010/011`) dùng lại TC v1.0 nguyên trạng — ⛔ KHÔNG chép sang fragment này.
> Áp `Project_rule.md §Custom Rules §10.4` ngay từ lượt đầu: **đúng 1 dòng Expected/TC, neo step cuối**.
> Áp `Project_rule.md §Custom Rules §10.2` (màn có tab → mỗi tab 1 TC verify data): đã tuân thủ từ v1.0 (`TC-ACT-004`/`005`, `TC-ACT-012`/`014`) — lượt này giữ nguyên cặp `TC-ACT-012`/`014`.
> **Chiến lược ID:** TC của SC MODIFIED giữ nguyên ID v1.0 (`TC-ACT-001/005/008/012/013/014`, Lifecycle `MODIFIED`); TC NEW nối tiếp từ ID cao nhất đã dùng ở v1.0 (`TC-ACT-014`) → dải mới `TC-ACT-015..018`. **Không gỡ TC nào** ⇒ không phát sinh lỗ ID. Các số trống trong fragment này (`002/003/004` · `006/007` · `009/010/011`) là **TC CARRIED đang sống ở `03_test-cases/v1.0/fragments/TC-ACT-v1.0.md`**, ⛔ không phải ID bị xoá — consolidate lấy đủ 18 TC từ 2 fragment.

## 0. Khai báo bắt buộc trước khi đọc bảng

### 0.1 Vì sao 9 SC ra 10 TC (không phải 12) — khai tường minh để `review-tc` không đọc nhầm là bỏ sót

| SC | TC | Quyết định |
|---|--:|---|
| `SC-ACT-015` | **1** (không tách 2) | Then có 2 vế (`RETURNED` hiện kèm lý do ⟷ `CANCELLED` vẫn ẩn) nhưng **giá trị của SC nằm ở PHÉP ĐỐI CHỨNG**, tách rời là mất đúng thứ nó sinh ra để bắt — `risk_assessment.md` Khuyến nghị #3 + `test_data_catalog.md` (*"⛔ không seed lẻ"*). Hai đơn xem trong **cùng 1 lượt, cùng 1 trạng thái tĩnh của danh sách** ⇒ gộp 1 step Check cuối + 1 Expected theo `§10.4` mục 1. |
| `SC-ACT-005` | **1** (không tách 2) | Then có 3 vế; 2 vế đã có home ở TC khác trong cùng lô — chuỗi lý do "Hết hạn" là `TC-ACT-008`, lý do hoàn hàng của `RETURNED` là `TC-ACT-015`. TC này giữ đúng vế còn lại: **cả 3 loại đơn kết thúc đều nằm ở tab "Đã hoàn thành"**. ⛔ Không mất coverage (`§10.4` mục 3) — chỉ không nhân bản assert. |
| `SC-ACT-013` | **1** | 4 dấu vết cần rà (★ · điểm · tier · chỉ số môi trường) đều thuộc **cùng 1 trạng thái tĩnh của card** ⇒ 1 step Check cuối liệt kê đủ (`§10.4` mục 1). |
| `SC-ACT-016` | **2** | 2 hành vi **fail độc lập** (empty state che thanh tab dưới ⟷ màn không cuộn được) ⇒ tách TC theo `§10.4` mục 2. |

### 0.2 Oracle đã chốt — áp nguồn mới nhất, KHÔNG theo PRD (`testcase-guide.md §B.4.4`)

| Điểm | PRD nói | Nguồn chốt áp vào TC | TC |
|---|---|---|---|
| Nhãn màn + 2 tab | `EMP-05`/`EMP-06`/`AC-09.1.01` — tab **"Đang chạy"** / **"Hoàn tất"** | ✅ `C-ACT-02` **Resolved 2026-09-16** — BA chốt **theo app**: màn **"Đơn của tôi"** · tab **"Đang diễn ra"**/**"Đã hoàn thành"** · nav **"Hoạt động"**. Lệch là do **PRD chưa cập nhật**, ⛔ KHÔNG phải defect app | `TC-ACT-001` và Steps của mọi TC còn lại |
| Đơn "Đã huỷ" nằm đâu | `§8.12.3` cho mọi vai *"Xem lý do"* đơn `CANCELLED` ⇒ đọc được là đơn phải hiện | ✅ `C-ACT-04(b)` **Resolved 2026-09-17** — BA: đơn "Đã huỷ" **vẫn ẨN khỏi cả 2 tab**, rule v1.0 **CÒN HIỆU LỰC**; `§8.12.3` chưa cập nhật theo rule này | `TC-ACT-015` |
| Trạng thái mới vào tab nào | PRD có 12 trạng thái nhưng **không nói** trạng thái nào ở tab nào | ✅ `C-ACT-04(a)` **Resolved 2026-09-17** — `RESCHEDULED`/`RETURNING`/`INCIDENT` → tab **"Đang diễn ra"**; `RETURNED`/`EXPIRED` → tab **"Đã hoàn thành"** | `TC-ACT-005`, `TC-ACT-015` |
| Chuỗi lý do "Hết hạn" | — | ✅ PRD v1.1 **gọi tên chuỗi lần đầu**: `§8.5.1 BR05-03` + `AC-09.1.01` ⇒ bản **ngắn** *"Không có ai nhận mang giúp trong thời gian đăng"* thắng bản dài của `KB-ORD-07` (#4) mà v1.0 đang assert | `TC-ACT-008` — **đảo Expected so với cùng ID ở v1.0** |
| ★★★★★ trên card | `§4 SCOPES` loại trừ vĩnh viễn + `§8.14.1 BR14-03` | ✅ Dấu vết ★ nay là **defect phải log bug**, không còn là "leftover ghi nhận" như v1.0 | `TC-ACT-013` — **siết Expected so với cùng ID ở v1.0** |

🔴 **3 TC đảo/siết kỳ vọng so với bản v1.0 cùng ID — `TC-ACT-008`, `TC-ACT-013`, `TC-ACT-014`.** Consolidate/execute **phải lấy bản v1.1**.

### 0.3 Điểm KHÔNG assert được — ghi nợ, không viết thành TC ghi-nhận

- `SC-ACT-017` mang nhãn `[GAP]`: điều kiện *"hiện empty state dựa cờ dữ liệu rỗng, không dựa null field"* là rule **backend**, ⛔ không verify trực tiếp qua UI. `TC-ACT-018` chỉ assert **phần quan sát được**: phân biệt rõ *đang tải* ⟷ *không có dữ liệu*, và ⛔ không skeleton vô hạn. Vế cờ backend ghi nợ cho lượt có công cụ kiểm tầng dữ liệu.
- ⚠️ **Mâu thuẫn đã biết giữa 2 câu trả lời BA, chưa chạm TC lượt này:** `C-ACT-03(c)` nói tin **Hết hạn KHÔNG cho tương tác**, còn `C-ORD-17` nói card Hết hạn ở tab "Đã hoàn thành" **có "Xem lý do/đăng lại"**. `TC-ACT-008` cố ý **chỉ đọc card, không tap** (giữ nguyên cách v1.0) nên không dính mâu thuẫn. SC về đích tap card (`SC-ACT-009/010/011`) là **CARRIED**, không regenerate lượt này ⇒ để `analyze-requirements` đối chiếu lại với BA trước khi đụng tới.

### 0.4 Seed dùng chung (`Project_rule.md §Custom Rules §10.3`)

**`SEED-ACT-02` (tái dùng nguyên từ v1.0) — 1 tài khoản CBNV "trắng", 0 đơn ở cả 2 tab** → `TC-ACT-012/014/016/017/018`.
⚠️ **Tài nguyên khan hiếm nhất của lượt này** — `HOME` (`SC-HOME-025..027`) và `GIFT` (`SC-GIFT-008`) cũng cần. Xin **1 tài khoản mới tinh dùng chung**, chạy hết cụm empty state của cả 3 module trong 1 lượt rồi mới để nó "bẩn" đi; ⛔ đừng để mỗi module tự xin một tài khoản.

**`SEED-ACT-03` (mới v1.1) — bộ 4 đơn kết thúc trên CÙNG tài khoản A** → `TC-ACT-005/008/013/015`:
- 1 đơn `COMPLETED` — tạo qua flow UI thật (A đăng tin → B nhận và giao → C xác nhận đã nhận hàng).
- 1 đơn `CANCELLED` — A huỷ đơn qua UI.
- 1 đơn `RETURNED` — ⛔ **KHÔNG dựng riêng**, tái dùng thẳng đơn của `SEED-DLV-03` (lô `DLV` đã đẩy tới trạng thái này qua `TC-DLV-063`). Lô `DLV` `BLOCKED` ⇒ `TC-ACT-015` `BLOCKED` và `TC-ACT-005` chỉ chạy được phần `COMPLETED` + `EXPIRED`.
- 1 đơn `EXPIRED` — ⛔ **không seed được qua UI**, cần dev/QA lùi "Đến ngày" hoặc chờ thời gian thật; gộp chung kế hoạch seed thời gian với `TC-DLV-057`/`TC-DLV-067` và `SC-TS-006`.

**Môi trường riêng cho `TC-ACT-018`:** throttle mạng về **3G chậm** — mạng nhanh thì pha loading trôi qua quá nhanh, không phân biệt được *đang tải* ⟷ *không có dữ liệu*.

## Test Cases

| Testcase ID | Scenario ID | Req ID | DOC Source | Group | Priority | Test Title | Pre-condition | Steps | Expected Result | Test Data | Status | Notes | Version Origin | Lifecycle | Assigned To |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-ACT-001 | SC-ACT-001 | REQ-ACT-001 | DOC-v1.1-01 · DOC-v1.1-02 | UI | P2 | Check màn Hoạt động dùng đúng bộ nhãn của app cho tên màn và hai tab | - Tài khoản A là CBNV có hồ sơ đầy đủ trên STG.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco từ host app FoxPro. (setup)<br>2. Nhấn tab "Hoạt động" ở bottom nav. (setup)<br>3. Check nhãn mục vừa nhấn ở bottom nav, tiêu đề màn và hàng tab con phía trên danh sách. | 3. Nhãn bottom nav là "Hoạt động"; tiêu đề màn là "Đơn của tôi"; hàng tab con có đúng 2 tab "Đang diễn ra" và "Đã hoàn thành", không có tab con thứ ba. | — |  | Đã sửa theo BA trả lời `C-ACT-02` 2026-09-16: assert cứng bộ nhãn của app, thay vì chỉ assert "có đúng 2 tab" như bản v1.0. · Mục đích: nhãn tab xuất hiện trong Steps của rất nhiều TC ở `ACT` và các module khác, chốt sai là phải sửa rải rác. · Căn cứ: BA chốt **theo app**; PRD `EMP-05`/`EMP-06`/`AC-09.1.01` ghi "Đang chạy"/"Hoàn tất" là **PRD chưa cập nhật** — ⛔ không assert theo PRD, app hiện đúng bộ nhãn này thì PASS. | v1.1 | MODIFIED |  |
| TC-ACT-005 | SC-ACT-005 | REQ-ACT-004 | DOC-v1.1-01 | Functional | P2 | Check tab Đã hoàn thành chứa đủ cả ba loại đơn kết thúc | - Tài khoản A là CBNV có hồ sơ đầy đủ trên STG, đã seed sẵn `SEED-ACT-03` (xem §0.4).<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco và nhấn tab "Hoạt động". (setup)<br>2. Nhấn tab con "Đã hoàn thành". (setup)<br>3. Check badge trạng thái của từng card trong danh sách. | 3. Danh sách có đủ ba card mang badge "Hoàn thành", "Hết hạn" và badge của đơn đã trả lại người gửi; KHÔNG có card nào badge "Chờ ghép", "Đã ghép", "Đang giao" hay "Đã giao". | 1 đơn `COMPLETED` + 1 đơn `EXPIRED` + 1 đơn `RETURNED` của `SEED-ACT-03` (test_data_catalog §ACT — Bộ 3 đơn kết thúc) |  | Đã sửa theo `AC-24.2.01` + BA trả lời `C-ACT-04(a)` 2026-09-17: bổ sung loại đơn thứ ba (`RETURNED`) vào danh sách phải có, bản v1.0 chỉ có 2 loại. · Mục đích: `RETURNED` là kết cục mới của v1.1, dev dễ quên xếp nó vào tab nào. · Căn cứ: chuỗi lý do của card "Hết hạn" verify ở `TC-ACT-008`, lý do hoàn hàng của card `RETURNED` verify ở `TC-ACT-015` — TC này chỉ chốt vị trí tab. Lô `DLV` chưa đẩy được đơn tới `RETURNED` ⇒ ghi `Blocked` phần đó, ⛔ không PASS cả TC. | v1.1 | MODIFIED |  |
| TC-ACT-008 | SC-ACT-008 | REQ-ACT-005 | DOC-v1.1-01 | UI | P3 | Check card đơn Hết hạn hiện badge và đúng chuỗi lý do chính thức | - Tài khoản A là CBNV có hồ sơ đầy đủ trên STG, đã seed sẵn `SEED-ACT-03` (xem §0.4) — gồm đơn "Hết hạn" cần dev/QA chuẩn bị trước vì ⛔ không tạo được qua UI.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco, nhấn tab "Hoạt động" rồi nhấn tab con "Đã hoàn thành". (setup)<br>2. Check badge và dòng lý do trên card đơn "Hết hạn". | 2. Card hiển thị badge "Hết hạn" kèm dòng lý do đúng chuỗi "Không có ai nhận mang giúp trong thời gian đăng". | Đơn `EXPIRED` của `SEED-ACT-03` (test_data_catalog §ACT — Text lý do "Hết hạn", ⛔ không seed được qua UI) |  | Đã sửa theo `DOC-v1.1-01 §8.5.1 BR05-03` + `AC-09.1.01` 2026-09-17: chuỗi lý do đổi sang bản **ngắn**, bỏ vế "— tin đã tự động đóng." mà bản v1.0 cùng ID đang assert. · Mục đích: 2 nguồn từng lệch phần đuôi câu; PRD v1.1 nay gọi tên chuỗi lần đầu nên thắng quan sát cũ `KB-ORD-07` (#4). App hiện bản dài ⇒ FAIL và log bug, ⛔ không hạ Expected về bản v1.0. · Chỉ ĐỌC card, ⛔ không tap — `C-ACT-03(c)` và `C-ORD-17` đang nói ngược nhau về việc card Hết hạn có tương tác được không. | v1.1 | MODIFIED |  |
| TC-ACT-012 | SC-ACT-012 | REQ-ACT-008 | DOC-v1.1-01 | UI | P2 | Check empty state tab Đang diễn ra hiện đúng chuỗi và đúng một nút đăng tin | - Tài khoản CBNV "trắng" trên STG = `SEED-ACT-02` (không có đơn nào ở trạng thái từ "Chờ ghép" đến "Đã giao") — dùng chung với `TC-ACT-014/016/017/018`, chuẩn bị 1 lần.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco bằng tài khoản "trắng". (setup)<br>2. Nhấn tab "Hoạt động". (setup)<br>3. Check vùng danh sách của tab "Đang diễn ra": icon, dòng tiêu đề, dòng giải thích và số lượng nút CTA. | 3. Vùng danh sách hiện icon nét mảnh màu neutral, dòng tiêu đề đúng chuỗi "Không có đơn đang thực hiện", 1 dòng giải thích, và đúng 1 nút CTA nhãn "Đăng tin gửi hàng". | Tài khoản 0 đơn đang hoạt động = `SEED-ACT-02` (test_data_catalog §ACT — Text empty state tab đang-chạy) |  | Đã sửa theo `C-ORD-06` Resolved 2026-09-15 (tài liệu đã phê duyệt, `EMP-05`): bản v1.0 chỉ assert "không có card nào" vì text empty state chưa có đặc tả; nay assert verbatim chuỗi + nhãn CTA. · Mục đích: cặp bất đối xứng với `TC-ACT-014` — tab này CÓ đúng 1 CTA, tab kia KHÔNG có; đây là nội dung phải assert, không phải chi tiết trình bày. · Căn cứ: `BR17-01` *"tối đa một CTA"* nên "không có" ở tab kia là lựa chọn thiết kế hợp lệ. | v1.1 | MODIFIED |  |
| TC-ACT-013 | SC-ACT-013 | REQ-ACT-009 | DOC-v1.1-01 | Business Rule | P2 | Check card đơn Hoàn thành không còn dấu vết sao điểm tier hay chỉ số môi trường | - Tài khoản A là CBNV có hồ sơ đầy đủ trên STG, đã seed sẵn `SEED-ACT-03` (xem §0.4).<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco, nhấn tab "Hoạt động" rồi nhấn tab con "Đã hoàn thành". (setup)<br>2. Check toàn bộ nội dung hiển thị trên từng card đơn trong danh sách. | 2. KHÔNG card nào chứa chuỗi sao, điểm số, nhãn tier hay chỉ số môi trường. | Đơn `COMPLETED` của `SEED-ACT-03` (test_data_catalog §ACT) |  | Đã sửa theo `DOC-v1.1-01 §8.14.1 BR14-03` + `§4 SCOPES` 2026-09-17: bản v1.0 chỉ assert "nhấn vào chuỗi sao không mở màn chấm điểm" và cố ý KHÔNG assert sự tồn tại của chuỗi leftover; nay **sự tồn tại của dấu vết đã là defect**. · Mục đích: kết quả phản trực giác so với bản cũ — người chạy quen v1.0 dễ cho PASS khi vẫn thấy ★ trên card. · Căn cứ: `§4` loại trừ vĩnh viễn ★/điểm/tier/chỉ số môi trường. Còn ★ ⇒ **log bug**, ⛔ KHÔNG diễn giải là "dấu vết phase sau". Chạy cùng lô với `SC-GIFT-011` để log 1 bug cho 2 bề mặt. | v1.1 | MODIFIED |  |
| TC-ACT-014 | SC-ACT-014 | REQ-ACT-008 | DOC-v1.1-01 | UI | P2 | Check empty state tab Đã hoàn thành hiện đúng chuỗi không có CTA và ẩn hẳn khối lịch sử | - Tài khoản CBNV "trắng" trên STG = `SEED-ACT-02` (chưa có đơn "Hoàn thành", "Hết hạn" hay đơn đã trả lại người gửi) — dùng chung với `TC-ACT-012/016/017/018`, không chuẩn bị lại.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco bằng tài khoản "trắng". (setup)<br>2. Nhấn tab "Hoạt động". (setup)<br>3. Nhấn tab con "Đã hoàn thành". (setup)<br>4. Check vùng danh sách: dòng tiêu đề, số lượng nút CTA và khu vực khối lịch sử. | 4. Vùng danh sách hiện dòng tiêu đề đúng chuỗi "Chưa có đơn hoàn tất", KHÔNG có nút CTA nào, và khối lịch sử bị ẩn hẳn chứ không hiện khung rỗng. | Tài khoản 0 đơn kết thúc = `SEED-ACT-02` (test_data_catalog §ACT — Text empty state tab hoàn-tất) |  | Đã sửa theo `C-ORD-06` Resolved 2026-09-15 (`EMP-06`): bản v1.0 chỉ assert "không có card nào"; nay assert verbatim chuỗi + KHÔNG CTA + ẩn hẳn khối lịch sử. · Mục đích: đối chứng âm cho `TC-ACT-012` — hai tab rỗng cạnh nhau nhưng khác nhau ở CTA và ở khối lịch sử; app hiện nút ở tab này là **sai spec**. · Căn cứ: `BR17-01` + `EMP-06`. | v1.1 | MODIFIED |  |
| TC-ACT-015 | SC-ACT-015 | REQ-ACT-004 | DOC-v1.1-01 | Business Rule | P2 | Check đơn đã trả người gửi hiện kèm lý do còn đơn đã huỷ vẫn bị ẩn | - Tài khoản A là CBNV có hồ sơ đầy đủ trên STG, đã seed sẵn `SEED-ACT-03` (xem §0.4) — gồm cặp đối chứng 1 đơn đã trả lại người gửi và 1 đơn "Đã huỷ".<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco và nhấn tab "Hoạt động". (setup)<br>2. Nhấn tab con "Đã hoàn thành". (setup)<br>3. Nhấn tab con "Đang diễn ra" rồi rà toàn bộ danh sách. (setup)<br>4. Nhấn lại tab con "Đã hoàn thành" và check cả hai đơn của cặp đối chứng trong cùng một lượt xem. | 4. Đơn đã trả lại người gửi CÓ trong danh sách tab "Đã hoàn thành" và hiện lý do hoàn hàng trên card; đơn "Đã huỷ" KHÔNG xuất hiện ở cả hai tab. | Cặp đối chứng 1 đơn `RETURNED` + 1 đơn `CANCELLED` cùng tài khoản của `SEED-ACT-03` (test_data_catalog §ACT — Cặp đối chứng CANCELLED ⟷ RETURNED) |  | Mục đích: hai kết cục bất thường nhưng hai cách xử lý ngược nhau — giá trị của TC nằm ở **phép đối chứng**, xem lẻ từng đơn thì không bắt được lỗi "gom chung mọi đơn bất thường vào một nhánh hiển thị". · Căn cứ: `AC-24.2.01` cho vế `RETURNED`; vế `CANCELLED` theo BA trả lời `C-ACT-04(b)` 2026-09-17 — đơn "Đã huỷ" **vẫn ẩn khỏi cả 2 tab**, rule v1.0 CÒN HIỆU LỰC, `§8.12.3` cho mọi vai *"Xem lý do"* đơn huỷ là **PRD chưa cập nhật**. Đơn "Đã huỷ" hiện ra ⇒ FAIL và log bug, ⛔ không sửa Expected theo `§8.12.3`. · Phụ thuộc lô `DLV` để có đơn `RETURNED` — lô đó `BLOCKED` thì TC này `BLOCKED`. | v1.1 | NEW |  |
| TC-ACT-016 | SC-ACT-016 | REQ-ACT-010 | DOC-v1.1-01 | UI | P3 | Check empty state không che thanh tab dưới ở cả hai tab rỗng | - Tài khoản CBNV "trắng" trên STG = `SEED-ACT-02` (không có đơn nào ở cả 2 tab) — dùng chung với `TC-ACT-012/014/017/018`, không chuẩn bị lại.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco bằng tài khoản "trắng". (setup)<br>2. Nhấn tab "Hoạt động". (setup)<br>3. Nhấn lần lượt từng mục trên thanh tab dưới rồi quay lại tab "Hoạt động".<br>4. Nhấn tab con "Đã hoàn thành".<br>5. Nhấn lần lượt từng mục trên thanh tab dưới rồi quay lại tab "Hoạt động", và check thanh tab dưới ở cả hai tab con. | 5. Thanh tab dưới hiển thị đủ 5 mục và mọi mục đều nhấn được ở cả hai tab con, không mục nào bị empty state che lấp. | Tài khoản 0 đơn ở cả 2 tab = `SEED-ACT-02` (test_data_catalog §ACT — Tài khoản trắng) |  | Mục đích: cặp với `TC-ACT-017` — che thanh tab dưới và khoá cuộn là hai lỗi layout khác bản chất, sửa được lỗi này không đảm bảo hết lỗi kia. · Căn cứ: `DOC-v1.1-01` §8.17.2 BR17-03 + `§6.2 AC-29.1.01`. `ACT` là màn duy nhất có 2 empty state cạnh nhau nên rule hình thức chung của `FR17` đặt home tại đây. | v1.1 | NEW |  |
| TC-ACT-017 | SC-ACT-016 | REQ-ACT-010 | DOC-v1.1-01 | UI | P3 | Check màn vẫn cuộn được khi đang hiện empty state ở cả hai tab rỗng | - Tài khoản CBNV "trắng" trên STG = `SEED-ACT-02` (không có đơn nào ở cả 2 tab) — dùng chung với `TC-ACT-012/014/016/018`, không chuẩn bị lại.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS. | 1. Đăng nhập FoxEco bằng tài khoản "trắng". (setup)<br>2. Nhấn tab "Hoạt động". (setup)<br>3. Vuốt lên và vuốt xuống trên vùng danh sách của tab "Đang diễn ra".<br>4. Nhấn tab con "Đã hoàn thành".<br>5. Vuốt lên và vuốt xuống trên vùng danh sách, và check phản hồi cuộn của màn ở cả hai tab con. | 5. Màn phản hồi thao tác cuộn ở cả hai tab con, không bị khoá cứng tại chỗ. | Tài khoản 0 đơn ở cả 2 tab = `SEED-ACT-02` (test_data_catalog §ACT — Tài khoản trắng) |  | Căn cứ: `DOC-v1.1-01` §8.17.2 BR17-03 — rule đòi empty state vừa không che thanh tab dưới vừa giữ màn cuộn được. | v1.1 | NEW |  |
| TC-ACT-018 | SC-ACT-017 | REQ-ACT-010 | DOC-v1.1-01 | UI | P3 | Check tab rỗng phân biệt rõ trạng thái đang tải với trạng thái không có dữ liệu | - Tài khoản CBNV "trắng" trên STG = `SEED-ACT-02` (không có đơn nào ở cả 2 tab) — dùng chung với `TC-ACT-012/014/016/017`, không chuẩn bị lại.<br>- Host app FoxPro đã cài trên thiết bị Android/iOS.<br>- Có sẵn công cụ throttle mạng về 3G chậm — không dựng được bằng thao tác trong app. | 1. Throttle mạng thiết bị về 3G chậm. (setup)<br>2. Đăng nhập FoxEco bằng tài khoản "trắng". (setup)<br>3. Nhấn tab "Hoạt động" và theo dõi liên tục vùng danh sách của tab "Đang diễn ra" từ lúc vào màn.<br>4. Nhấn tab con "Đã hoàn thành" và theo dõi liên tục vùng danh sách từ lúc chuyển tab.<br>5. Check chuỗi trạng thái hiển thị đã quan sát ở cả hai tab con. | 5. Ở cả hai tab con, vùng danh sách đi qua pha đang tải rồi chuyển hẳn sang empty state; pha đang tải kết thúc chứ không lặp skeleton vô hạn. | Tài khoản 0 đơn ở cả 2 tab = `SEED-ACT-02`; mạng throttle 3G chậm (test_data_catalog §ACT — Tốc độ mạng) |  | Mục đích: neo vào `[GAP]` của `SC-ACT-017` — điều kiện *"hiện empty state dựa cờ dữ liệu rỗng, không dựa null field"* là rule **backend**, ⛔ không verify được qua UI, nên TC chỉ assert phần quan sát được. · Căn cứ: `DOC-v1.1-01` §8.17 Pre-Conditions + §8.17.2 BR17-02. Bắt buộc throttle 3G chậm: mạng nhanh thì pha loading trôi qua quá nhanh, không phân biệt được hai trạng thái. | v1.1 | NEW |  |
