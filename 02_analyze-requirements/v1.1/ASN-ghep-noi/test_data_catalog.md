# Test Data Catalog — v1.1 · Module ASN (DELTA)

> Tạo bởi: analyze-requirements (DELTA 2026-09-15) · layout **module-first v2**.
> **Structure-lock:** giữ nguyên 6 cột `| Field | Loại data | Valid | Invalid | Boundary | Nguồn |`.
> Chỉ chứa data **mới/thay đổi** của lượt delta này. Bảng đầy đủ v1.0 (10 dòng, không đổi) xem `v1.0/ASN-ghep-noi/test_data_catalog.md` — KHÔNG lặp lại ở đây.

## Module ASN — Ghép nối (delta DOC-v1.1-01 §8.3/§8.4/§9)

| Field | Loại data | Valid | Invalid | Boundary | Nguồn |
|-------|-----------|-------|---------|----------|-------|
| Định nghĩa "khớp" (auto-match, BR04-01) | Fixture | đủ **3 điều kiện**: điểm lấy/giao trùng + khoảng ngày overlap + buổi di chuyển overlap | thiếu 1/3 điều kiện (→ không khớp) | **overlap = có phần chung, KHÔNG cần trùng tuyệt đối** — ranh giới "0 phần chung" vs "≥1 phần chung" | `DOC-v1.1-01` §8.4 BR04-01 |
| Chu kỳ quét auto-match (BR04-02) | Runtime | quét lại **≤ 60 giây** sau khi có tin mới | > 60s chưa quét (→ lỗi hiệu năng) | **60s là ngưỡng SLA**, không phải giá trị cố định — SC ghi nhận "trong ngưỡng", không assert giây chính xác | `DOC-v1.1-01` §8.4 BR04-02 |
| Số thông báo khớp cho 1 tin đăng OFFER *(thay BR04-04 — BA 2026-09-16)* | Runtime | **≤ 5** thông báo `NTF-03` cho **mỗi** tin OFFER; **KHÔNG có trần theo ngày** | > 5 thông báo cho cùng 1 tin OFFER | **6 tin NEED cùng khớp** ⇒ chỉ 5 thông báo. ✅ **Chốt 2026-09-17 (`C-ASN-04` Resolved, BA trả lời đủ (a)–(e)):** người nhận = **Carrier ĐĂNG TUYẾN**; chọn 5 tin theo **thời gian đăng** (tin đăng trước ưu tiên trước — **cùng 1 rule** với trần gợi ý 5 của `BR03-06`); <5 tin khớp thì gửi đúng số đó, 0 tin thì không gửi; tin NEED đăng SAU chỉ được gửi thêm nếu tuyến **chưa đủ 5**; mỗi tuyến có trần **ĐỘC LẬP** 5 (đăng 2 tuyến ⇒ tối đa 10, ⛔ không cộng dồn chung) | BA trả lời `C-NTF-02` 2026-09-16 · `RISK-ASN-08` **Closed** |
| Concurrency double-accept (NFR-06) | Runtime | **50 request đồng thời** → đúng 1 người ghép, **0%** trùng | > 0% trùng (→ FAIL bảo mật dữ liệu) | **50 request** là ngưỡng đo NFR, cần công cụ load/concurrency test — ⛔ không test tay được | `DOC-v1.1-01` §9 NFR-06 |
| Nút "Gọi" cạnh SĐT (sau khi lộ liên hệ) | Fixture · UI-evidence | Bấm "Gọi" mở **action sheet** (bottom sheet), header = tên người liên hệ + SĐT (vd "Đồng Công Chi Linh" / "0912 345 678"), rồi 3 lựa chọn: **"Gọi điện thoại"** · **"Gọi Zalo"** · **"Copy số điện thoại"** · dòng **"Huỷ"** để đóng | chọn ngoài 3 lựa chọn + Huỷ trên | Nguồn ảnh gốc demo còn có thêm **"Gọi Fchat"** (kênh nội bộ FPT) — theo yêu cầu QC, **KHÔNG đưa vào danh mục chính thức** để viết TC; nếu STG có thêm lựa chọn này thì không tính là thiếu sót/lệch, chỉ không assert. Component này dùng chung cho MỌI nơi có "SĐT + nút Gọi" (`ASN`, `FEED` Chi tiết tin, `DLV` Liên hệ người gửi) — không viết TC riêng từng module, chỉ 1 TC ở `ASN` (home rule lộ SĐT `BR-CON-02`) rồi cross-ref | `00_input/v1.1/design/Goi dien.png` — ảnh UI thật, QC cung cấp 2026-09-17 |
| Tập "Buổi" (field OFFER, đăng "Tôi nhận giao hàng") | Fixture · QA-obs | **4 lựa chọn quan sát trên demo 2026-09-17** (`https://giangdc.github.io/foxeco_demo/`, wizard OFFER, tài khoản Carrier): **"Sáng" (6–12h) · "Chiều" (13–17h) · "Sau giờ làm" (17–19h) · "Giờ nào cũng được"** | chọn ngoài 4 lựa chọn trên | Đây là **quan sát demo (`DOC-v1.1-03`), KHÔNG phải nguồn rule chốt** — PRD chỉ nói "tập buổi", không liệt kê tên; dùng làm test data tạm, ⛔ không coi là verbatim chính thức cho tới khi có Figma/STG hoặc BA xác nhận (`Project_rule §10.1`) | Vibe-check demo QA GiangDC2 2026-09-17 |
| Đồng bộ realtime 3 vai (NFR-08) | Runtime | 3 vai thấy cùng trạng thái trong **≤ 5 giây** | > 5 giây (→ FAIL NFR) | mốc **5 giây** đo trên 3 thiết bị thật | `DOC-v1.1-01` §9 NFR-08 |
| Request đọc tin OFFER qua API (NFR-11) | Fixture | request bởi **chủ tin** → cho phép | request bởi **không phải chủ tin** → chặn lỗi + ghi audit log | ranh giới **chủ tin vs không phải chủ tin** — cần công cụ gọi API trực tiếp (không thuần UI) | `DOC-v1.1-01` §9 NFR-11 |

## Ghi chú chung
- **Không đổi** so với v1.0: 3 mốc `3/5/6` (trần gợi ý/carrier) và ranh giới khớp nhị phân theo địa chỉ — bảng gốc `v1.0/ASN-ghep-noi/test_data_catalog.md` vẫn là nguồn cho các field đó.
- ✅ **`RISK-ASN-08` Closed 2026-09-16** — BA: *không có trần theo ngày, `BR04-04` dư*; rule hiện hành **5 thông báo / 1 tin đăng OFFER**. ⛔ Không hỏi admin giá trị cấu hình nữa. Chi tiết rule còn thiếu → `C-ASN-04`; phép so "trùng điểm" với địa chỉ tự do → `C-ASN-05`.
- 3 field NFR (`NFR-06`/`NFR-08`/`NFR-11`) đều cần **công cụ ngoài UI thuần** (concurrency tool / đa thiết bị đồng bộ / API client) — phù hợp giao automation/backend test hơn manual.
- ✅ **Đồng bộ realtime 3 vai (NFR-08) có bằng chứng demo trực tiếp 2026-09-17**: bấm "Tôi mang giúp được" ở demo 3-role (`https://giangdc.github.io/foxeco_demo/`) → cả 3 panel Sender/Carrier/Receiver cập nhật "Chờ ghép" → "Đã ghép" tức thì, không cần refresh — củng cố tính khả thi của `TC-ASN-008` (dù vẫn cần đo mốc ≤5s bằng thiết bị thật/STG, demo chỉ xác nhận cơ chế sync tồn tại).
- ⚠️ **Demo 3-role vẫn ghi đè đơn cũ khi đăng OFFER mới trong lúc có đơn NEED đang "Đã ghép"** (cùng model dữ liệu với demo `FoxEcoQC` cũ) — KHÔNG dùng để verify hành vi *tại thời điểm submit* kiểu `SC-ASN-018` (đăng tin thứ 2 không ghi đè tin thứ 1). **Nhưng** đã tìm ra cách lách được giới hạn này cho câu hỏi "card OFFER trông ra sao": điều hướng thẳng vào tab "Hoạt động" → "Đang diễn ra" (thay vì xem lại qua màn "Theo dõi đơn" sau submit, nơi bị ghi đè) — quan sát được card OFFER riêng biệt, đã dùng để đóng `C-ORD-16`/`C-ACT-04` 2026-09-17 (xem `CHANGELOG.md` các module đó).
