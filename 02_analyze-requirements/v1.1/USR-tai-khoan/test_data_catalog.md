# Test Data Catalog — v1.1 · Module USR

> Tạo bởi: analyze-requirements (DELTA 2026-09-15, lượt bù thứ hai) · layout **module-first v2**.
> Chỉ liệt kê data **mới/đổi** của lượt delta. Data v1.0 xem `v1.0/USR-tai-khoan/test_data_catalog.md`.

| Data | Loại data | Valid | Invalid | Boundary | Nguồn |
|------|-----------|-------|---------|----------|-------|
| Số điện thoại mặc định | Master | Giá trị ban đầu = **SĐT trên HRIS** (HRIS không có ⇒ rỗng); **bắt đầu bằng 0, đúng 10 ký tự, cả 10 là số** (vd `0901234567`) | **Để trống** · `123` · `84901234567` · `+84912345678` · `0912 345 678` (khoảng trắng) · `09123a5678` (chữ) · `1912345678` (không đầu 0) | ⭐ **10 số đầu 0** (hợp lệ) · **9 số** · **11 số** · **để trống** — đều chặn lưu | `DOC-v1.1-01` §8.15.1 BR15-02 · §8.15.2 · §6.2 AC-30.1.02 · BA chốt 2026-09-16. Chuỗi lỗi **theo demo** `DOC-v1.1-02` — BA xác nhận chính thức 2026-09-17 (PRD không đặc tả): trống ⇒ "Vui lòng nhập số điện thoại" · sai định dạng ⇒ "Số điện thoại không hợp lệ (10 số, bắt đầu bằng 0)" |
| Địa chỉ mặc định | Master | Mở màn: **giá trị đã lưu** nếu có; chưa lưu ⇒ **địa chỉ làm việc HRIS**; HRIS không có ⇒ **rỗng**. Sửa: gõ **≥ 3 ký tự** → chọn 1 văn phòng trong danh sách gợi ý. **Để trống hợp lệ** | Chuỗi gõ tay không chọn (`asdfghjkl1`, tên văn phòng gõ đủ nhưng không bấm chọn) ⇒ **rời ô thành rỗng**, lưu rỗng | ⭐ Gợi ý: **2 ký tự** (chưa hiện) · **3 ký tự** (hiện). ⭐ Nguồn giá trị: HRIS có / HRIS trống / đã lưu trước đó / đã lưu **rỗng** (mở lại vẫn rỗng). ⭐ Tìm theo **tên văn phòng**, không phân biệt dấu + hoa thường: `Tân Thuận` = `tan thuan` = `TAN THUAN`. ⭐ Từ khoá không khớp ⇒ **không hiện gợi ý**. ⛔ Biên 200/201 ký tự **hết hiệu lực** | BA chốt 2026-09-16 (`C-USR-05`) · `DOC-v1.1-01` §8.15.2 |
| Trường SSO (tên · phòng ban · MNV · email) | Master | Giá trị đồng bộ từ SSO, **chỉ đọc** | — | ⭐ **Email phải có icon KHIÊN** (PRD ghi "khoá" — BA chốt theo UI là khiên, 2026-09-16) và **không có ô nhập** — 2 assert riêng, không gộp | `DOC-v1.1-01` §8.15.1 BR15-01 · §6.2 AC-30.2.01 |
| Text banner lưu thành công | Master | `"Đã lưu thông tin của bạn"` — verbatim, **banner xanh** | Chuỗi gần đúng | Banner phải **tự ẩn khi người dùng sửa tiếp** (`BR15-05`), ⛔ không cần bấm đóng | `DOC-v1.1-01` §8.15.1 BR15-05 · §6.2 AC-30.1.01 |
| Cặp giá trị hồ sơ TRƯỚC/SAU | Runtime | Ghi lại SĐT **X** + địa chỉ **Y** trước khi sửa; đổi sang **X'** / **Y'** | — | ⭐ **Bắt buộc có bước "ghi lại giá trị TRƯỚC"** cho `SC-USR-014` · `017` · `018` — không có bước này thì 3 TC không phát hiện được gì | `DOC-v1.1-01` §8.15.1 BR15-03/BR15-04 |
| Đơn đã tạo TRƯỚC khi sửa hồ sơ | Runtime | ≥ 1 đơn đã đăng, **ghi lại SĐT + địa chỉ lấy hàng hiển thị trên đơn đó** | Đơn tạo **sau** khi sửa (không kiểm được rule) | ⭐ Cần **cả hai**: 1 đơn cũ (giữ giá trị cũ) + 1 đơn tạo mới sau khi sửa (dùng giá trị mới) — `AC-30.2.02` assert cả 2 vế | `DOC-v1.1-01` §8.15.1 BR15-03 · §6.2 AC-30.2.02 |
| Tài khoản có mục "Cập nhật thông tin" | Fixture | Tài khoản trên bản app **đã build `FR15`** | — | 🔴 App quan sát 2026-07-24 là **view-only hoàn toàn** ⇒ nếu STG chưa build thì **8 SC mới `BLOCKED`**, ⛔ không PASS (`RISK-USR-06`) | `DOC-v1.1-01` §8.15 Trigger vs `KP-01` §2 KB-USR-01 |
| Avatar (chữ viết tắt) | Master | Chữ cái đầu của **2 từ cuối** trong Tên, viết hoa: "Trần Văn A" → `VA` · "Đồng Công Chí Linh" → `CL` · **không dấu**: "Đặng Ánh" → `DA`. **Giống nhau** ở trang Cá nhân và màn "Cập nhật thông tin"; chỉ đọc | Icon người mặc định · chữ viết tắt lấy sai từ (vd `TV`, `TA`) · **còn dấu** (`ĐÁ`) · 2 màn hiện khác nhau | · tên **2 từ** ("Trần A" → `TA`) · chữ cái đầu **có dấu** ("Đặng Ánh" → `DA`). Tên luôn ≥ 2 từ ⇒ không có biên tên 1 từ | BA chốt 2026-09-16 (`C-USR-05(a)`) |
| Danh sách văn phòng | Master | ✅ `DOC-v1.1-04` `00_input/v1.1/location_address_catalog.xlsx` — **399** văn phòng; oracle = cột `name` (tìm theo bản bỏ dấu `search_text`) | — | Từ khoá đã đối chiếu: `fp` (2 ký tự, không gợi ý) · `fpt` (8) · `Lê Thái Tổ` (1) · `Cẩm Lệ`/`cam le`/`CAM LE` (2) · `xyz` (0) · `hcm` (0 theo tên — không tìm theo mã tỉnh) — chi tiết `04_test-data/valid/USR-office-catalog.md` | BA cung cấp 2026-09-16 |
| Tài khoản test cho `SC-USR-022` · `SC-USR-023` | Master | ✅ **ĐÃ CÓ 2026-09-18** (`DOC-v1.1-05`) — **C:** `FOXECO_STG_USER_C` (MNV 00041796), chưa từng lưu hồ sơ, HRIS có SĐT (10 số đầu 0) + địa chỉ `HCM LôB3,E-Office,KCN TânThuận`. Mapping vai + oracle: `04_test-data/valid/USR-accounts.md`; SĐT đọc từ `00_input/v1.1/datatest` (local, ⛔ không chép vào repo) · **D:** 🚫 **KHÔNG CÒN CẦN — DESCOPED 2026-09-18 (QC chốt):** HRIS bắt buộc **SĐT + địa chỉ làm việc** khi tạo nhân viên ⇒ **không tồn tại CBNV có HRIS trống**, nhánh này không xảy ra trong thực tế và không seed được data. `TC-USR-041`/`TC-USR-044` đánh `DESCOPED`, ⛔ không chạy | — | ⚠️ Chuỗi HRIS = **mã tỉnh + tên VP**; `DOC-v1.1-04` cột `name` = `LôB3,E-Office,KCN TânThuận` ⇒ prefill kỳ vọng **không có tiền tố `HCM`**, lệch thì ghi nhận + hỏi BA. 🕐 SĐT mới đổi 18/09, FoxEco có thể chỉ đồng bộ **sau** 18/09 ⇒ chạy `TC-USR-043` từ 19/09, lệch đúng ngày 18/09 ⇒ `BLOCKED`, ⛔ không log bug. Tài khoản đã lưu **một lần** thì không quay về trạng thái "chưa lưu" ⇒ ⛔ chạy `TC-USR-040`/`043` (C) trước khi dùng C cho TC khác | BA 2026-09-16 · QC cung cấp tài khoản 2026-09-18 (`DOC-v1.1-05`) |
| Khu vực/văn phòng · kênh liên hệ | — | — | — | ⛔ **KHÔNG đưa vào Test Data** — kênh liên hệ **bỏ hẳn** (BA 2026-09-16); khu vực/văn phòng chờ BA vòng 2 (`C-USR-05(b)`) | `DOC-v1.0-01` §A6 USR-02 vs `DOC-v1.1-01` §8.15 |

## Ghi chú
- ✅ **Tài khoản test đã có (2026-09-18)** — `DOC-v1.1-05` `00_input/v1.1/datatest`: 5 tài khoản đang làm việc + 1 đã nghỉ việc, pass dùng chung + OTP. Mapping vai A/B/C/D + thứ tự chạy bắt buộc + 3 gap còn lại: **`04_test-data/valid/USR-accounts.md`**. Credentials ở `~/.foxeco-v2/credentials.env` (ngoài repo).
- 🔴 **Tiền đề chặn nhất: app đã build `FR15` chưa.** Toàn bộ 8 SC mới phụ thuộc mục menu "Cập nhật thông tin" tồn tại. **Vibe-test trước `generate-tc`** (`RISK-USR-06`).
- ⭐ **Ba TC bắt buộc có bước "ghi lại giá trị TRƯỚC"**: `SC-USR-014` (kiểm persist thật, không tin banner) · `SC-USR-017` (đơn cũ không đổi) · `SC-USR-018` (hồ sơ không bị ghi đè). Thiếu bước này thì cả ba PASS giả.
- **SĐT mẫu dùng chung với `ORD`** — `BR01-08`/`§8.1.4` cũng dùng định dạng VN 10 số đầu 0 cho SĐT người gửi/người nhận/người uỷ quyền; chuẩn bị **1 bộ SĐT hợp lệ + invalid** dùng cho cả 2 module, ⛔ đừng làm 2 bộ.
- **Đơn `RETURNED` cho `SC-USR-005`**: tái dùng lô `DLV` `SC-DLV-053..056` và chạy **cùng lượt** với `SC-GIFT-014` + `SC-HOME-008` để so 3 màn bằng 1 đơn.
