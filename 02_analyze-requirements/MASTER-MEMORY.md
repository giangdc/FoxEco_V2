# MASTER-MEMORY — Cross-Version Registry

> Cập nhật lần cuối: 2026-09-15
> Active version: **v1.0** (thực thi/test) · **v1.1 đang DELTA analyze song song** — v1.0 pipeline (review-tc REJECTED, vibe-test/log-bug chưa chạy) vẫn còn dở nên KHÔNG áp dụng §10 Version Cutover; §8 dưới đây vẫn là bảng sống của v1.0.
>
> **⚠️ Tầng 1 (project) — ROUTER cross-version.** Chứa registry (version / DOC / TC-file / downstream path), quyết định cross-version. **KHÔNG chứa chi tiết SC/data/risk per-module** (sống ở tầng 3 module fragment). **KHÔNG lặp** cùng 1 bảng ở version `MEMORY.md`: §2 DOC Registry là **nguồn duy nhất** ở đây; §3 giữ **lifecycle cross-version**.

## 1. Version Registry
| Version | Release Date | Input Folder | Analyze Folder | Status | Tổng DOC | Tổng SC (all) | Tổng SC (new+mod) | Parent |
|---------|-------------|-------------|----------------|--------|----------|--------------|-------------------|--------|
| v1.0 | TBD | `00_input/v1.0/` | `02_analyze-requirements/v1.0/` | ANALYZED | 6 | 211 | 211 | — (version đầu của chuỗi phân tích mới) |
| v1.1 | TBD | `00_input/v1.1/` | `02_analyze-requirements/v1.1/` | **ANALYZED** (delta 10/11 module có thư mục · `FEED` sửa tại chỗ ở `v1.0/` — xem §3) | 4 | 302 | **Δ 134** (89 NEW + 45 MODIFIED) · **3 DEPRECATED** · ⚠️ **mẫu số phải test = 139** (134 + **5** SC `FEED` bị chạm, giữ lifecycle NEW — xem §3) | v1.0 |

> 📌 **v1.0 được PHÂN TÍCH LẠI TỪ ĐẦU ngày 2026-09-07** bằng bộ skill `qc-claude-v1 v1.1` (layout `module-first v2`, 11 module). Bản phân tích v1.0 **cũ** (bộ skill v1.0, layout `flat`, 8 module — 46 REQ / 92 SC / 323 TC) được lưu trữ ở `_handoff-v1.0/04_archive-project-v1.0/` và **KHÔNG phải parent** của bản này: cùng version, cùng tài liệu nguồn, khác phương pháp phân tích. ⛔ Không dùng archive làm baseline regression cho v1.0 (xem §9).

## 2. DOC ID Registry (Global)
> **`Status`** — trạng thái của FILE: `Active` = có trên disk trong `00_input/`, bản latest · `Superseded` = còn file nhưng đã có bản mới · `Removed <ngày>` = đã xoá khỏi working tree — **DOC-ID vẫn giữ**.
> ⚠️ **KHÔNG BAO GIỜ xoá dòng khỏi registry này.** Đổi `Status` + điền `Superseded by`.
> ⚠️ Đổi tên / gộp / xoá file input phải cập nhật cột `File` NGAY (`health-check` F-07).

| DOC ID | Version | File | Loại | Status | Superseded by | Modules |
|--------|---------|------|------|--------|---------------|---------|
| DOC-v1.0-01 | v1.0 | `Doc/FoxEco BRD v3.2.md` | Markdown — **BRD v3.2** (27/07/2026). **Nguồn nghiệp vụ chính, THẮNG khi mâu thuẫn về rule** | Active | — | USR, HOME, FEED, ORD, ACT, ASN, DLV, GIFT, CNL, NTF, TS |
| DOC-v1.0-02 | v1.0 | `Doc/tổng hợp từ file demo.docx` | Word — **PRD (Nháp)** tái dựng từ demo standalone. **THẮNG khi mô tả chi tiết màn hình/field**; ⛔ hành vi demo KHÔNG dùng làm oracle | Active | — | USR, HOME, FEED, ORD, ACT, ASN, DLV, GIFT, NTF |
| DOC-v1.0-03 | v1.0 | `Design/FoxEco Demo 3 vai tro (standalone) (2).html` | HTML — prototype tương tác 3 vai trò, **reference-only, ⛔ không trích làm nguồn rule** ⇒ cột `Modules` là **bề mặt prototype mô tả 3 màn này**, KHÔNG phải nơi citation — 0 citation trong `requirement_traceability.md` là ĐÚNG, không phải orphan | Active | — | FEED, ASN, DLV *(mô tả, không citation)* |
| DOC-v1.0-04 | v1.0 | `Design/Fox Eco Doc/` — gồm `images/*` (82 ảnh, nguồn UI chi tiết nhất) + `canvas.fig` (nguồn Figma gốc) + `meta.json` + `thumbnail.png` | Figma UI mockup. ⚠ status bar "9:41" = mẫu chuẩn Apple, **không phải screenshot máy thật** | Active | — | USR, HOME, FEED, ORD, DLV, GIFT, NTF |
| DOC-v1.0-05 | v1.0 | `Design/Screenshot From 2026-07-27 15-23-25.png` | Ảnh mockup màn Hoạt động (⚠ cùng cảnh báo "9:41") | Active | — | ACT |
| DOC-v1.0-06 | v1.0 | `_knowledge-pack/` (KP-01, 02, 03, 05, 06, 07 + `evidence/`) | Knowledge pack — kiến thức nghiệp vụ **NGOÀI tài liệu** (BA-chat · QA-obs · Figma · vibe-test). ⛔ KHÔNG là nguồn requirement gốc | Active | — | USR, HOME, FEED, ORD, ACT, ASN, DLV, GIFT, CNL, NTF, TS |
| DOC-v1.1-01 | v1.1 | `FoxEco PRD v1.0 - Gui Hang.pdf` | PDF — **PRD chính thức** (mã `1.0-BM/PM/HDCV/FTEL`, PRD Standard v1.2, rev 08/09/2026 "[A] Bổ sung flow exit giao hàng"). ⚠️ **Cùng tính năng "Gửi Hàng" đã phân tích ở v1.0** (khớp gần 1:1 cả 11 module qua FR01–FR18), KHÔNG phải sản phẩm mới — bản thân PRD tự nhận "MỚI HOÀN TOÀN" chỉ vì PM viết độc lập, không đối chiếu bộ phân tích v1.0. Chi tiết/chính thức hơn hẳn `DOC-v1.0-01/02` | Active | — | USR, HOME, FEED, ORD, ACT, ASN, DLV, GIFT, CNL, NTF, TS |
| DOC-v1.1-02 | v1.1 | `FoxEco Demo 3 vai tro (standalone) v4.0 (1).html` | HTML — prototype 3 vai trò bản **v4.0** (kế thừa `DOC-v1.0-03`), reference-only ⛔ không trích làm nguồn rule | Active | — | (mô tả, không citation — như `DOC-v1.0-03`) |
| DOC-v1.1-03 | v1.1 | `design/` — **31 ảnh** PNG chụp demo `DOC-v1.1-02` (tiền tố tên file = module: `ASN_01..03` · `DLV_01..07` · `FEED_01..02` · `HOME_01..04` · `NTF_01..03` · `ORD_08` ×2 · `TS_01..05` · `USR_01..02`; **5 ảnh QC bổ sung 2026-09-17 KHÔNG theo tiền tố:** `Goi dien.png` (ASN/FEED/DLV — action sheet nút Gọi) · `gia tri hang uoc tinh.png` (**ORD** — wizard Bước 1/3) · `gift_tang_qua_man_doncuatoi.png` (GIFT) · `ORD_08_card toi nhan giao hang.png` · `ORD_08_dangtin_offer_ghide_len_don_dang_co.png`) | Ảnh bằng chứng UI từ vibe-check demo 2026-09-15/16/17 — **evidence phụ**, ⚠ demo ≠ STG; ⛔ không trích làm nguồn rule (rule lấy từ `DOC-v1.1-01` hoặc câu trả lời BA). Đăng ký 2026-09-16 theo health-check F-08; +5 ảnh 2026-09-17 | Active | — | ASN, DLV, FEED, GIFT, HOME, NTF, ORD, TS, USR *(Source Location của CL/SC)* |
| DOC-v1.1-04 | v1.1 | `location_address_catalog.xlsx` | Excel — **danh mục 399 văn phòng FTEL** (cột `name` · `search_text` · mã tỉnh · toạ độ). BA cung cấp 2026-09-16 làm **nguồn danh sách gợi ý "Địa chỉ mặc định"**. Dữ liệu master, không phải tài liệu rule; ⚠ 118 `name` bị cắt 30 ký tự, toạ độ MISSING toàn bộ. Profile + từ khoá test: `04_test-data/valid/USR-office-catalog.md` | Active | — | USR *(prefill sang ORD, ASN)* |
| DOC-v1.1-05 | v1.1 | `datatest` | Text — **tài khoản test STG + giá trị HRIS mặc định** (5 tài khoản đang làm việc + 1 đã nghỉ việc: email `stag_*@fpt.com` · MNV · SĐT · văn phòng HRIS; pass dùng chung + OTP). QC GiangDC2 cung cấp 2026-09-18 làm **nguồn tài khoản + oracle prefill HRIS** cho `TC-USR-040..044`. 🔒 File chứa credentials ⇒ **đã đưa vào `.gitignore`, KHÔNG commit**; pass tách sang `~/.foxeco-v2/credentials.env`. Mapping vai A/B/C/D + gap: `04_test-data/valid/USR-accounts.md` — ✅ **file này ĐÃ ĐƯỢC TẠO 2026-09-19** (trước đó bị 6+ chỗ trích dẫn nhưng **chưa từng tồn tại / chưa từng commit** ⇒ link chết). 🔑 **QC cấp lại 5 tài khoản 2026-09-19** (`stag_giangdc2` · `stag_anhdc4` · `stag_anhptm17` · `stag_taipm` · `stag_huyennhk`) qua `04_test-data/account.txt` — 🔒 **đã gitignore**; secret tách sang `~/.foxeco-v2/credentials.env` (`chmod 600`, đã tạo). 🔴 **OTP staging CỐ ĐỊNH, dùng chung mọi account ⇒ AI TỰ ĐĂNG NHẬP ĐƯỢC** — mọi ghi chép cũ *"OTP nhập tay, AI không lấy được"* (VR-001 §0 · VR-003 · VR-004 ledger) **ĐÃ LỖI THỜI**, TC từng `NOT_RUN` chỉ vì OTP nay chạy được. ⚠ Địa chỉ HRIS dạng **mã tỉnh + tên VP** (`HCM LôB3,E-Office,KCN TânThuận`) ⇒ khác cột `name` của `DOC-v1.1-04`; 🔴 **không có tài khoản HRIS trống** ⇒ `TC-USR-041/044` BLOCKED | Active | — | USR *(tài khoản dùng chung cho ORD, ASN, DLV, GIFT)* |

**Ghi chú registry:**
- **`DOC-v1.1-01` CÓ kéo `USR` vào delta** *(sửa 2026-09-15 — ghi chú trước đó SAI)*. Bản trước ghi *"FR15 không áp dụng ở v1.1 ⇒ `USR` không có delta"* vì cho rằng `FR15` (cho sửa SĐT/địa chỉ mặc định) **xung đột** với `SC-USR-003`/`C-USR-03` (màn Cá nhân view-only, chốt 2026-07-24 theo quan sát app). ⛔ **Lập luận đó nhầm một xung đột CẦN PHÂN TÍCH thành lý do bỏ qua cả FR.** Bằng chứng rõ nhất: chính lượt delta đó đã trích `AC-30.1.01` — một AC **thuộc `FR15`** — làm nguồn resolve `C-ORD-10`. Hiện hành: `FR15` **áp dụng**, `C-USR-03` **bị đảo** (có màn `"Cập nhật thông tin"` sửa được **đúng 2 trường**; 4 trường SSO vẫn chỉ đọc). Phân tích đầy đủ: `v1.1/USR-tai-khoan/`.
- **Đánh số giữ nguyên theo đợt v1.0 cũ** (01 = BRD · 02 = PRD demo · 03 = prototype HTML · 04 = ảnh Figma) để citation trong `_handoff-v1.0/04_archive-project-v1.0/` và `KP-04` còn đối chiếu được. `05` (screenshot) và `06` (knowledge pack) là DOC-ID **mới** của lượt này.
- **`DOC-v1.0-06` KHÔNG phải tài liệu yêu cầu.** Toàn bộ `§D3` Functional Requirements, `§D4` Business Rules, `§D1b` User Story + AC, `§D8` Validate rules nằm ở `DOC-v1.0-01`. KP chỉ chứa **phán quyết clarification** và **hành vi app thật** — dùng để quyết định khi 2 nguồn tài liệu mâu thuẫn.
- **Thứ tự thắng khi mâu thuẫn** (áp dụng suốt lượt phân tích này): (1) **`DOC-v1.0-06`** nếu có phán quyết BA/PO có ngày hoặc bằng chứng app/Figma cụ thể → (2) **`DOC-v1.0-01`** (BRD, bản mới nhất 27/07) → (3) **`DOC-v1.0-02`** (PRD demo) → (4) `DOC-v1.0-04`/`05` (mockup, chỉ đối chiếu cấu trúc) → (5) `DOC-v1.0-03` (prototype, reference-only). Mỗi lần áp thứ tự này đều ghi rõ ở Analyst Note của REQ/CL tương ứng.
- **BRD v3.1** (bản cũ hơn) **KHÔNG** nằm trong `00_input/` — lưu ở `_handoff-v1.0/04_archive-project-v1.0/docs-superseded/`, đúng rule input retention (chỉ bản latest trong input).
- **Lấy lại file đã xoá:** `git show <commit-trước-khi-xoá>:'00_input/v1.0/<path>'`.
- 📌 **Cách `DOC-v1.1-03` được cite — ghi lại để `health-check` khỏi báo nhầm orphan (2026-09-17).** Cột `Modules` của nó liệt **9 module** và **cả 9 đều cite thật**, nhưng phần lớn cite **bằng TÊN FILE ảnh** (`DLV_04_...png`, `USR_01_...png`) chứ không kèm chuỗi `DOC-v1.1-03`: chỉ `ORD` (9 lần) và `ASN`/`FEED` (1 lần) ghi kèm DOC-ID. ⇒ Đếm theo chuỗi DOC-ID sẽ ra *"chỉ 2/9 module cite"* và kết luận **orphan oan**. Đếm đúng là **grep tên file ảnh**. 🟡 Nợ hình thức (không chặn gì): 7 module nên bổ sung tiền tố `DOC-v1.1-03` trước tên ảnh khi có dịp sửa, để Source Location truy được bằng 1 phép tìm duy nhất.

## 3. Scenario Lifecycle (Cross-Version)
> Version ĐẦU (tất cả NEW) — dùng roll-up gọn theo Module. Từ version delta trở đi liệt kê per-SC cho MODIFIED/CARRIED/DEPRECATED.

| Module | Origin | Lifecycle | Count |
|--------|--------|-----------|-------|
| USR | v1.0 | NEW | 12 |
| HOME | v1.0 | NEW | 24 |
| FEED | v1.0 | NEW | 14 |
| ORD | v1.0 | NEW | 51 |
| ACT | v1.0 | NEW | 14 |
| ASN | v1.0 | NEW | 18 |
| DLV | v1.0 | NEW | 30 |
| GIFT | v1.0 | NEW | 12 |
| CNL | v1.0 | NEW | 13 |
| NTF | v1.0 | NEW | 16 |
| TS | v1.0 | NEW | 7 |
| **Tổng** | | | **211** |

### v1.1 (delta — 10/11 module)
> Từ version delta trở đi liệt kê **per-SC** cho MODIFIED / DEPRECATED; NEW gom theo dải; CARRIED không liệt kê lại.
> ✅ Bảng **đã đóng**: 10 module có thư mục v1.1; `FEED` không dựng thư mục — PRD có chạm bề mặt Bảng tin nhưng thay đổi 2026-09-16 (`SC-FEED-009`/`013` viết lại, giữ lifecycle NEW) được **sửa tại chỗ ở `v1.0/FEED-bang-tin/`**. ⚠️ `USR` được bù ngày 2026-09-15 sau khi phát hiện khai nhầm *"không có delta"* — xem `v1.1/USR-tai-khoan/CHANGELOG.md §1`.

| Module | NEW (dải) | MODIFIED (per-SC — giữ ID v1.0, bản v1.1 authoritative) | DEPRECATED | Tổng tới v1.1 |
|---|---|---|---|--:|
| USR | `SC-USR-013..024` (12 SC) | `SC-USR-002` · `SC-USR-003` · `SC-USR-007` · `SC-USR-011` · `SC-USR-012` | — | 24 |
| HOME | `SC-HOME-025..030` | `SC-HOME-008` · `SC-HOME-019` · `SC-HOME-021` | `SC-HOME-024` · `SC-HOME-010` | 30 |
| FEED ⚠️ | `SC-FEED-015` *(sửa tại chỗ ở v1.0, 2026-09-17)* | **`SC-FEED-002`** · `SC-FEED-007` · `SC-FEED-009` · `SC-FEED-013` | — | 15 *(không có thư mục v1.1)* |
| ORD | `SC-ORD-052..065` (14 SC — gồm `SC-ORD-059`, xem ⚠️ dưới bảng) | `SC-ORD-005` · `012` · `025` · `026` · `028` · `029` · `036` · `041` · `043` · `044` · `045` · `050` | — | 65 |
| ACT | `SC-ACT-015..017` | `SC-ACT-001` · `005` · `008` · `012` · `013` · `014` | — | 17 |
| ASN | `SC-ASN-019` | `SC-ASN-006` · `008` · `011` · `014` · `015` | — | 19 |
| DLV | `SC-DLV-031..059` · `061..064` (**33 SC**) | — *(3 REQ đổi nhưng SC gắn vào là SC mới)* | **`SC-DLV-060`** | 64 |
| GIFT | `SC-GIFT-013` · `SC-GIFT-014` | `SC-GIFT-002` · `003` · `006` · `007` · `008` · `011` | — | 14 |
| CNL | `SC-CNL-014..017` | `SC-CNL-004` · `006` · `009` · `010` · `012` | — | 17 |
| NTF | `SC-NTF-017..022` | `SC-NTF-005` · `008` · `014` | — | 22 |
| TS | `SC-TS-008..015` | — | — | 15 |
| **Tổng** *(lifecycle, 10 module delta)* | **89 NEW** | **45 MODIFIED** | **3 DEPRECATED** | **302** |

> ⚠️ **`SC-ORD-059` là `NEW`, KHÔNG phải `MODIFIED` — đã đính chính 2026-09-17 (lượt `health-check` thứ 2).**
> Ngày 2026-09-17 nó từng bị đổi nhãn `NEW → MODIFIED` khi BA đảo Then theo `C-ORD-14`. **Nhãn đó sai:** `MODIFIED` ở framework này
> có nghĩa hẹp — *"SC có từ version trước, giữ nguyên ID v1.0, bản v1.1 authoritative"*. `SC-ORD-059` **sinh ra ở v1.1**
> (v1.0 chỉ có `SC-ORD-001..051`), nên **đảo Then trong cùng version là sửa NỘI DUNG, không đổi lifecycle**.
> 🔬 **Phép kiểm bắt được nó — dùng lại mỗi lượt health-check:**
> `carried + modified + deprecated_từ_version_trước` của module ở v1.1 **phải bằng** tổng SC của chính module đó ở v1.0.
> Chạy 2026-09-17: **10/11 module khớp tuyệt đối**, riêng `ORD` ra `39+13+0 = 52` trong khi v1.0 có `51` — thừa **đúng 1**,
> chỉ thẳng vào SC bị dán nhãn sai.
> ⚠️ **Đọc đúng vế `deprecated`, đừng lấy thẳng `counts.deprecated`** *(bổ sung 2026-09-17 sau `review-tc` B-1)*:
> ô `deprecated` gộp **2 loại khác hẳn nhau** — ① SC **sinh ở version TRƯỚC** rồi bị bỏ (`SC-HOME-010/024`) ⇒ **CÓ** vào vế trái;
> ② SC **sinh Ở CHÍNH version này** rồi bị descope ngay trong nó (`SC-DLV-060` — born v1.1, descope v1.1) ⇒ **KHÔNG** vào vế trái,
> vì nó chưa từng tồn tại ở v1.0. Lấy thẳng `counts.deprecated` sẽ làm `DLV` ra `30+0+1 = 31 ≠ 30` và **báo CRITICAL oan**.
> **Cách phân loại bằng máy:** SC có số thứ tự nằm trong **dải NEW của version này** (`<module>/CHANGELOG.md` frontmatter `id_range`)
> ⇒ loại ②. Hiện v1.1 có **đúng 1** SC loại ②: `SC-DLV-060`.
> ✅ Chứng cứ độc lập thứ 2: `TC-MASTER-v1.1.xlsx` ghi `TC-ORD-075`/`076` (phủ `SC-ORD-059`) là `Lifecycle = NEW`.
> ⛔ **Bài học:** "kết luận bị đảo" ≠ "lifecycle đổi". Đảo oracle trong cùng version ⇒ ghi ở `CHANGELOG §2 Kết luận bị đảo`,
> **không** đụng cột `Lifecycle`.

> ⚠️ **Đọc dòng `FEED` cho đúng — đây là chỗ 2 nghĩa bị trộn và đã gây drift số suốt các lượt trước.**
> **5** SC của `FEED` (**`002`** · `007` · `009` · `013` · `015`) **bị chạm ở v1.1 nhưng GIỮ lifecycle `NEW`** (home vẫn ở `v1.0/FEED-bang-tin/`, frontmatter `counts:` của nó ghi `new: 15 · modified: 0`).
> ⇒ Chúng **KHÔNG** cộng vào 2 cột `NEW`/`MODIFIED` của dòng **Tổng** — dòng Tổng là **lifecycle của 10 module delta**, khớp `counts:` frontmatter.
> 📐 **Hai con số khác nhau, đừng lẫn:**
> · **Δ lifecycle = 134 SC** (89 NEW + 45 MODIFIED) — dùng khi nói "v1.1 sinh/đổi bao nhiêu SC".
> · **Phải test lại = 139 SC** (134 + **5** SC `FEED` bị chạm) — dùng làm mẫu số regression (§4) và mẫu số sinh TC.
> ⛔ Con số **139** (dùng 2026-09-17 lượt sáng) **SAI** — bỏ sót `SC-FEED-002`, phát hiện ở `review-tc` M-1/M-2 cùng ngày.
> 🔬 **Bằng chứng `SC-FEED-002` thuộc nhóm bị chạm:** `v1.0/FEED-bang-tin/test_scenario_map.md` ghi *"hết gap CTA 2026-09-16"* + `DOC Source` = *vibe-check demo 2026-09-16*; fragment `TC-FEED-v1.1.md` khai *"4 TC ứng với 4 SC vừa đổi Then"* gồm `TC-FEED-002`; và `TC-FEED-002` trong TC-MASTER mang `Lifecycle = MODIFIED`. Ba nguồn độc lập cùng nói nó bị sửa.
> 🔴 Nguồn canonical của cả hai: frontmatter `counts:` ở `<module>/test_scenario_map.md`. Bảng này là **bản dẫn xuất** — lệch thì file module thắng.

> 📌 **3 SC DEPRECATED** — `SC-HOME-024` · `SC-HOME-010` (BA chốt `C-HOME-05`) · **`SC-DLV-060`** (*descope role admin, `review-tc` B-1 2026-09-17 — giữ `NEW` trong khi đã out-of-scope làm `R2-01` báo đỏ oan mỗi lượt review*) — theo `Project_rule §Quy ước đếm scenario`, cả 3 **vẫn trong tổng 302** nhưng **không** vào P1/P2/P3 (40+171+88 = 299).
> 📌 **SC MODIFIED giữ nguyên ID v1.0 và bản v1.1 là authoritative** — ⛔ không sửa bản v1.0 của chúng. 🔴 **Hai SC có Then NGƯỢC NHAU giữa 2 bản, và cả hai bản đều chạy được nên không có gì báo lỗi khi lấy nhầm:** `SC-CNL-006` (v1.0: PASS = *không thấy* nút Báo sự cố · v1.1: PASS = *thấy*) và `SC-USR-003` (v1.0: PASS = *không có control sửa nào* · v1.1: chỉ vùng SSO chỉ đọc, **có** lối vào màn sửa). Điểm chung: cả hai kết luận v1.0 đều Resolved **theo quan sát app**, không theo tài liệu.

## 4. Regression Scope
### v1.0
**Phải test (new + modified):** toàn bộ **211 SC** (version đầu của chuỗi phân tích mới — không có SC CARRIED).

**Nên regression (carried — high risk):** — *(không có: v1.0 không có parent trong chuỗi phân tích mới)*

**Không cần test (carried — low risk, stable):** — *(không có)*

> 📌 **Bộ TC baseline của đợt v1.0 cũ (323 TC) KHÔNG được dùng làm regression scope.** Lý do: (a) **0/323 TC có Scenario ID** (`_handoff-v1.0/03_tc-baseline-v1.0/TC-SC-MAPPING-TODO.md`) ⇒ không map được sang 211 SC mới; (b) bộ TC cũ có **5 lỗi đã biết** (`KP-04 §4`); (c) chỉ **~17/323 TC** từng chạy thật trên app. ⇒ Quyết định 2026-09-07: coi trạng thái hiện tại là **baseline mới**, chuỗi `--delta` bắt đầu từ version kế tiếp.

### v1.1 — Regression Scope

**Phải test (new + modified):** **139 SC** = 89 NEW + 45 MODIFIED *(10 module delta, theo `counts:` frontmatter)* + **5 SC `FEED` bị chạm** (**`SC-FEED-002`** · `007` · `009` · `013` · `015` — giữ lifecycle NEW, home ở `v1.0/`). Đây là mẫu số bắt buộc của v1.1.
> ✅ **Đối chiếu độc lập:** `TC-MASTER-v1.1.xlsx` sinh TC cho đúng tập này — **139/139 SC có TC = 100%** *(sau khi `SC-DLV-060` chuyển `DEPRECATED` 2026-09-17, `review-tc` B-1 — nó rời mẫu số nên không còn là "SC hở")*. ⛔ 2 con số cũ đều SAI, đã sửa 2026-09-17: *"132 SC = 88 NEW + 44 MODIFIED"* (health-check G-02) rồi *"139 SC"* (review-tc M-2, sót `SC-FEED-002`).

**Nên regression (carried — high risk):** SC của 3 module `Risk Level = High` mà v1.1 có chạm tới — `ORD` (39 carried) · `ASN` (13) · `DLV` (30) · `CNL` (8, nâng High ở v1.1). Ưu tiên cụm liên quan trực tiếp tới thay đổi: vòng đời đơn · nhật ký · ghép nối.

**Không cần test (carried — low risk, stable):** `FEED` (**10/15**) — ⚠️ **trừ **`SC-FEED-002`** (card tin — hết gap CTA 2026-09-16) · `SC-FEED-007` (tên hiện trước ghép) · `SC-FEED-009` (ảnh bản đồ tĩnh vẽ tuyến) · `SC-FEED-013` (empty state `EMP-04`) · `SC-FEED-015` (mới — thiếu toạ độ)** phải test lại vì Then đổi 2026-09-16/17; ngoài ra `SC-FEED-010/011/012` là P1/bug đã biết nên vẫn nên regression. `GIFT` carried (6) · `NTF` carried (13) · `USR` carried (9) rủi ro thấp.

> ⚠️ **3 SC DEPRECATED** — `SC-HOME-024` · `SC-HOME-010` · **`SC-DLV-060`** (tất cả 2026-09-17) — bỏ khỏi mọi mẫu số pass-rate.
> 🔴 **Trừ ra khỏi mẫu số cho tới khi gỡ nợ — CHỈ còn nợ MÔI TRƯỜNG/APP, ⛔ không còn nợ clarification:**
> · `SC-GIFT-013/014` + `SC-ACT-015` + phần `RETURNED` của `SC-DLV-053..056` — chờ nhánh `FR09` có trên STG
> · `SC-CNL-015` — chờ trạng thái `INCIDENT` có trên STG
> · **`SC-USR-013..020` (8 SC)** — chờ xác nhận app đã build `FR15` (app quan sát 2026-07-24 còn view-only hoàn toàn)
> Verdict đúng khi chưa gỡ là **BLOCKED**, ⛔ không PASS.
> ✅ **GỠ 2026-09-17 (health-check G-06b):** `SC-ORD-058..060` *(nợ `C-ORD-13`)* và `SC-ORD-031..035` *(nợ `C-ORD-04`)* **trở lại mẫu số** —
> `C-ORD-13` Resolved (BA cấp nguồn HRIS/stag + email mẫu + tên miền + hành vi nhánh nghỉ việc) · `C-ORD-04` Resolved (BA: chip *Thuốc/Y tế* **giữ, không chặn/không cảnh báo**; banner + điều khoản hàng cấm **không có ở v1.1**).
> 📌 Cả hai đã có TC trong `TC-MASTER-v1.1.xlsx` ⇒ để trong danh sách trừ là **hạ thấp mẫu số một cách sai**.
> 📌 `NTF` · `TS` vẫn đang chờ PM xác nhận scope Phase 1 (`KP-03 §3.1`) — nhưng **PRD v1.1 đặc tả đầy đủ cả hai** và đặt `FR13`/`FR16` vào business process chính ⇒ **mặc định lập kế hoạch là IN scope**. `CNL` **đã xác nhận IN scope 2026-09-17** (`RISK-CNL-06` Resolved, QC GiangDC2) — không còn chờ.

## 5. Version Comparison

### v1.0 → v1.1

| Chỉ số | v1.0 | v1.1 | Δ |
|---|--:|--:|--:|
| Module có phân tích | 11 | 11 *(10 có delta + 1 giữ nguyên)* | — |
| REQ | 116 | **142** | +26 |
| SC (tổng, gồm DEPRECATED) | 211 | **302** | **+89 NEW** · **45 MODIFIED** giữ ID · **3 DEPRECATED** *(⚠️ `SC-ORD-059` giữ `NEW` — nhãn `MODIFIED` ngày 2026-09-17 đã đính chính, xem §3)* · +**5** SC `FEED` bị chạm nhưng giữ lifecycle NEW ⇒ **mẫu số phải test = 139** |
| P1 / P2 / P3 | 27 / 117 / 67 | **40 / 171 / 89** | +13 / +54 / +22 |
| Clarification | 35 | **67** | +5 mở mới lượt delta 2026-09-15 · **+25 mở mới lượt UPDATE 2026-09-16** (rà sâu sau khi BA trả lời) · 1 mở lại |
| CL còn Open | 17 | **1** *(2026-09-18 — **mở lại `C-USR-07`**; trước đó là 0 từ 2026-09-17 tối. `C-USR-07` = câu (v) của `C-USR-05` — *"địa chỉ mặc định còn cho bỏ trống không?"* — **BA chưa từng trả lời thẳng** nhưng `C-USR-05` vẫn bị đóng `Resolved`, tới `vibe-test VR-003` mới lộ ra: app **cho lưu hồ sơ với ô địa chỉ TRỐNG**, 2 lần liên tiếp, không chặn không báo lỗi. ⇒ nay **10/11 module** hết điểm hỏi BA. Vẫn còn **1 Partially**: `C-DLV-08` (a)(b) — QA tự xác minh qua vibe-check block LỊCH SỬ, không chờ BA)* | −16 · Σ`cl` = **68** · Σ`cl_resolved` = **66** — nguồn canonical: `counts:` ở `<module>/risk_assessment.md` |
| RISK | 64 | **87** | +23 |
| DOC nguồn | 6 | **4** *(v1.1: `DOC-v1.1-01` PRD · `-02` prototype · `-03` 31 ảnh demo · `-04` danh mục văn phòng)* | PRD chính thức thay thế BRD+PRD-demo làm nguồn chính. ⛔ Con số cũ *"2"* SAI (chỉ đếm 2 DOC đầu), sửa 2026-09-17 |

**Điều gì thực sự đổi (không phải số):**
1. **Nguồn nghiệp vụ đổi hạng.** v1.0 chạy trên BRD v3.2 + một PRD *tái dựng từ demo*; v1.1 có **PRD chính thức của PM** (`1.0-BM/PM/HDCV/FTEL`) với 18 FR, ~90 business rule, 31 US và bộ AC đầy đủ. Phần lớn Δ là **chi tiết hoá**, không phải tính năng mới.
2. **Nhánh giao-không-thành-công là tính năng mới thật.** `FR08`/`FR09` sinh ra 3 trạng thái chưa từng có (`RESCHEDULED` · `RETURNING` · `RETURNED`) và kéo theo delta ở `DLV` (34 SC) · `NTF` (6 sự kiện) · `GIFT` (2 SC) · `ACT` (1 SC).
3. **Hai bug đã biết chuyển hạng.** `SC-CNL-010` (huỷ nhận xoá log) và `SC-CNL-009` (huỷ đơn không ghi log) ở v1.0 dựa trên **override của QA**; `BR11-02`/`BR11-03` nay nói trúng đúng hành vi lỗi ⇒ thành **vi phạm đặc tả đã phê duyệt**, `SC-CNL-010` nâng P2→P1.
4. **HAI kết luận bị đảo 180°, và cả hai cùng một kiểu.** `C-CNL-01` (*"màn Báo sự cố out of scope"* → `FR16` đưa vào scope) và `C-USR-03` (*"hồ sơ view-only hoàn toàn"* → `FR15` cho sửa 2 trường). ⭐ **Điểm chung đáng ghi nhớ: cả hai đều là CL được Resolved theo QUAN SÁT APP, không theo tài liệu** — đó là loại phán quyết dễ bị đảo nhất khi có doc mới. Hệ quả kiểm thử: `SC-CNL-006` và `SC-USR-003` có Then **ngược nhau giữa 2 bản**, và **cả hai bản đều chạy được** nên lấy nhầm bản sẽ cho kết luận ngược mà không có gì báo lỗi.
5. **Xuất hiện loại vấn đề mới: tài liệu tự mâu thuẫn.** `C-ORD-04` — `§8.1.4` cho *"Thuốc/Y tế"* là loại hàng hợp lệ trong khi `BR01-07` xếp *"thuốc"* vào hàng cấm. ⛔ Không phân xử được bằng thứ tự ưu tiên nguồn vì **cả hai vế cùng là `DOC-v1.1-01`**.
6. **Tích hợp hệ thống ngoài được nêu tên lần đầu** — Danh bạ nội bộ (`BR01-09`), ảnh hưởng tiền đề test (`C-ORD-13`).
7. **Nhóm rule "bằng chứng đã ghi thì bất biến" nay có 4 thành viên** — `BR11-03` (`SC-CNL-010`) · `BR18-05` (`SC-ORD-065`) · `NFR-07` (`SC-DLV-062`) · `BR15-03`/`BR15-04` (`SC-USR-017`/`018`). App **đã vi phạm nhóm này 1 lần đã live-verify** (`KB-CNL-01`) ⇒ chạy cùng lô, cùng vỡ thì gộp **1 bug report cho nguyên nhân gốc**.

## 6. TC Files Registry
| Version | TC-MASTER File | Tổng TC | Ngày consolidate | Status |
|---------|---------------|---------|-------------------|--------|
| v1.0 | `03_test-cases/v1.0/TC-MASTER-v1.0.xlsx` | 219 | 2026-09-07 | ✅ Consolidated · ❌ **Review 0/100 REJECTED** (2026-09-07, G1 FAIL) — 13 sheet (Overview + ALL + 11 sheet module). ⚠️ **Vẫn cần cho v1.1**: chứa 163 TC CARRIED + 2 TC đã GỠ (`TC-HOME-010`/`024`) ⛔ không chạy nữa. 🔴 Bản copy `TC-MASTER-LATEST.xlsx` **nay trỏ v1.1** |
| **v1.1** | `03_test-cases/v1.1/TC-MASTER-v1.1.xlsx` | **223** | 2026-09-17 | ✅ **Consolidated** · ✅ **Review 85/100 CONDITIONAL · G1 PASS** — recheck 2026-09-17: **0 Critical · 0 Major · 0 Minor · 3 Info** (điểm thô **100**, bị `reviewer-agent.md §6` cap **85** vì direct mode). Lượt đầu 80/100 (2 Major + 14 Minor) → đã fix hết cùng ngày. ⛔ **`APPROVED` (90+) KHÔNG đạt được ở direct mode** — cần `ANTHROPIC_API_KEY` chạy agent mode. Báo cáo: `11_tc-review/review-report-v1.1.md` — 13 sheet (Overview + ALL + 11 sheet module), 0 trùng ID. 🔴 **KHÔNG gộp CARRIED** (QC chốt 2026-09-17): file chỉ chứa TC của **140 SC NEW+MODIFIED**; 163 TC CARRIED nằm ở file v1.0 ⇒ execute/vibe-test phải mở **song song 2 file** mới phủ đủ 302 SC. ⚠️ **54 TC ID trùng 2 file** (SC MODIFIED giữ ID v1.0) — **LUÔN lấy bản v1.1**; 5 TC kỳ vọng NGƯỢC nhau: `TC-ACT-008/013/014`, `TC-USR-003/008`. Bản copy: `03_test-cases/TC-MASTER-LATEST.xlsx` |

> 📌 Bộ TC của đợt v1.0 **cũ** (323 TC, schema 16 cột đã convert) lưu ở `_handoff-v1.0/03_tc-baseline-v1.0/` — **KHÔNG** đưa vào `03_test-cases/v1.0/` (xem §4).

## 7. Downstream Path Registry
| Skill | Active Version Path |
|-------|-------------------|
| analyze-requirements | `02_analyze-requirements/v1.0/` (router `MEMORY.md` + 11 module — **thực thi/test hiện hành**) · `02_analyze-requirements/v1.1/` (router + 10 module delta, **ANALYZED**, cập nhật gần nhất **2026-09-17** — `FEED` đọc ở `v1.0/FEED-bang-tin/`; bảng hỏi BA `v1.1/CL-hoi-BA-v1.1.xlsx` là **bản DẪN XUẤT**, ⛔ không phải nguồn) |
| generate-tc | `03_test-cases/v1.0/` (11 fragment `.md` + `TC-MASTER-v1.0.xlsx`, 219 TC) · **v1.1: `03_test-cases/v1.1/` — 11 fragment `.md` + `TC-MASTER-v1.1.xlsx` (223 TC, CONSOLIDATED 2026-09-17)**. 🔴 **execute/vibe-test phải mở SONG SONG 2 file** — v1.1 KHÔNG gộp CARRIED (xem §6). Bản copy `03_test-cases/TC-MASTER-LATEST.xlsx` = **v1.1** (đã verify md5 2026-09-17). *(⛔ Mô tả cũ "fragment TC-USR-v1.1.md · PARTIAL · chưa có TC-MASTER v1.1" HẾT HIỆU LỰC — sửa 2026-09-17 theo `health-check` G-02.)* |
| export-tc-rp | `03_test-cases/v1.0/exports/` (phase design) · `09_reports/` (phase report) |
| review-tc | `11_tc-review/` |
| vibe-test | `08_test-runs/vibe/` |
| vibe-test | PARTIAL | 2026-09-19 | v1.1, VR-008, module ASN | scope 26: có verdict 10 · chạy run này 5 (5P/0F/0B) · N-A 2 · CÒN NỢ 16 · evidence 5/5 · 34 locators (merge 100%) · MCP calls ~230, snapshot 0/7 màn · ⏸️ QC dừng phiên 12:33 "mai tiếp" | mobile |
| vibe-test | PARTIAL | 2026-09-19 | v1.1, **VR-009**, module ASN | scope 26: có verdict **17** · chạy run này **7 (7P/0F/0B)** · N-A 2 · **CÒN NỢ 9** · evidence **7/7** · 28 locators (merge 100%) · MCP calls ~245, **snapshot 1/9 màn** · gate: rule-1 **7/7 sạch**, exit 1 **chỉ do còn nợ coverage** · 🛑 dừng 15:46 vì hết sức phiên (khung giờ `Chiều` đóng 17:00, nhóm trần cần ~50–60' + 3 lượt đổi tài khoản) | mobile |
| vibe-test | PARTIAL | 2026-09-19 | v1.1, **VR-010**, module ASN | scope 26: có verdict **24** · chạy run này **7 (5P/2F/0B)** · N-A 2 · **CÒN NỢ 2** *(`006`/`008` — QC chủ động để lại, cần 2–3 thiết bị)* · evidence **7/7** · 38 locators (merge 100%) · MCP calls ~534, **snapshot 19 (7 harvest + 12 phép ĐO — giải trình ở mcp-session-log §Statistics)** · gate: rule-1 **7/7 sạch**, 0 vi phạm, exit 1 **chỉ do 2 TC NOT_RUN** · 🐛 **1 bug gốc chưa log**: trần 5 thông báo khớp tuyến áp theo **TÀI KHOẢN** thay vì theo **TUYẾN** (`TC-ASN-016` + `TC-ASN-025`) ⇒ `/log-bug` · ⚠️ `TC-ASN-014`/`017` PASS nhưng **không phân biệt được** ⇒ chạy lại sau fix | mobile |
| vibe-test | PARTIAL | 2026-09-19 | v1.1+v1.0, **VR-011**, module **GIFT** | scope **14** *(HỢP 2 file TC-MASTER: v1.1 8 TC ∪ v1.0 6 TC riêng — chỉ đọc v1.1 sẽ sót 6)*: có verdict **11** · chạy run này **12 (7P/2F/2B + 1 NOT_EVIDENCED)** · N-A 0 · **CÒN NỢ 3** *(`008` cần account trắng · `011` cần 1 đơn Hoàn thành chưa tặng quà — **nợ SEED**; `010` nội dung ĐẠT nhưng ảnh **sai slot** sau khi đính chính verdict, ⛔ không đổi tên ảnh và ⛔ không chụp lại được vì STG hết đơn đủ điều kiện)* · evidence **11/11** · **43 locators (merge 100%)** · MCP calls ~125, **snapshot 2/7 màn** *(Pha B: 0 snapshot)* · gate: rule-1 **12/12 sạch**, 0 vi phạm, exit 1 **chỉ do 2 TC NOT_RUN** · 🔁 **ĐÍNH CHÍNH TRONG PHIÊN: `001`+`010` đổi FAIL→PASS** — app khớp **Then của `SC-GIFT-001`/`SC-GIFT-010`** và khớp **`C-GIFT-04`/`C-GIFT-02` đã Resolved**; phần lệch là **chữ TC lỗi thời** ⇒ sửa TC, ⛔ không log bug · 🐛 **2 FAIL còn lại, cả hai CHỜ BA CHỐT**: **G2** thông báo `NTF-07` deep-link vào "Quà đã nhận" trong khi copy nói "Trang cá nhân" (`012`) · **G3** popup còn chuỗi v1.0 `"Đã gửi lời cảm ơn!"` thay chuỗi `BR14-02` (`003`, **cần BA chốt** trước khi log bug) · 🔒 `013`/`014` BLOCKED vì **STG chưa build `FR09`** · 🔴 **spec-gap tách riêng**: nút `"✓ Cảm ơn người vận chuyển"` (`KB-GIFT-01` ô 5·Sender + When `SC-GIFT-001`) **không tồn tại trên app** → `/analyze-requirements --update` · 🔑 **đề nghị hạ/đóng `RISK-GIFT-05`** — hành vi `KB-GIFT-04` (back nhảy sang đơn khác) **KHÔNG tái hiện** qua 2 lượt thử đơn thật · 🔑 **đóng `C-GIFT-03` vế (b)** (CÓ lịch sử nhận quà) + **`KB-GIFT-03` có bằng chứng 3 tài khoản** · 🔴 **`stag_anhptm17@` KHÔNG còn sạch** (5 đơn/5 quà) ⇒ STG **hết tài khoản trắng**, chặn cụm empty state GIFT+ACT+HOME | mobile |
| vibe-test | PARTIAL | 2026-09-19 | v1.0+v1.1, **VR-012**, module **DLV** | scope **81** *(HỢP 2 file TC-MASTER: v1.0 30 CARRIED `001–030` ∪ v1.1 51 `031–081`, **0 ID trùng**)*: có verdict **19** · chạy run này **15 (13P/2F/0B)** · **N-A 4** *(`074` `075` `076` `078` — **API-tier**: pre-condition đòi proxy giả mạo request, ⛔ không phủ được qua UI; vẫn là **P1**, cần route sang tier security)* · **CÒN NỢ 62** · evidence **15/15** · **33 locators (merge 100%)** · MCP calls ~155, snapshot 24/3 màn *(12 lần là **phép ĐO** vế âm + tồn kho, khai rõ ở `mcp-session-log §Statistics`)* · gate: rule-1 **15/15 sạch**, **0 vi phạm evidence**, exit 1 **chỉ do còn nợ coverage** · 🚫 **DỪNG VÌ BLOCKER QUYỀN, ⛔ không phải hết sức/không phải app lỗi**: `tap` nút `Đã giao cho người nhận` bị **auto-mode classifier** chặn (`Modify Shared Resources`) — DLV là **máy trạng thái** nên **55/62 TC nợ** phụ thuộc trực tiếp vào nhóm thao tác bị chặn (đổi trạng thái đơn · mở màn *Xác nhận đã giao* · đăng tin · đổi tài khoản). **Cần user gỡ quyền mới chạy tiếp được** · 🐛 **2 FAIL = 1 lỗi gốc**: icon copy **không đổi màu xanh** (`080`+`081`), **trùng `TC-ORD-085`/VR-004** ⇒ mở **1 bug component**, nội dung copy vẫn ĐÚNG · 🔴 **spec-gap**: **người nhận MẤT cụm liên hệ khi đơn ở `Đang giao`** (có ở `Đã ghép` theo `RP-ASN-lo-sdt`, có lại ở `Đã giao` theo `TC-DLV-028`) — loại trừ 'ẩn dưới nếp gấp' bằng 4 phép cuộn + quét chuỗi; **chưa SC nào phủ** ⇒ `/analyze-requirements --update` · ⚠️ **CẢNH BÁO TC LỖI THỜI**: `TC-DLV-017/018/019` (CARRIED v1.0) tả **popup 2 nút** cho nút mà v1.1 (`043`–`053`) tả là **cả một màn 'Xác nhận đã giao'** ⇒ phiên sau FAIL ở đây thì **route về `review-tc`, ⛔ đừng log bug** · 🔑 **oracle dùng lại được cho cả nhóm `001–015`**: nhãn khoá = `clickable=false` + không `resource-id`; nút thật = `clickable=true` + có `content-desc`; ⛔ **KHÔNG dùng `enabled`** (bẫy `T4`) | mobile |
| execute-maintain | `08_test-runs/` |
| log-bug | `05_bug-reports/draft/` · `05_bug-reports/jira/` (recursive) |
| test-report | `09_reports/` |
| scan-source-code / implement-automation | `10_source-code/` — **N/A** (project manual, chưa scaffold) |

## 8. Pipeline Status — v1.0
> Thứ tự + tên skill khớp `PIPELINE.md §2 Skill Registry`. Status ∈ NOT_STARTED / IN_PROGRESS / PARTIAL / COMPLETED / SKIPPED / FAILED.
> **CURRENT-STATE, OVERWRITE — đúng 1 dòng / skill.** ⛔ KHÔNG append thêm dòng cho mỗi lần chạy lại.

| # | Skill | Status | Last Run | Scope | Output | Notes |
|---|-------|--------|----------|-------|--------|-------|
| 0.5 | init-source-code | SKIPPED | — | — | — | Project chạy manual — không có automation (`Project_rule §Automation Rules` = N/A) |
| 1 | init-project | COMPLETED | 2026-09-07 | — | CLAUDE.md / PIPELINE.md / COMMANDS.md / Project_rule.md | Seed thêm 11 module codes + DOC notation + Custom Rules §10.1/§10.2 ngày 2026-09-07 |
| 2 | create-test-plan | NOT_STARTED | — | — | — | Không phải hard prerequisite của analyze |
| 3 | analyze-requirements | COMPLETED | 2026-09-07 | v1.0 · 11/11 module | `02_analyze-requirements/v1.0/` — router + **11 × 5 file** | INIT lại từ đầu theo bộ skill v1.1 (`module-first v2`). 116 REQ · 211 SC · 35 CL (17 Open, **0 blocker**) · 64 RISK |
| 4 | generate-tc | COMPLETED | 2026-09-07 | v1.0 · 11/11 module · mode `standard` | `03_test-cases/v1.0/` — `TC-MASTER-v1.0.xlsx` + 11 fragment `.md` + `CHANGELOG.md` | **219 TC** (P1 27 · P2 123 · P3 69) phủ **211/211 SC** · 0 SC hở · 0 TC trùng/orphan. Consolidate 2026-09-07. 13 TC **dự kiến FAIL có chủ đích** — xem `03_test-cases/v1.0/CHANGELOG.md §2` ràng buộc 6 |
| 4b | export-tc-rp | NOT_STARTED | — | — | — | Renderer deliverable FPT — chạy sau consolidate |
| 5 | review-tc | COMPLETED | 2026-09-07 | v1.0 · FULL · 219 TC · 4 reviewer context-độc-lập (R1+R4 · R2 · R3×2) | `11_tc-review/review-report-v1.0.md` | ❌ **Score 0/100 — REJECTED · G1 FAIL ⇒ block downstream.** 0 Critical · 36 Major · 36 Minor · 17 Info (89 finding, severity chuẩn hoá theo catalog). Major dồn ở 3 pattern hình thức: `R3-14` 42 TC (Pre-condition ⇄ Setup) · `R3-02`+`R3-17` 12 TC (Expected không verbatim / có "hoặc") · `R3-01`+`R3-09`+`R3-03` (step thiếu element/giá trị). Coverage sạch: 211/211 SC · 0 orphan · 0 Critical structural. ⚠️ Không có `ANTHROPIC_API_KEY` ⇒ không gọi API instance, cap 85 áp nhưng vô hiệu |
| 6 | scan-source-code | SKIPPED | — | — | — | Không có automation |
| 7 | vibe-test | **PARTIAL** | 2026-09-19 | v1.0 · **8 TC CARRIED `USR`** (VR-001/003) · **40 TC CARRIED `ORD`** (VR-002/004) · **19 TC CARRIED `HOME`** (VR-005) — scope các module này bắt buộc mở song song 2 fragment | `08_test-runs/vibe/VR-001-USR-2026-09-18/` · sổ cái `08_test-runs/vibe/coverage/coverage-USR.md` | **8 TC CARRIED: 5 chạy (4P/1F) · 2 NOT_RUN · 1 không đụng.** ✅ PASS: `001` (mở FoxEco từ host → đúng danh tính) · `004` (dòng định danh đúng pattern `[Phòng ban] · MNV: [mã]`) · `007` (vắng Điểm ECO/uy tín/CO₂) · `009` (Đơn của tôi → tab "Đang diễn ra" active) · `010` (Quà đã nhận). ❌ FAIL `011` — ⚠️ **KHÔNG phải lỗi app**: TC này (*"không có bề mặt cấu hình kênh liên hệ trên app **v1.0**"*) **mâu thuẫn trực tiếp** `FR15` v1.1 vốn cố ý THÊM field "Số điện thoại mặc định" ⇒ TC hết hiệu lực, route `/analyze-requirements --update`, QC chốt sang `DESCOPED`+`Skipped` (⛔ giữ nguyên dòng, `§10.5` FREEZE). ⏳ NOT_RUN: `005` (cần 3 tài khoản + hoàn tất 1 đơn end-to-end) · `006` (cần tài khoản **BLANK**) — cả 2 chặn bởi **OTP nhập tay**. ⚠️ `004` PASS theo **pattern**, nhưng MNV thật trên STG là **8 chữ số** (`00131946`) khác ví dụ `FTEL2291` trong Expected — cần QC chốt nếu muốn siết định dạng mã. 🔴 **10 module v1.0 còn lại CHƯA vibe-test.** **Nên chạy TRƯỚC generate-tc** cho `ACT` (`KB-ORD-07`) và `DLV` (15 ô ma trận) — xem §9 🔵 **VR-002 (2026-09-18) — thêm 40 TC CARRIED của `ORD`:** chạy **20/40** (13P/5F/2B), còn nợ 20. ✅ PASS: `001` (4 thành phần màn Đăng tin mới + banner đúng nguyên văn) · `002` (step indicator `Bước 1 / 3`) · `009`/`010` (banner "giá trị cao" đúng nguyên văn, không hiện với Thấp/Vừa) · `011` (ghi chú 300 ký tự, chặn 301) · `015`/`016`/`017`/`018` (prefill + chỉ-đọc tên người gửi) · `030`/`031` (chặn ngày quá khứ + `Đến < Từ`, chặn ở tầng picker) · `054` (không có ô nhập số tiền). ❌ **4 FAIL đều là TC HẾT HIỆU LỰC, ⛔ KHÔNG phải lỗi app, ⛔ không log bug:** `006` (`C-ORD-09` chốt app **CÓ** nhãn "Tài liệu" ⇒ 🔴 `KB-VIBE-01` của `KP-01 §10.2` **đã lỗi thời**, app không còn nhãn `Giấy tờ, hồ sơ` — mọi ràng buộc dẫn `KB-VIBE-01` ở module khác phải kiểm lại) · `007` step 5 + `008` step 6 + `014` (v1.1 thêm **3 trường bắt buộc** TRỌNG LƯỢNG + KÍCH THƯỚC + **ẢNH HÀNG** ⇒ điều kiện enable `Tiếp theo` đã đổi; `014` mâu thuẫn **trực tiếp** `TC-ORD-063` v1.1 P1) ⇒ QC chốt `DESCOPED` theo `§10.5`. 🚩 `017` PASS nhưng **câu chữ sai nguồn prefill**: app điền **địa chỉ mặc định hồ sơ** (`C-ORD-10`), không phải địa chỉ HRIS như Expected v1.0 ⇒ đề nghị sửa câu chữ. 🚫 BLOCKED `069`… *(xem §8b — TC này thuộc v1.1)*. ⏳ 20 TC CARRIED còn nợ: nhóm địa chỉ gợi ý (`019`–`026`/`029`/`032`/`033`), bước 3 + đăng tin (`034`–`041`), OFFER (`042`–`045`), sửa/hết hạn (`046`–`052`). 🔴 **CẢNH BÁO TRÙNG TÊN:** nhãn `VR-002` trong `KP-01 §10.2/§10.3` là run của **đợt v1.0 CŨ**, KHÔNG phải phiên này (`08_test-runs/vibe/VR-002-ORD-2026-09-18/`). 🟣 **VR-003 (2026-09-18) — `TC-USR-006` (CARRIED v1.0) ĐÃ CHẠY: ✅ PASS.** Card chỉ số của tài khoản trắng `00002352` hiện đủ 2 ô, cả 2 = `0`, card không bị ẩn ⇒ ⛔ không dẫn `C-ORD-06`. 📌 Đính chính số đếm của chính dòng này: *"5 chạy (4P/1F) · 2 NOT_RUN · 1 không đụng"* **sai từ đầu** — danh sách liệt kê ngay sau đó là **5 PASS** (`001`/`004`/`007`/`009`/`010`) **+ 1 FAIL** (`011`) = **6 chạy**, cộng 2 NOT_RUN (`005`/`006`) là vừa đủ 8, **không có TC nào "không đụng"**. ⇒ **Số đúng sau VR-003: 8 TC CARRIED = 7 chạy (6P/1F) · 1 NOT_RUN (`005`, chờ QC tạo data 3 vai).** 🟩 **VR-005 (2026-09-19) — thêm 19 TC CARRIED của `HOME`:** chạy **13/19** (12P / 0F / 1B — `007` đổi FAIL → PASS 2026-09-21), còn nợ **6**. ✅ PASS: `001` (bottom nav đủ 5 tab đúng thứ tự, `Trang chủ` active) · `003` (lời chào khớp tên hồ sơ — ⚠️ tách **2 TextView**, không phải 1 chuỗi) · `005`/`006` (chấm đỏ chuông có→không sau khi đọc hết) · `009` (đủ **6/6** thành phần section "Đơn của tôi") · `011`/`012`/`013` (đủ bộ **3 nhãn vai** `Gửi:` cam / `Giao:` tím / `Nhận:` xanh lục) · `015` (chạm card mở đúng đơn — khớp cả **loại hàng + 2 địa chỉ**) · `016` (`Xem tất cả` → màn Hoạt động; ℹ️ tab tên `Hoạt động` nhưng tiêu đề màn là `Đơn của tôi`, khớp `Project_rule §Module Codes`, ⛔ không phải sai điều hướng) · `022` (`Xem bảng tin gửi hàng` → tab Bảng tin). ✅ PASS `007` *(đổi từ FAIL 2026-09-21, không chạy lại)* — ứng viên bug H1 **đã đóng, không log bug**: app hiện `Tiện đường — Giúp đồng nghiệp` = chuỗi PRD (`C-HOME-01(b)`); Expected cũ theo BRD là lỗi thời ⇒ QC GiangDC2 sửa Expected. 🚫 BLOCKED `002` step 2 (0 tin ⇒ không mở được Chi tiết tin; **3/4 màn con còn lại ĐÚNG**). ⚠️ 5 TC PASS (`009`/`011`/`012`/`013`/`015`) **khai báo lệch step** — dùng đơn có sẵn vì bug `B1` chặn đăng tin NEED; Expected không assert trạng thái đơn ⇒ verdict tin được. ⏳ **6 TC CARRIED còn nợ**: `004` (cần 3 tài khoản 3 vai — 🔍 header **không có node icon vai trò nào**, nghi `SC-HOME-004` chưa build) · `014` (cần đẩy 1 đơn qua 4 trạng thái; 🔍 quan sát rời đã khớp 2/4 mốc: `Đã ghép`→2/5 đoạn, `Đã giao`→4/5) · `017`/`018`/`020`/`023` (cần ≥1 tin do tài khoản khác đăng). ⚠️ `008`/`019`/`021` **KHÔNG** thuộc 19 TC CARRIED — 3 ID này trùng 2 file và **lấy bản v1.1** (`CLAUDE.md`), đã tính ở §8b. 🔴 Chặn gốc: **0 tin NEED hợp lệ trên STG** + **OTP nhập tay** khi đăng nhập tài khoản phụ. |
| 8 | implement-automation | SKIPPED | — | — | — | Không có automation |
| 9 | review-src-tc | SKIPPED | — | — | — | Không có automation |
| 10 | execute-maintain | SKIPPED | — | — | — | Không có automation — execute thủ công / qua `vibe-test` |
| 11 | log-bug | NOT_STARTED | — | — | — | **≥9 bug/nghi vấn bug đã biết chưa log** (`KP-05 §5`) — xem §9 |
| 11b | sync-jira-bugs | NOT_STARTED | — | — | — | ⛔ Jira **chưa cấu hình** (`Project_rule §Jira Integration` còn comment) |
| 12 | test-report | NOT_STARTED | — | — | — | Phải nêu rõ nhánh Admin (`§D4` cột Admin) ngoài phạm vi v1.0 |
| 13 | health-check | COMPLETED | 2026-09-15 | QUICK · toàn repo (v1.0 + v1.1) · lượt 2 trong ngày | inline summary (mode QUICK — không sinh file report) | **0 CRITICAL · 8 WARNING · 5 INFO.** 🟢 **3 CRITICAL của lượt sáng đã xử lý:** router `v1.1/MEMORY.md` đã dựng · nghĩa `counts:` chốt cumulative (10/10 chỉ số khớp 3 tầng module↔router↔MASTER) · `C-CNL-01` nay có khối `🔁 Kết luận bị đảo` ở `v1.1/CNL-huy-don/CHANGELOG.md §2` (hạ WARNING — v1.0 bị `Project_rule` cấm sửa nên pointer chỉ đi được 1 chiều). **WARNING treo từ lượt trước, chưa ai xử lý (4):** TC-MASTER.xlsx drift diện rộng 9/11 module (Nợ #6) · §8 `generate-tc` Last Run còn 2026-09-07 · TC `CHANGELOG §2` ràng buộc 3 stale (drift B-06 đã fix 2026-09-14) · `validate-vibe-run.mjs` lệch toolkit (hardcode `python3`). **WARNING mới (4):** G-03 quote lặp home **16→21** (+6 do 5 module delta bù, nặng nhất `v1.1/ASN` 7 dòng) · F-08 22 ảnh `00_input/v1.1/design/` vẫn chưa có DOC-ID **và nay đã bị cite làm Source Location** · `MASTER §7` Downstream Path chỉ trỏ `v1.0/` trong khi v1.1 đã ANALYZED · `C-USR-03` lặp pattern đảo-kết-luận của `C-CNL-01`. **Sạch:** F-05/F-06 21/21 module (REQ-rows == quote-blocks) · F-07 8/8 DOC Active resolve · G-01/G-08 cả 2 router chỉ §1+§2 · G-05 0 dòng trùng · E-01..E-07 registry rỗng đúng trạng thái · H-03 9/9 section · H-04 5/5 hook. **Chưa sửa gì — health-check chỉ report.** |

## 8b. Pipeline Status — v1.1 (delta, chạy song song)
> Bảng riêng vì **§10 Version Cutover CHƯA áp dụng** — v1.0 còn dở (`review-tc` REJECTED, `vibe-test`/`log-bug` chưa chạy) nên §8 vẫn là bảng sống của v1.0.
> Cùng luật với §8: **CURRENT-STATE, OVERWRITE — đúng 1 dòng / skill.** Skill chưa chạm v1.1 thì không cần dòng.

| # | Skill | Status | Last Run | Scope | Output | Notes |
|---|-------|--------|----------|-------|--------|-------|
| 3 | analyze-requirements | **COMPLETED** | **2026-09-17** | v1.1 · **10/11 module** + `FEED` (home ở `v1.0/`) — DELTA ×3 lượt + UPDATE ×5 (vibe-check demo ×2 · áp câu trả lời BA ×2 · rà TOÀN BỘ `CL-hoi-BA-v1.1.xlsx` từng dòng) | `02_analyze-requirements/v1.1/` — router `MEMORY.md` + 10 × 5 file · `v1.1/CL-hoi-BA-v1.1.xlsx` (bảng hỏi BA — **bản DẪN XUẤT**, ⛔ không phải nguồn) | **142 REQ · 302 SC · 67 CL · 87 RISK.** 🟢 **CL: 0 Open · 66 Resolved · 1 Partially** (`C-DLV-08` (a)(b) — QA **tự xác minh** qua vibe-check block LỊCH SỬ, ⛔ không hỏi BA nữa) ⇒ **11/11 module hết điểm hỏi BA**. 📐 Δ lifecycle **135** (90 NEW + 45 MODIFIED) · **mẫu số phải test 139** (+4 SC `FEED` bị chạm, giữ lifecycle NEW) · 2 DEPRECATED (`SC-HOME-010/024`). 🔑 **Kết luận scope quan trọng nhất của lượt này:** mọi bề mặt **role admin** (*"chuyển admin hỗ trợ"* `AC-23.2.01`/`BR08-06`/`BR09-04`, hạn giữ hàng tại quầy 4h/24h) là **OUT OF SCOPE v1.1** — ⛔ không viết assert, ⛔ **không log bug** khi không thấy. 🔁 Kết luận bị đảo trong lượt: trần thông báo khớp *(đảo lần 3 — về "5 thông báo / 1 tin đăng OFFER")* · `SC-FEED-009` placeholder → **bản đồ thật** · nhãn Loại hàng **"Tài liệu"** (theo PRD) ⟷ nhãn tab **theo app** — ⚠️ BA quyết **từng nhãn**, ⛔ không có luật chung *"PRD thắng"* hay *"app thắng"* · `C-ORD-18` **UI thắng PRD §8.1.4** cho 3 trường Bước 1/3 wizard. 🟡 **Nợ còn lại — là nợ MÔI TRƯỜNG/DATA, ⛔ KHÔNG còn nợ clarification:** nhánh `FR09` (`RETURNED`) chưa có trên STG · trạng thái `INCIDENT` chưa có · app đã build `FR15` chưa (`SC-USR-013..020`) · email mẫu `@fpt.com` đã nghỉ việc (`TC-ORD-076`) · 4 tài khoản test (`TC-USR-040/041/043/044`). ⚠️ **Số ở ô này là bản DẪN XUẤT** — canonical ở frontmatter `counts:` của `<module>/test_scenario_map.md` (REQ/SC/priority) và `<module>/risk_assessment.md` (CL/RISK). *(⛔ Bản cũ ghi `Last Run 2026-09-16` · `295 SC` · `65 CL (37 Resolved · 3 Partially · 25 Open)` · "TC v1.1 chưa generate" — **HẾT HIỆU LỰC**, sửa 2026-09-17 theo `health-check` A-01.)* 🟡 **CẬP NHẬT 2026-09-18 — CL: 67 → 68, Open: 0 → 1.** Mở lại **`C-USR-07`** (*"địa chỉ mặc định để RỖNG có hợp lệ không, và có đường nào xoá địa chỉ đã lưu?"*) sau khi `vibe-test VR-003` lưu hồ sơ **2 lần với ô địa chỉ để trống** và app **cho qua cả 2 lần** (không chặn, không báo lỗi). 🔍 **Đây là lỗi quy trình, không phải phát sinh mới:** `C-USR-07` chính là **câu (v)** trong 6 câu con (i)–(vi) của `C-USR-05`; BA **chưa bao giờ trả lời** câu đó, nhưng `C-USR-05` vẫn được set `Resolved` ở lượt 5 (2026-09-16) ⇒ **đóng CL gộp làm rơi câu hỏi con**. ⇒ Khuyến nghị cho lượt `analyze-requirements` sau: **tách CL nhiều câu thành CL con**, hoặc thêm phép kiểm *"mọi câu (a)(b)(c)… có câu trả lời trích dẫn được"* trước khi đóng. 🔴 Cùng ngày, câu **(vi)** (*"HRIS không có địa chỉ làm việc thì ô hiện gì?"*) thì **ĐÓNG** theo QC GiangDC2: *"địa chỉ data hris luôn luôn có nhé… hiện tại ko có do bị bug không load data từ hris"* ⇒ nhánh đó **không tồn tại**, ô rỗng là **triệu chứng của `BUG-008`**; `SC-USR-022`/`SC-USR-023` (*"prefill từ HRIS"*) **được xác nhận ĐÚNG**, ⛔ không sửa SC theo hành vi app. ⇒ **`TC-USR-044` giữ nguyên `N-A`/DESCOPED** — lập luận *"ô SĐT rỗng có xảy ra thật nên TC nên sống lại"* (nêu ở VR-001/VR-003) **bị bác**: TC đó mô tả *"HRIS trống SĐT"*, còn thực tế là *app không nạp dữ liệu*. |
| 4 | generate-tc | **COMPLETED** | 2026-09-17 | v1.1 · ✅ **11/11 module + CONSOLIDATE** · mode `standard` | `03_test-cases/v1.1/TC-MASTER-v1.1.xlsx` + 11 fragment `.md` + `CHANGELOG.md` | ✅ **Consolidated 2026-09-17 — 223 TC** (P1 22 · P2 145 · P3 56 — đếm trực tiếp từ 11 fragment) cho **140 SC** NEW+MODIFIED, phủ 139 SC. 🔴 **Consolidate phải GỠ `TC-HOME-010` + `TC-HOME-024`** (SC DEPRECATED, kỳ vọng ngược bản mới) — chiến lược ID (a) để lỗ, 2 ID không tái sử dụng. Áp `Project_rule §10.4` (1 dòng Expected/TC) ở cả 7. 🔴 `SC-DLV-060` **DESCOPED 0 TC** (role admin out of scope v1.1). ✅ **Consolidate xong** → `TC-MASTER-v1.1.xlsx` (13 sheet, 0 trùng ID, đã gỡ `TC-HOME-010`/`024`). ⏳ Bước tiếp: `/health-check` rồi `/review-tc` (gate G1). 🔴 `ACT`: 3 TC (`TC-ACT-008/013/014`) **đảo/siết kỳ vọng so với bản v1.0 cùng ID** — consolidate đã lấy đúng bản v1.1. ⏳ **Chưa review (gate G1)** — đây là việc còn lại duy nhất của bước này. ⚠️ v1.0 `review-tc` vẫn REJECTED (gate riêng của v1.0, không chặn v1.1) |
| 4b | export-tc-rp | **COMPLETED** | 2026-09-17 | v1.1 · `--phase design` · 223 TC · 11 module | `03_test-cases/v1.1/exports/ISC_foxeco-v2_v1.1_TestCase.xlsx` (+ `.summary.json`) | ✅ Bản **thiết kế TC** chuẩn FPT SDLC — 20 sheet, band EXECUTION + Bug Data **TRỐNG** đúng hợp đồng `--phase design`; Dashboard 11 module khớp `10/13/13/51/5/8/11/9/48/17/38 = 223`; RTM **72 dòng REQ**; recalc LibreOffice OK (đã có sẵn số). ⚠️ **Kỳ test để TRỐNG** (`--period-tbd`, QC chốt 2026-09-17) — `01_test-plans/` rỗng, chưa có TP. **QC phải điền `Summary!C10/C11` trước khi gửi PM.** 🔴 **Renderer BỎ QUA sheet `Seed`** (đúng thiết kế: bỏ sheet có `A1 ≠ 'Testcase ID'`) ⇒ export ban đầu **tái hiện đúng lỗi `review-tc` M-1**: 21 mã `SEED-*` ở 92 ô mà không có định nghĩa, tệ hơn là **17 ô ghi *"xem sheet `Seed` của chính file này"* trong khi file đó KHÔNG có sheet Seed** — câu chỉ đường thành nói dối. Đã vá: chèn sheet `Seed` vào export (dùng lại `readd_cover_logo` + `recalc_with_libreoffice` của chính skill) ⇒ **21 trỏ == 21 định nghĩa**; chart 18 · ảnh 2 · data-validation 98 · CF 13 **còn nguyên**. 🟡 **Nợ toolkit:** bước vá này làm TAY, `export-tc-rp` chưa tự carry sheet `Seed` ⇒ **render lại là mất**. Cần sửa skill (hoặc `Project_rule §10.3` mục 4 mở rộng sang deliverable). 🟡 `Report Test §1b` chỉ đủ 10 dòng ⇒ `Trust & Safety` + `Tài khoản & Hồ sơ` bị gom vào *Khác / chưa gán chức năng* — chưa ảnh hưởng (phase design không có bug), nhưng **sẽ ảnh hưởng ở `--phase report`**. ⛔ Chưa sinh email (`python-docx` chưa cài; `--phase design` vốn không cần). |
| 5 | review-tc | **COMPLETED** | 2026-09-17 | v1.1 · FULL · 223 TC · mode `standard` · **direct mode** (thiếu `ANTHROPIC_API_KEY` ⇒ fallback `reviewer-agent.md §6`, **cap 85** đã áp) | `11_tc-review/review-report-v1.1.md` | ✅ **80/100 — CONDITIONAL · Quality Gate G1 PASS** (0 CRITICAL · 2 MAJOR · 14 MINOR · 3 INFO) ⇒ **KHÔNG block downstream**. 🟠 **M-1** khối `Seed dùng chung` **không được carry vào TC-MASTER** lúc consolidate: **88/223 TC (39%)** trỏ **21 mã `SEED-*`**, 17 TC ghi thẳng *"xem ghi chú đầu fragment"*, nhưng quét 13 sheet workbook ra **0 định nghĩa**. ⛔ KHÔNG phải lỗi generate-tc — `Project_rule §10.3` chỉ bắt **fragment** khai khối Seed, **không luật nào** bắt consolidate mang sang. Phải fix TRƯỚC `/vibe-test`. 🟠 **M-2** `§3` dòng `FEED` thiếu `SC-FEED-002` (đã sửa ở v1.1 — *"hết gap CTA 2026-09-16"*, TC mang `MODIFIED`) ⇒ mẫu số **139 SAI, đúng là 140**; `§8b` + TC `CHANGELOG` ghi *140* mới là bản đúng. 🟡 R3-19 **12 TC** thiếu nhãn `(setup)` (đối chứng: 563 step ở 208/223 TC đã gắn đúng ⇒ luật được áp, chỉ không đều) · `SC-DLV-060` đã descope nhưng còn `Lifecycle NEW` (sẽ kích hoạt lại R2-01 mỗi lượt) · header `TC-ORD-v1.1.md` còn ghi *"SC-ORD-059 là MODIFIED"* (hết hiệu lực). 🟢 **Sạch:** R1-01 0 trùng ID · R1-02 0 (9 module có gap nhưng **cả 9 đều khai ở §0.1**) · **R1-03 0** (223/223 Expected neo đúng 1 step, 0 gộp dải, 0 trỏ TC khác) · R1-04..13 0 ô trống/invalid · R3-15 **0 dấu `→`** · R3-16 0 · **R3-17 0** (4 ứng viên đều dương tính giả — *"Đã ghi nhận phản hồi"* là chuỗi UI verbatim) · R3-20 0 (222/223 tách dòng) · R4-06/08/09/10 0. 📈 **v1.0 0/100 REJECTED → v1.1 80/100**: cả **3 pattern Major** từng đánh chìm v1.0 (`R3-14` 42 TC · `R3-02`/`R3-17` 12 TC · `R3-01/09/03`) nay **0 finding**, nhờ `Project_rule §10.3` + `§10.4`. ✅ **2/2 MAJOR ĐÃ FIX cùng ngày (2026-09-17).** **M-1:** thêm sheet **`Seed`** vào `TC-MASTER-v1.1.xlsx` (9 khối, phủ **21/21** mã `SEED-*`) + sửa **34 ô** `Pre-condition`/`Test Data` từ *"xem ghi chú đầu fragment"* → *"sheet `Seed` của chính file"*; đồng bộ `TC-MASTER-LATEST.xlsx`. Kiểm chứng: mã bị trỏ 21 = mã có định nghĩa 21 · **0** ô còn trỏ ra ngoài workbook · 223 TC **không đụng**. 🔴 Phát hiện kèm, nặng hơn báo cáo gốc: **`SEED-DLV-01`** — bị `CNL`/`HOME`/`NTF` trỏ — **không định nghĩa ở fragment v1.1 nào**, nằm ở **file thứ ba** `03_test-cases/v1.0/CHANGELOG.md §1`. 🛡️ Chặn tái phát: `Project_rule §10.3` thêm **mục 4** — consolidate BẮT BUỘC sinh sheet `Seed` đủ mọi mã được trỏ + phép kiểm *tập trỏ == tập định nghĩa*. **M-2:** `§3` dòng `FEED` bổ sung **`SC-FEED-002`** (3 nguồn độc lập xác nhận nó bị sửa ở v1.1); mẫu số **139 → 140** ở `§1`·`§3`·`§4`·`§5`; coverage ghi lại **139/140 = 99,3%**; `§4` `FEED` 11/15 → **10/15**. ⏭️ **Còn 14 MINOR + 3 INFO** (R3-19 ×12 · `SC-DLV-060` còn `Lifecycle NEW` · header `TC-ORD-v1.1.md` stale) ⇒ **score dự kiến sau `--recheck` = 85** (86 bị cap direct mode). ⛔ **Score 80 ở §6 và report là điểm LÚC REVIEW, chưa tính lại** — re-score phải qua `/review-tc --recheck`. ✅ **ĐỢT 2 — 14/14 MINOR ĐÃ FIX (cùng ngày).** **#1 R3-19:** 12 TC gắn nhãn `(setup)` ở **cả** TC-MASTER (2 sheet/TC) **lẫn** fragment — step có nhãn 563 → **575 (+12)**. 🔍 Sửa lại kết luận báo cáo gốc: `TC-ORD-048` **step 3 KHÔNG phải setup** (Expected step 5 assert *"kết quả tìm ở step 3"*) ⇒ chỉ gắn step 4. 7 step khác cố ý **không** gắn — điều hướng nằm trong luồng đang test. **#2 B-1:** `SC-DLV-060` `NEW → DEPRECATED`; `counts` 33/1, `p3` 12→11; lan 3 tầng (router Tổng **89/45/3**, P3 82→81 · MASTER §1/§3/§4/§5). Mẫu số **140 → 139**, coverage **139/139 = 100%** ⇒ `R2-01` hết báo đỏ oan. 🔴 **Phép kiểm lifecycle của chính §3 báo CRITICAL oan cho `DLV` ngay sau đó** — nó giả định mọi SC DEPRECATED đều sinh ở version trước, trong khi `SC-DLV-060` **born v1.1, descope v1.1**. Đã sửa **định nghĩa phép kiểm** ở §3: tách **2 loại DEPRECATED** (① version trước → vào vế trái · ② chính version này → KHÔNG), kèm cách phân loại bằng `id_range`. Chạy lại **10/10 khớp**. **#3 B-2:** header `TC-ORD-v1.1.md` bỏ câu *"SC-ORD-059 là MODIFIED"*. **+ Nợ #23:** chép định nghĩa đầy đủ `SEED-DLV-01` vào `TC-CNL-v1.1.md` (nợ do chính lượt M-1 mở ra). ⇒ **0 CRITICAL · 0 MAJOR · 0 MINOR · 3 INFO** · TC không bị đụng (223 TC, 0 trùng ID). ⛔ **Score 80 ở §6 vẫn là điểm LÚC REVIEW** — chạy `/review-tc --recheck` để chốt; dự kiến **85** (100 bị cap direct mode). 🔄 **RECHECK 2026-09-17 — `/review-tc --recheck`: 80 → 85 (+5) · G1 PASS → PASS.** Verify 8/8 finding cũ: **M-1 · M-2 · R3-19×12 · B-1 · B-2 = ✅ Fixed** · I-2 Superseded (coverage **139/139 = 100%**) · I-3 Resolved · I-1 Still Open (Info, `TC-NTF-008` step 212 ký tự). Chạy **lại đủ battery R1–R4**, không chỉ verify finding cũ: R1-01/03/04..13 **0** · R2-01/02 **0 SC hở** · R2-12 **0** · R3-01 **0** · R3-09 **0** · R3-15/16/20 **0** · R4-06/08/09/10 **0**; 2 ứng viên R3-06/R3-17 là **dương tính giả**; R4-01 nhất quán (217 TC dùng *"Đăng nhập"*, 0 dùng *"Login"*). 🔵 **New (Info, không trừ điểm):** `TC-HOME-008`/`031` step 1 gắn `(setup)` nhưng Expected tham chiếu output của nó — **không raise** vì đó chỉ là **mốc so sánh** (hành vi đọc số hero đã có TC riêng `TC-HOME-029`), khác với `TC-ORD-048` step 3 đã **bị từ chối** gắn nhãn vì tìm tin trên Bảng tin **chính là** hành vi đang test. Cùng 1 tiêu chí, 2 kết luận ngược — nhất quán. ⚠️ Chưa kiểm: sheet `Seed` mới có ảnh hưởng renderer `export-tc-rp` không (skill đó chưa chạy cho v1.1). 🔴 **Điểm thô 100/100 — 0 finding trừ điểm.** 85 là **trần cứng** của direct mode (`reviewer-agent.md §6`), và 85 rơi dải 70–89 ⇒ **`APPROVED` là BẤT KHẢ THI về cấu trúc ở direct mode**; fix thêm TC KHÔNG nâng được điểm. Muốn `APPROVED` phải có `ANTHROPIC_API_KEY`. |
| 13 | health-check | COMPLETED | 2026-09-17 | VERSION · v1.1 (+ `v1.0/FEED-bang-tin` — home duy nhất của FEED) · có parse `TC-MASTER-v1.1.xlsx` | inline summary (mode VERSION — không sinh file report) | **4 CRITICAL · 7 WARNING · 4 INFO.** 🔴 **G-02** cùng cặp số NEW/MODIFIED v1.1 có **4 giá trị** trong chính MASTER: §1 *(91+45)* · §3 dòng tổng *(91+45)* nhưng cộng các dòng của nó ra *(91+48)* · §4 *(88+44 = 132 SC)* · §5 *(90+46)*; canonical = **90 NEW / 46 MODIFIED**. 🔴 **B-04** `SC-ORD-059` MODIFIED ở module (đảo Then theo `C-ORD-14` 2026-09-17) nhưng §3 vẫn đếm trong dải NEW `SC-ORD-052..065` — đây là gốc của lệch ORD 14/12 ⟷ 13/13. 🔴 **G-02** MASTER tự mâu thuẫn về `generate-tc` v1.1: §6+§8b nói **đã consolidate 223 TC**, §7 nói *"PARTIAL, chưa có TC-MASTER v1.1"*, và ô Notes §8b #4 vừa ghi *"✅ Consolidate xong"* vừa ghi *"Chưa consolidate"*. 🔴 **G-02** `CLAUDE.md` còn nguyên v1.0 (*Current version v1.0* · TC-MASTER 219 TC) và khai `TC-MASTER-LATEST.xlsx` là copy của v1.0 — md5 chứng minh nó là copy **v1.1**. 🟡 **G-06b** 14 chỗ register sống còn *"chờ &lt;CL&gt;"* cho CL đã Resolved — nặng nhất `MASTER §4` trừ `SC-ORD-058..060` (chờ `C-ORD-13`) + `SC-ORD-031..035` (chờ `C-ORD-04`) khỏi mẫu số · ASN ×3 file (`C-ASN-04`) · ACT ×3 (`C-ACT-03/04`) · DLV ×2 (`C-DLV-05/06`) · GIFT · ORD. 🟡 **G-06b** `ASN/risk_assessment` KN#2 còn *"việc còn lại: hỏi BA C-ASN-04/05"* (Resolved) và `03_test-cases/v1.1/CHANGELOG §3 Nợ #7` ghi ASN/NTF *"chưa write-back"* trong khi **đã write-back**. 🟡 **A-01** §8b `analyze-requirements` Last Run 2026-09-16 + Notes còn *295 SC · 65 CL · 25 Open* (canonical **302 SC · 67 CL · 0 Open · 1 Partially**). 🟡 **F-01** router frontmatter `doc_source` thiếu hẳn `DOC-v1.1-04` (USR cite 6 lần), ghi *26 ảnh* trong khi đĩa có **31** (MASTER §2 đã ghi 31), `modules` thiếu GIFT/ORD, `updated` còn 2026-09-16. 🟡 **G-03** 13 quote lặp home (7 cùng-module: ORD 4 · USR 3; 6 cross-module: `BR14-03` ×3 file, `BR14-04`, `BR18-04`, `§8.12.3`). 🟡 **G-07** `CL-hoi-BA-v1.1.xlsx` là view dẫn xuất maintain tay, không nhãn DERIVED — đã drift thật 2026-09-17. 🟡 **G-02** §5 ghi *DOC nguồn v1.1 = 2* trong khi §2 có **4** DOC v1.1 Active. 🔵 C-01 `SC-DLV-060` 0 TC (DESCOPED có chủ đích) · A-05 active version v1.0 (chủ đích) · `updated:` frontmatter lỗi thời (8/10 scenario_map ghi 2026-09-15, 6 CHANGELOG ghi 2026-09-16) · `C-ORD-07` khuyết số trong dãy. **Sạch:** B-01/05/06 counts module↔router **10/10 chỉ số** khớp (REQ 142 · SC 302 · P 40/171/89 = 300 = 302−2 DEPR · CL 67 · RISK 87) · C-02/C-03/C-04 **223 TC, 0 trùng ID, 0 orphan SC, fragment↔xlsx 0 drift 11/11 module, TC Gen Log Σ=223** · Mode suy từ artifact = `standard` (0 Coverage Matrix, 0 technique tag) khớp ô log ⇒ C-08/C-09/C-10 không raise · F-04 223/223 TC + 139/139 SC ref đúng pattern · F-05 **0/58 REQ thiếu quote** · F-06 **0 SC thiếu quote** (heading dạng dải `SC-DLV-037..042` tính đủ) · F-07 4/4 DOC v1.1 resolve · F-08 0 file input chưa DOC-ID · G-01/G-08 router chỉ §1+§2 · G-05 0 dòng skill trùng · D/E N/A (manual, registry rỗng đúng) · H-03 9/9 section · H-04 3/3 hook + 5/5 file · H-05/H-06 `verify_evidence.py`/`oracle_runs.py`/`validate-vibe-run.mjs` **identical** toolkit. ✅ **4/4 CRITICAL ĐÃ SỬA cùng ngày (2026-09-17).** **(1+2)** Phát hiện hướng sửa ban đầu **SAI CHIỀU** rồi đảo lại: `SC-ORD-059` **giữ `NEW`** — nhãn `MODIFIED` gán sáng 2026-09-17 là sai theo đúng định nghĩa dự án (*"MODIFIED = giữ nguyên ID v1.0"*), vì SC này **sinh ở v1.1** (v1.0 chỉ có `SC-ORD-001..051`). 🔬 **Phép kiểm mới — chạy mỗi lượt health-check:** `carried+modified+deprecated` (v1.1) **phải =** tổng SC v1.0 của chính module đó. Chạy 2026-09-17: 10/11 khớp, `ORD` ra `52 ⟷ 51` ⇒ chỉ thẳng vào SC bị dán nhãn sai. Chứng cứ độc lập 2: `TC-MASTER-v1.1.xlsx` đã ghi `TC-ORD-075/076` là `Lifecycle NEW`. Sửa đủ **3 tầng**: `ORD/test_scenario_map.md` (`counts` 14/12 + cột Lifecycle + khối 🔁) → router §2a (ORD 14/12 · Tổng **90 NEW / 45 MOD**) + §2b → MASTER §1 (Δ 135 · DOC 3→**4**) · §3 (dòng Tổng + 2 khối ⚠️ giải thích) · §4 (**132 → 139 SC**) · §5 · `ORD/CHANGELOG §1` dòng ĐÍNH CHÍNH. 🔑 Chốt **2 con số không được lẫn**: **Δ lifecycle = 135** ⟷ **mẫu số phải test = 139** (135 + 4 SC `FEED` bị chạm, giữ lifecycle NEW). **(3)** §7 Downstream Path viết lại (v1.1 = 11 fragment + `TC-MASTER-v1.1.xlsx` **223 TC CONSOLIDATED**, cảnh báo phải mở **song song 2 file**); ô §8b #4 gỡ *"Chưa consolidate"* — nay chỉ còn *"chưa review (G1)"*. **(4)** `CLAUDE.md` viết lại §Version Info: tách **version phân tích v1.1** ⟷ **version thực thi v1.0**, thêm **bảng 2 TC-MASTER** + cảnh báo **54 TC trùng ID / 5 TC kỳ vọng ngược**, sửa `TC-MASTER-LATEST.xlsx` = copy **v1.1** (verify md5), Workflow + Folder Reference đổi sang `v[X]`. ✅ Kiểm chứng sau sửa: 3 tầng khớp · 10/10 module qua phép kiểm lifecycle · TC-MASTER **không bị đụng** (223 TC / 139 SC nguyên trạng — chỉ sửa `.md`). ✅ **TIẾP: 7/7 WARNING + INFO cũng đã sửa xong cùng ngày (lượt 3).** **#1 G-06b** — **18** chỗ *"chờ `<CL>`"* cho CL đã Resolved (nhiều hơn 14 của lượt trước vì quét thêm `test_data_catalog` + `requirement_traceability`): thay bằng **kết luận BA thật**, ⛔ không xoá chữ suông. Phát hiện thêm **11 heading CL** còn ghi `OPEN`/`PARTIALLY` trong khi bảng Status ghi `Resolved` (`C-ORD-04/09/13/16` · `C-ACT-04` · `C-DLV-05/06` · `C-GIFT-04` · `C-HOME-06` · `C-TS-03` · `C-FEED-01`) — đã đồng bộ. **#2** `ASN/risk_assessment` KN#2 + TC `CHANGELOG §3 Nợ #7` — ⚠️ **dòng nợ tự nó mới là chỗ stale**: ASN/NTF **đã** write-back đủ từ trước. **#3 A-01** §8b `analyze-requirements` → Last Run **2026-09-17**, số về canonical **142 REQ · 302 SC · 67 CL (0 Open · 66 Resolved · 1 Partially) · 87 RISK**. **#4 F-01** router `doc_source`: thêm **`DOC-v1.1-04`**, sửa *26 → 31 ảnh*, `modules` đủ 9. 📌 Ghi thêm vào `§2` quy ước đếm citation của `DOC-v1.1-03`: **cả 9 module đều cite thật nhưng bằng TÊN FILE ảnh**, chỉ 3 module kèm chuỗi DOC-ID ⇒ đếm theo DOC-ID sẽ báo **orphan oan**, phải grep tên file. **#5 G-03** 14 nhóm quote lặp → 15 dòng đổi thành pointer `↪` về home (REQ→traceability · SC→scenario_map · CL→risk_assessment). **#6 G-07** `CL-hoi-BA-v1.1.xlsx` gắn **banner DERIVED đỏ** + ghi cách dựng lại + đồng bộ `ORD` (1 → **0** điểm hỏi BA, `C-ORD-18` Resolved) ⇒ tổng cột = **0**, khớp `cl_open = 0`. **#7** 14 file `updated:` lỗi thời → đặt lại theo **ngày mới nhất CÓ TRONG NỘI DUNG** (⛔ không theo mtime). 🔬 **Kiểm chứng lượt 3 — tất cả về 0:** counts 3 tầng khớp + phép kiểm lifecycle 10/10 · 223 TC / 0 trùng ID / 139 SC · F-05 **0** · F-06 **0** · G-03 **0** · G-06b **0** · heading lệch **0** · F-01 ✅ · ảnh 31=31 · `updated:` lỗi thời **0**. 🟢 **v1.1 SẠCH — 0 CRITICAL · 0 WARNING · 0 INFO.** ⏭️ Bước kế: **`/review-tc`** (gate G1 — nợ duy nhất còn lại của `generate-tc` v1.1). |
| 7 | vibe-test | **PARTIAL** | 2026-09-19 | v1.1 + CARRIED v1.0 · **VR-001/003 `USR`** · **VR-002/004 `ORD`** · **VR-005 `HOME`** · **VR-007 `ASN`** · **VR-016 `FEED`** · **VR-017 `ACT`** · **VR-018 `CNL`** · mobile (Appium MCP, emulator-5554) | `08_test-runs/vibe/VR-001-USR-2026-09-18/` (log · report · locators · mcp-session-log · scope-ledger · 66 ảnh) · **sổ cái tích lũy `08_test-runs/vibe/coverage/coverage-USR.md`** · `08_test-runs/vibe/locators/vibe-locators-latest.md` | **scope 46: có verdict 42 · CÒN NỢ 4 · evidence 40/40 · 19P/15F/6B/2 N-A · 77 locators (45 ✅ verified) · MCP calls ~330.** 🔴 **SCOPE_TOTAL = 46 = 38 (v1.1) + 8 CARRIED v1.0** — v1.1 không gộp CARRIED ⇒ phủ đủ USR phải mở **cả 2 fragment**. 🐞 **8 ứng viên bug (B1..B8)** từ 12 TC FAIL; 3 TC FAIL còn lại là **dây chuyền** (`029`/`030` do B1+B4 · `011` không phải lỗi app) ⇒ ⛔ không mở 15 bug rời. Nặng nhất: **B1** lưu thành công **không có banner** `Đã lưu thông tin của bạn` + app **thoát màn** (TC **P1** `TC-USR-015`; soi 60 frame, dữ liệu **vẫn lưu đúng** — `TC-USR-016` PASS ⇒ lỗi UI feedback, KHÔNG mất dữ liệu) · **B2** chặn lưu **IM LẶNG 0 thông báo** với SĐT chứa khoảng trắng/`+84` (`021`/`022`, xác minh 2 lượt + kiểm persist) · **B4** chuỗi gõ tay **không bị xoá rỗng và BỊ LƯU** làm địa chỉ mặc định (`028`; kiểm 3 tín hiệu blur) ⇒ sẽ prefill chuỗi rác sang **địa chỉ lấy hàng của đơn**, ảnh hưởng ngoài USR · **B5** gợi ý địa chỉ tìm theo **mã tỉnh** thay vì tên VP (`039`: `hcm` → 12 VP HCM mà 0 tên chứa `hcm`) — đúng cảnh báo `USR-office-catalog §2 K6`, vi phạm `C-USR-05`. 🚫 **6 BLOCKED**: 5 TC (`032`/`034`/`035`/`036`/`037`) do **master data VP trên STG ≠ `DOC-v1.1-04`** — 3 phép đo độc lập (`Lê Thái Tổ` khớp ✅ · `Tầng 20` → **0 gợi ý** ⇒ VP không tồn tại · `fpt` → 9 VP, chỉ 2/8 tên khớp) ⇒ **oracle không có trên STG, không phán quyết được app** ⇒ cần **BA refresh `DOC-v1.1-04`**; 1 TC (`027`) dây chuyền B1. ✅ **3 rule gợi ý VẪN ĐÚNG** (kiểm độc lập với lệch data): ngưỡng 3 ký tự · không phân biệt dấu+hoa/thường (`Cẩm Lệ`/`cam le`/`CAM LE` ra **tập y hệt**) · khớp chứa-chuỗi giữa từ (`tan` ⊂ `tang`). 🟢 **Vùng SẠCH NHẤT — ranh giới hồ sơ ⟷ đơn, cả 2 chiều ĐÚNG:** `TC-USR-025` (**P1**, đổi hồ sơ ⇏ đổi đơn đã đăng) + `TC-USR-026` (sửa trong đơn ⇏ ghi đè hồ sơ); persist cũng sạch (`016` mở lại màn · `042` mở lại app · `030` vế HRIS ⇒ **HRIS không ghi đè hồ sơ đã lưu**). ⏳ **4 NOT_RUN cùng 1 blocker duy nhất: cần đăng nhập tài khoản khác + OTP nhập tay** (AI không lấy được OTP) — `040`+`043` vai **C** (gom 1 phiên, chạy **trước mọi lần bấm Lưu** trên C; `043` chờ **từ 19/09** vì SĐT HRIS mới đổi 18/09) · `006` vai **BLANK** · `005` cần **3 lượt login** + hoàn tất 1 đơn end-to-end. 🔴 **Dữ kiện then chốt cho `040`/`043`:** tài khoản A mở màn lần đầu **CẢ 2 field SĐT + địa chỉ đều RỖNG** dù HRIS có đủ ⇒ **FoxEco KHÔNG prefill từ HRIS**; nếu C cũng vậy thì 2 TC đó **FAIL và là bug thật**. 🙋 Kèm: vì app không prefill, **mọi** user lần đầu đều thấy ô SĐT rỗng ⇒ tình huống `TC-USR-044` (đã DESCOPE vì *"không tồn tại"*) **thực tế CÓ xảy ra** — ⛔ không tự đổi verdict (`§10.5` FREEZE), **báo QC quyết**. 📨 **2 việc route `/analyze-requirements --update`, KHÔNG phải bug:** (R1) `TC-USR-011` CARRIED v1.0 (*"không có bề mặt cấu hình kênh liên hệ"*) **mâu thuẫn trực tiếp** `FR15` v1.1 vốn cố ý THÊM field SĐT ⇒ lẽ ra phải `DEPRECATED`/`MODIFIED` chứ không `CARRIED`; (R2) wizard "Đăng tin" có **2 field bắt buộc chưa đặc tả** — `ẢNH HÀNG *` (≥1 ảnh, Bước 1/3) + `BUỔI MONG MUỐN` (≥1 buổi, Bước 2/3), nút `Tiếp theo` **disable âm thầm không nói thiếu gì** (mất ~15 phút dò). 🗂️ **Dữ liệu phát sinh trên STG:** 2 tin NEED `Chờ ghép` (ghi chú `Test vibe VR-001 TC-USR-025/026`, người nhận `stag_anhdc4@fpt.com`) ⇒ ảnh hưởng TC đếm đơn của `ORD`/`FEED`/`HOME`; hồ sơ A đổi thành SĐT `0912345670` + địa chỉ `363 Nguyễn Hữu Thọ, Cẩm Lệ` (trước phiên: **cả 2 rỗng**) ⇒ ⛔ **A không còn trạng thái "chưa từng lưu hồ sơ"**, đừng dùng A test prefill-HRIS nữa. 🔧 **5 bẫy kỹ thuật cho `implement-automation`** (đầy `vibe-locators.md`): `selected` **không bao giờ** expose trên tab/chip RN · `clickable` **không** phản ánh enable/disable · một số `resource-id` có trong page source nhưng `id`/`accessibility id` **không resolve** (dùng content-desc) · `get_element_attribute` không nhận `showing-hint` (đọc qua page source) · ô địa chỉ **buộc chọn từ gợi ý**. ⚠️ Gate `.claude/hooks/verify_evidence.py`: **evidence 40/40 + trích dẫn 40/40 ✅, 0 vi phạm**; exit 1 **chỉ** do 4 TC còn nợ ⇒ đúng ca PARTIAL. 🔵 **VR-002 · 2026-09-18 · module `ORD` · mobile (Appium MCP, emulator-5554) · tài khoản A:** `08_test-runs/vibe/VR-002-ORD-2026-09-18/` · sổ cái **`08_test-runs/vibe/coverage/coverage-ORD.md`**. **scope 88 (= 48 v1.1 + 40 CARRIED v1.0): có verdict 42 · CÒN NỢ 46 · evidence 43/43 · 25P/14F/3B · 62 locators (47 ✅) · 4 snapshot / 5 màn.** 🐞 **14 FAIL ≠ 14 bug — gộp về 6 ứng viên:** **B1 CHẶN IM LẶNG (High)** nút `Tiếp theo` khoá mà **không báo lỗi** ở 5 nhánh: thiếu ảnh (`063` **P1**) · thiếu TRỌNG LƯỢNG (`064`) · thiếu KÍCH THƯỚC (`066`) · địa chỉ giao trùng địa chỉ lấy (`083`/`084`, so sánh **có trim**) · email sai định dạng (`077`). ⚠️ **Không phải thiếu framework**: cùng app **CÓ** báo lỗi inline đúng ở 3 nhánh khác (ảnh >5MB `068` · chưa chọn buổi `058` · khoảng ngày >7 `057`) ⇒ là **bỏ sót 5 nhánh validate**. **B2 (High)** KHÔNG có popup `Thoát và bỏ nội dung đã nhập?` — back ở **bước 1** thoát thẳng + **xoá dữ liệu soạn dở**, trái `AC-01.2.01`/`C-ORD-08`; back ở **bước 2** chỉ lùi bước (Steps `053`/`062` mô tả sai đường đi). Cùng họ `TC-USR-045` của VR-001 ⇒ **nghi lỗi hệ thống, đề nghị log ở phạm vi app**. **B3 (High)** **trần ảnh thực tế chỉ 4/5**: sau ảnh thứ 4, `multi-photo-add-button` **biến mất khỏi cây** + dải ảnh **không cuộn ngang** (thử 3 kiểu gesture) + `multi-photo-remove-3` NOT FOUND ⇒ ảnh thứ 4 **không xoá được** ⇒ `070`/`071` **BLOCKED bởi bug**, không phải thiếu data (đã chuẩn bị đủ 6 ảnh). **B4 (Medium, `074` P1)** autofill người nhận chỉ **2/3 ô** (tên ✓ SĐT ✓, **địa chỉ giao RỖNG**) — ⚠️ **hỏi BA TRƯỚC KHI LOG** (`AC-04.x`): HRIS có địa chỉ ⇒ bug app, còn nếu spec chỉ autofill 2 ô ⇒ sửa Expected. **B5 (Low, `058`)** buổi mong muốn **không có mặc định** (PRD `§8.1.4` chốt `Sau giờ làm (17–19)`). **B6 (Medium, `059`)** **cho chọn buổi đã trôi qua** (13:44 vẫn chọn được `Sáng 8–12h`) — trái `C-ORD-15(b)`, ⚠️ rule **không có trong PRD** nên log bug **phải dẫn CL này**. 🟢 **Vùng SẠCH:** oracle `C-ORD-18` **khớp 100% app** (nhãn **"TRỌNG LƯỢNG"** + Nhẹ/Trung bình/Nặng, **"KÍCH THƯỚC"** + Cầm tay/~20×20/> 20×20, kiểu **chip**) ⇒ ⛔ không có defect nhãn như §0.2 dự phòng · `C-ORD-14` **được thi hành** (email lạ/nghỉ việc ⇒ chặn tạo đơn kể cả khi điền tay đủ, `075`/`076` PASS) · `C-ORD-15(a)` đúng **cả 2 chiều** biên 7/8 ngày · `C-ORD-10`/`C-ORD-11` đúng (prefill địa chỉ hồ sơ + ô văn bản tự do, ⛔ không preset — **khác hẳn** ô địa chỉ của USR) · trần 200/300 ký tự đúng biên · `062` không lưu nháp. 🚫 `069` **BLOCKED — không có đường UI**: app chỉ mở camera + **Android Photo Picker** (picker lọc sẵn chỉ ảnh), `.pdf` có thật trong `/sdcard/Pictures` vẫn không hiện ⇒ ⛔ **không PASS chỉ vì không có đường thử**; 📨 QC chốt `N-A` hoặc đổi Expected. 🧪 **Test data tự dựng** (`SEED-ORD-01`, PIL → `/sdcard/Pictures/seed_*`, **không commit**): 6 JPG nhỏ + 1 PNG + 1 ảnh **đúng 5MB** (⏳ chưa dùng — biên dưới hợp lệ) + 1 ảnh 6MB + 1 `.pdf`. 🗂️ **Không phát sinh dữ liệu trên STG** (phiên này **chưa đăng tin nào**, chưa tới bước 3) ⇒ ⛔ không thêm đơn rác; hồ sơ A **không bị sửa**. 🔧 **5 bẫy kỹ thuật MỚI T6–T10** + **5 đính chính** locator VR-001 (đã merge `locators/vibe-locators-latest.md`, hấp thụ **62/62 = 100%**): rid React Native **chỉ resolve** qua `-android uiautomator resourceId(...)` ⛔ không qua `id` · nút thêm ảnh có `content-desc` = **chính bộ đếm** (`0/5`→`1/5`) ⇒ dùng `resourceId("multi-photo-add-button")` · element **ngoài viewport KHÔNG resolve** ⇒ phải `scroll_to_element` trước · **`appium_mobile_keyboard(hide)` gửi BACK** ⇒ ở bước 1 wizard = **thoát + mất draft** (⛔ đừng dùng để rời ô) · điều kiện enable `Tiếp theo` B1 = **4 thứ** (giá trị + trọng lượng + kích thước + ≥1 ảnh), B2 = email **tra thấy HRIS** + tên + SĐT + địa chỉ giao **≠ địa chỉ lấy** + ≥1 buổi. ⚠️ **Sự cố phiên:** UiAutomator2 crash 1 lần giữa lô 3 → tạo lại session (`noReset=true, autoLaunch=false`), **0 TC bị mất** (app giữ state, chỉ phải find_element lại). 📨 **5 việc route `/analyze-requirements`** (spec-gap, KHÔNG phải bug): host app **đổi tab** sau khi đóng photo picker · chuỗi *"vui lòng nhập tay"* còn câu chữ `BR01-09` v1.0 mà `C-ORD-14` đã đảo · địa chỉ giao có thuộc nhóm autofill không (**quyết định B4**) · app **không phân biệt** email ngoài tên miền vs không có trên HRIS · tầng lọc định dạng file thuộc app hay OS. ⚠️ Gate `.claude/hooks/verify_evidence.py`: **evidence 43/43 + trích dẫn 43/43 ✅, 0 vi phạm fabrication**; còn nợ 46 TC ⇒ đúng ca **PARTIAL**. ▶️ Chạy tiếp: `/vibe-test --module ORD` (**46 TC**; đề xuất lô 4 = bước 3 + đăng tin hoàn tất để **tự sinh `SEED-ORD-04`**). 🔄 **ĐÍNH CHÍNH 2026-09-18 — `TC-USR-015` (P1): ❌ FAIL → ✅ PASS, ứng viên bug `B1` RÚT LẠI (còn 7, không phải 8).** QC GiangDC2 chốt **hành vi app ĐÚNG**: lưu hợp lệ ⇒ **không có banner** `Đã lưu thông tin của bạn` + app **điều hướng về màn "Cá nhân"**. Đã sửa `Expected Result` + `Notes` của TC ở **cả 3 nơi** (`fragments/TC-USR-v1.1.md` · `TC-MASTER-v1.1.xlsx` sheet `ALL` + `Tài khoản & Hồ sơ` · `TC-MASTER-LATEST.xlsx`) — `§10.5` chỉ cho sửa Expected/Notes, **Σ TC không đổi (223 · USR 38 · md5 LATEST == v1.1)**. Kết quả VR-001 sau đính chính: **20P / 14F / 6B / 2 N-A / 4 NOT_RUN**, verdict cuối **42/46 không đổi**. ⚠️ **CHƯA xử lý, chờ QC quyết:** `TC-USR-027` (BLOCKED — TC *"banner tự ẩn"* neo vào banner nay đã bỏ) · `TC-USR-029` (FAIL 2 lý do, lý do banner hết hiệu lực, **B4 vẫn còn**) · upstream `SC-USR-014` THEN + `AC-30.1.01`/`BR15-05` vẫn ghi *"banner + ở lại màn"* ⇒ cần `/analyze-requirements --module USR` + clarification BA · `Test Title` của `TC-USR-015` vẫn chứa *"hiển thị đúng banner xanh"* (Title không thuộc field được sửa ở `§10.5`). 🟣 **VR-003 · 2026-09-18 · module `USR` — CHẠY TIẾP PHẦN CÒN NỢ · tài khoản `00002352` (`stag_thuyntt22@fpt.com`, "Nguyễn Thị Thanh Thủy"):** `08_test-runs/vibe/VR-003-USR-2026-09-18/` · sổ cái `08_test-runs/vibe/coverage/coverage-USR.md`. **scope 46: có verdict 45 · CÒN NỢ 1 · evidence 4/4 · 2P/2F · 19 locators (17 ✅) · 5 snapshot / 5 màn · gate exit 0, 0 vi phạm.** Chạy **4/5 TC còn nợ** ⇒ USR từ **42/46 → 45/46**. 👤 **Tài khoản KHÔNG có trong `USR-accounts.md` lúc bắt đầu phiên** (không thuộc A/B/C/BLANK1/SPARE) — QC GiangDC2 xác nhận tại chỗ 2026-09-18 là **chưa từng lưu hồ sơ**, và đo được **2 chỉ số = 0/0** ⇒ dùng cho **cả** vai BLANK (`006`) **lẫn** thay vai C (`040`/`043`). Đã bổ sung vào `USR-accounts.md §1`. ✅ **`TC-USR-006` PASS** — card chỉ số hiện đủ 2 ô, cả 2 = `0`, **card KHÔNG bị ẩn** (đúng cặp biên với `TC-USR-005`) ⇒ **đóng gap #2 của `USR-accounts.md §4`**: lần đầu có tài khoản được **verify** 2 chỉ số = 0 (trước đó `BLANK1` mới chỉ là *ứng viên*, `BLANK1` nay không cần dùng và còn nguyên trạng thái). ✅ **`TC-USR-027` PASS** theo **bản Steps/Expected sửa 2026-09-18** (kiểm tính nhất quán hành vi **không-banner** qua **2 lần lưu liên tiếp** `0987654321` → `0987654322`): cả 2 lần đều `textContains("Đã lưu")` 🚫 NOT FOUND + đều điều hướng về "Cá nhân" ⇒ **gỡ nốt hệ quả của việc rút lại `B1`**; `TC-USR-027` hết treo. 🐞 **1 ỨNG VIÊN BUG MỚI — `B9` (Medium), gộp 2 TC:** `TC-USR-040` + `TC-USR-043` **cùng FAIL vì 1 defect** — màn "Cập nhật thông tin" **KHÔNG prefill SĐT và địa chỉ từ HRIS** ở lần mở đầu tiên: cả 2 EditText `showing-hint="true"` ⇒ **rỗng hoàn toàn**, dù HRIS của chính tài khoản đó **CÓ** SĐT (đọc oracle tại chỗ ở FoxPro → Cá nhân → Thông tin cá nhân → Thông tin). Vế *"cho phép nhập"* của Expected **ĐẠT** (con trỏ + bàn phím) — chỉ vế *"hiển thị giá trị HRIS"* fail. ⛔ **Không phải ca lẻ:** VR-001 đã quan sát **y hệt** trên tài khoản **A** — tài khoản CÓ địa chỉ HRIS đã biết (`HCM LôB3,E-Office,KCN TânThuận`) ⇒ 2 tài khoản / 2 phiên / cùng hiện tượng. 🕐 **Bẫy thời điểm 18/09 KHÔNG áp dụng cho `043`** — bẫy đó nói về *giá trị lệch do chưa đồng bộ*, còn đây là *không prefill gì cả* ⇒ verdict **FAIL** (không phải `BLOCKED`), **được phép log bug**. ⚠️ **Giới hạn oracle của `040` (ghi rõ để không bị hiểu là suy diễn):** HRIS mobile **không phơi trường "địa chỉ làm việc/văn phòng"** cho chính người dùng (đã kiểm cả *Thông tin* lẫn *Quá trình làm việc*) và `00002352` không có trong `00_input/v1.1/datatest` ⇒ **không đọc được chuỗi VP kỳ vọng**. FAIL vẫn đứng vững vì (a) field rỗng không thể bằng bất kỳ địa chỉ nào, (b) QC đã chốt HRIS **bắt buộc** trường địa chỉ làm việc (`USR-accounts.md §4` gap #1), (c) đối chứng trên A. 🙋 **3 việc chờ QC/BA quyết (⛔ phiên này không tự đổi):** **Q1** xem lại DESCOPE `TC-USR-044` (phiên này củng cố thêm bằng chứng tình huống "ô SĐT rỗng khi mở lần đầu" CÓ xảy ra thật — nhưng lý do DESCOPE gốc *"HRIS trống SĐT"* vẫn đúng về data ⇒ đây là **TC mô tả sai nguyên nhân**, không phải TC thừa) · **Q2** **địa chỉ mặc định RỖNG vẫn LƯU ĐƯỢC** — quan sát ngoài lề khi chạy `027`, app không chặn, không báo lỗi ⇒ **dữ kiện thực nghiệm cho câu hỏi BA đang treo ở `TC-USR-029`** · **Q3** `Test Title` của `TC-USR-027` vẫn là *"Check banner đã lưu tự ẩn…"* trong khi Steps/Expected đã đổi hẳn ⇒ **đúng y case `TC-USR-015`**, Title không thuộc field được sửa theo `§10.5`. 📨 **Route `/analyze-requirements --module USR`:** (R3) nợ upstream từ VR-001 nay **nặng thêm** — `SC-USR-014`/`SC-USR-019` + `BR15-05` + `AC-30.1.01` vẫn mô tả *"banner xanh"*/*"banner tự ẩn"* trong khi **2 TC (`015`, `027`) đã PASS theo hành vi KHÔNG banner** ⇒ đặc tả và kết quả test đang mâu thuẫn công khai. (R4) `SC-USR-022`/`SC-USR-023` đặc tả *"prefill từ HRIS"* — **chưa kết luận**, chờ BA phản hồi `B9` rồi mới quyết sửa app hay sửa SC. 🗂️ **Dữ liệu phát sinh:** hồ sơ `00002352` nay có SĐT `0987654322`, địa chỉ vẫn rỗng ⇒ 🔴 **tài khoản này MẤT trạng thái "chưa từng lưu hồ sơ"**. **Tài khoản kiểu này là tài nguyên TIÊU HAO** — sau VR-003 chỉ còn **vai C (`00041796`)** giữ được trạng thái đó ⇒ retest `B9` sau khi dev fix **phải dùng C**, ⛔ đừng bấm "Lưu" trên C trước. 2 chỉ số card vẫn `0/0` (lưu hồ sơ không ảnh hưởng chỉ số). 🔧 **4 bẫy kỹ thuật MỚI T11–T14** (merge `locators/vibe-locators-latest.md`, hấp thụ 19/19 = 100%). Nguy hiểm nhất **T11**: field RỖNG vẫn trả `text` = **chuỗi hint** (`09xx xxx xxx`) ⇒ assert `text != ""` sẽ **PASS OAN** — đúng chỗ `040`/`043` dễ bị đọc ngược; phải assert bằng `showing-hint`. **T12** 2 field màn "Cập nhật thông tin" **không có `resource-id`/`content-desc`**, chỉ định vị bằng **thứ tự EditText** ⇒ nên xin dev gắn `testID` trước khi automate. **T13** đường về FoxEco = back×2 → `Chức năng` → `FoxEco` (không deeplink). **T14** HRIS mobile không phơi địa chỉ làm việc ⇒ đừng phí phiên đi tìm. 📌 **Đính chính bookkeeping `coverage-USR.md`:** §Tổng hợp trước phiên ghi `BLOCKED 6 / NOT_RUN 4` nhưng đếm trên bảng chi tiết là **`5 / 5`** (sót lúc `TC-USR-027` đổi `BLOCKED → NOT_RUN` ngày 2026-09-18; Σ vẫn 46 nên lỗi không lộ). Đã sửa cả `coverage-USR.md` lẫn dòng VR-001 ở `08_test-runs/vibe/INDEX.md`. ⏳ **CÒN NỢ 1: `TC-USR-005`** — QC GiangDC2 chốt 2026-09-18 **bỏ qua phiên này**, QC tự tạo data (3 vai + 1 đơn chạy tới "Hoàn thành") rồi báo lại để check. ▶️ Chạy tiếp: `/vibe-test --tc TC-USR-005`. 🔑 **Đính chính vận hành:** `00_input/v1.1/datatest` ghi **OTP dùng chung mọi account** ⇒ AI **có thể** tự đăng nhập khi được QC cho phép, nhưng ⛔ **phải hỏi trước** (đăng xuất sẽ huỷ phiên người chạy vừa tạo; đăng nhập nhầm vai làm hỏng trạng thái dữ liệu không khôi phục được). Đã ghi vào `USR-accounts.md §0`. 🟠 **VR-004 · 2026-09-18 · module `ORD` · `--all` (chạy lại TOÀN scope) · tài khoản A · emulator-5554:** `08_test-runs/vibe/VR-004-ORD-2026-09-18/` · sổ cái `08_test-runs/vibe/coverage/coverage-ORD.md`. **scope 88: có verdict 69 · CÒN NỢ 19 · evidence 46/46 · 27P/17F/2B · ~41 locator mới (35 ✅) · 9 snapshot / 6 màn · MCP calls ≈390 · gate 0 vi phạm fabrication, exit 1 chỉ vì còn nợ.** Chạy **46 TC / 5 lô** ⇒ ORD từ **42/88 → 69/88** *(♻️ 2026-09-21 QC reset `TC-ORD-083`/`084`/`085` về NOT_RUN ⇒ **66/88, nợ 22**)*. 🛑 **Còn nợ KHÔNG do hết sức phiên** — đã chạy hết TC chạy được; **19 TC chặn tiền đề**: **17** cần 1 đơn NEED đăng thành công, **2** cần tài khoản B. 🐞🔴 **B1 — P1 BLOCKER MỚI, nặng nhất module: KHÔNG ĐĂNG ĐƯỢC TIN NEED (`TC-ORD-004`).** Dữ liệu hợp lệ đủ 3 bước + đã tick điều khoản ⇒ bấm `Đăng tin ngay` **không có gì xảy ra**: không toast/banner/spinner, nút vẫn bật, màn ở lại Bước 3/3. logcat: `E ReactNativeJS 'Failed to create post:' [FoxEcoApiError: Dữ liệu đầu vào không hợp lệ] code REQ_400, retryable:false, details:undefined` — **3 lần bấm / 3 lần lỗi** (00:18·00:19·00:21). **Đã loại trừ** giả thuyết ghi chú 300 ký tự (chạy lại ghi chú ngắn ⇒ vẫn 400); **đã xác nhận không đơn nào được tạo** (khối *Đơn của tôi* + số *13 đơn đã giúp* không đổi). Bằng chứng: `logcat-TC-ORD-004-REQ_400.txt`. ⛔ **Chặn cứng 17 TC** ⇒ module ORD **không thể đóng** cho tới khi fix. Nên tách 2 bug nếu Dev API ≠ Dev app: (a) API từ chối payload hợp lệ · (b) client nuốt lỗi (`details: undefined`). 🔑 **TÌM RA NGUYÊN NHÂN GỐC CỦA 'CHẶN IM LẶNG' Ở BƯỚC 2 (bẫy T11):** **CẢ HAI ô địa chỉ (`Địa chỉ lấy hàng` + `Địa chỉ giao hàng`) BẮT BUỘC chạm gợi ý `address-suggestion-N`**; gõ tay đủ chữ, text hiển thị đúng, nhưng **`Tiếp theo` giữ `enabled=false` VĨNH VIỄN và app không báo gì**. Mất ~20 MCP call để khoanh vùng. Kích hoạt bởi việc hồ sơ A **mất địa chỉ mặc định** (gốc `TC-USR-040`, VR-003) ⇒ ô này nay **RỖNG**, mọi phiên sau **buộc** gõ + chạm gợi ý. ♻️ **BÁC BỎ MỘT PHẦN KẾT LUẬN VR-002 — `083`/`084`:** luật *'địa chỉ giao phải khác địa chỉ lấy'* **CÓ thông báo lỗi đầy đủ** (`Địa chỉ giao phải khác địa chỉ lấy hàng`) — chứng minh bằng **`TC-ORD-026` PASS** (2 địa chỉ chọn từ gợi ý, trùng nhau ⇒ chặn + báo lỗi đúng). VR-002 kết luận *'chặn im lặng'* vì Steps của `083` bảo **gõ tay** ⇒ validate **chưa bao giờ chạy tới** luật trùng. ⇒ **gỡ `083`/`084` khỏi dẫn chứng bug chặn-im-lặng-nhánh-trùng-địa-chỉ**, chuyển sang bug *'địa chỉ phải chọn gợi ý'*. Ghi chú VR-002 *'so sánh CÓ trim'* là **suy diễn không căn cứ** — phiên này **không kết luận được** app có trim hay không, và `TC-ORD-084` **hiện không thực thi được đúng ý đồ** trên v1.1 ⇒ QC xem lại thiết kế TC. ⚠️ **1 TC ĐỔI VERDICT: `TC-ORD-017` ✅ → ❌** (ô `Địa chỉ lấy hàng` mất prefill). **KHÔNG phải TC lỗi thời — lỗi thật**; gộp **1 bug B4 xuyên 2 module** với `TC-ORD-043` (điểm xuất phát OFFER cũng không prefill) + **`TC-USR-040`** (VR-003). 🟢 **ĐỐI CHỨNG MẠNH NHẤT CHO BUG CHẶN IM LẶNG (dùng khi Dev phản bác):** cùng màn Bước 2/3 — ô **SĐT người nhận** báo lỗi đúng **5/5 nhánh** (`TC-ORD-025` PASS: 9 số·11 số·không bắt đầu 0·có chữ ⇒ `Số điện thoại không hợp lệ`) và ô **SĐT/tên uỷ quyền** cũng có lỗi (`079`/`081`: `Tên phải từ 2–60 ký tự`), trong khi ô **email** và ô **tên người nhận** **không có lỗi nào** (`021`/`024`/`051`). ⇒ ⛔ **KHÔNG được đóng bug bằng lý do 'app chưa có cơ chế báo lỗi'** — cơ chế có sẵn, **thiếu theo từng nhánh**. **TC đại diện nên dùng khi log: `TC-ORD-051`** (0/2 expected: không lỗi tại ô **và** không cuộn tới ô lỗi đầu tiên). 💡 **Khuyến nghị thiết kế (dữ kiện đắt):** form **OFFER** dùng cơ chế *nút luôn bật + lỗi đỏ inline khi submit* (`Chọn ít nhất 1 buổi`) ⇒ **UX tốt hơn NEED**. `TC-ORD-050` FAIL vì trái `VAL-01`, nhưng **đừng fix OFFER cho giống NEED — hãy port cơ chế inline của OFFER sang NEED** để dứt điểm B2. 🐞 **Bug mới khác:** **B6 lỗi validate không tự xoá khi đã sửa input** (bẫy **T13** — suýt tạo verdict PASS sai ngay trong phiên, phải dựng lại form sạch mới chấm được `083`) · **B8 copy không có phản hồi thị giác** (`085`: nội dung copy **đúng** `FPT Cầu Giấy` đo bằng **sentinel clipboard**, nhưng icon **không đổi xanh** đo bằng **lấy mẫu pixel** 2 ảnh liên tiếp trong 2s) ♻️ **1 ứng viên bug TỰ RÚT NGAY TRONG PHIÊN — `B9` *nút disable không làm mờ* (`008`): KHÔNG phải bug.** Kết luận ban đầu dựa trên **nhìn ảnh bằng mắt**; khi **đo pixel** thì nền nút disable `(253,175,62)` (cam nhạt, blend trắng) khác hẳn enable `(255,160,0)` (cam bão hoà) ⇒ **app CÓ làm mờ**, **VR-002 đã đúng** (`CHANGELOG ORD §Nợ #3`). ⚠️ **Bài học vận hành:** khác biệt opacity **không đánh giá được bằng mắt trên screenshot** — phải lấy mẫu pixel (cùng kỹ thuật đã dùng cho `085`). 🔴 **NỢ QC — 5 TC hết hiệu lực chờ `DESCOPED` (`§10.5`):** `006` · `014` (**mâu thuẫn TRỰC TIẾP `063` P1 — 2 TC không thể cùng đúng**) · `007` · `008` · `033` (v1.1 bỏ khung giờ mặc định, thay bằng chip BUỔI **không có mặc định**; rule đã được `059` phủ). ⭐ Riêng **`011` chỉ cần BỎ step 4-5** là dùng lại được cho v1.1 — **không cần DESCOPED** (biên 300/301 PASS trọn). 📨 **3 việc route `/analyze-requirements --update`:** (R5) **tóm tắt Bước 3/3 THIẾU `TRỌNG LƯỢNG` + `KÍCH THƯỚC` + ảnh** — 3 trường v1.1 mới thêm và **đều bắt buộc**, mà **không TC nào của v1.1 assert chúng xuất hiện ở bước 3** ⇒ spec gap · (R6) **2 ô địa chỉ bắt buộc chọn từ dropdown gợi ý** — hành vi **không có trong scenario_map** · (R7) email **ngoài tên miền** nhận thông báo *'Không tìm thấy email này — vui lòng nhập tay…'* thay vì báo sai tên miền ⇒ raise cùng **F5**. 🙋 **1 clarification BA:** màn chi tiết đơn **đã ghép** **không lộ SĐT nào** (`086` BLOCKED) — **trái banner cam kết** *'sau khi ghép, SĐT hai bên sẽ được lộ'* (`001`). 🗂️ **Dữ liệu phát sinh trên STG: KHÔNG có đơn mới** (B1 chặn) — hồ sơ A **không bị sửa**. 🧪 **`SEED-ORD-01` của VR-002 ĐÃ MẤT** (`/sdcard` emulator trống khi bắt đầu phiên) ⇒ dựng lại **`SEED-ORD-02`** (5 JPG màu đặc ~38KB phân biệt bằng mắt + 1 PNG tối + 1 JPG **5.6MB** + 2 file non-image), kèm **bản đồ instance Photo Picker** ở `_recon__photo-picker-seed-ord-02.png` (`instance(0)`=5.6MB … `(6)`=đỏ) — ⚠️ `content-desc` mọi thumbnail **giống hệt nhau** ⇒ buộc dùng `.instance(N)`. 🔧 **4 bẫy kỹ thuật MỚI T11–T14** + **4 đính chính** map VR-002 (merge `locators/vibe-locators-latest.md`, hấp thụ **35/35 = 100%**): **T11** 2 ô địa chỉ buộc chạm gợi ý (⭐ đắt nhất) · **T12** `(//android.widget.EditText)[N]` **KHÔNG ổn định** ở Bước 2/3 vì render lazy (ở cuối form, `[2]` trả về ô tên uỷ quyền) ⇒ dùng `@hint`+`@enabled` · **T13** lỗi validate dính, không tự xoá · **T14** bottom bar che vùng lỗi ⇒ cuộn 1 nhịp rồi mới assert. Màn **Wizard Bước 3/3 lần đầu được harvest** (VR-002 chưa từng tới được). ▶️ **Chạy tiếp sau khi B1 được fix:** `/vibe-test --module ORD` — bộ lọc pending tự bốc đúng **19 TC** còn nợ (17 TC sẽ thông ngay khi đăng tin được). 🟩 **VR-005 (2026-09-19) — module `HOME`, 14/30 TC: 13P / 0F / 1B · evidence 14/14 · gate `.claude/hooks/verify_evidence.py` 0 vi phạm.** 🔢 **Đính chính SCOPE_TOTAL `HOME` = 30, ⛔ KHÔNG phải 33** như `TC-HOME-v1.1.md §0.1` ghi: phép *"24 − 2 + 11"* **đếm trùng 3 TC MODIFIED** (`TC-HOME-008/019/021` có ở cả 2 file, `CLAUDE.md` chốt lấy bản v1.1) ⇒ hợp nhất theo ID duy nhất = 24 − 2 (`010`/`024` DEPRECATED) − 3 (trùng) + 11 = **30**. ⛔ Không sửa fragment (`§10.5` FREEZE Σ TC) — đây là đính chính **cách ĐẾM**. 🔴 **Blocker chi phối cả module: STG có 0 tin NEED hợp lệ**, xác nhận 2 bề mặt độc lập (Trang chủ §Tin mới `home-news-empty` + màn Bảng tin). Vì *"Tin mới"* **loại trừ tin của chính mình** (`SC-HOME-019`) nên tài khoản A ⛔ **không tự seed được** ⇒ 8 TC (`017`/`018`/`019`/`020`/`021`/`023`/`025`/`030`) phải chờ **tài khoản B + OTP nhập tay** — đúng blocker của VR-001/VR-003. 🍀 **Cùng dữ kiện đó lại MỞ KHOÁ `TC-HOME-026`**: `fragment §0.3` xếp TC này vào nhóm *"không dựng được trên STG dùng chung ⇒ ghi Blocked"*, nhưng STG tình cờ sạch tin nên empty state `EMP-01` verify được **thật**, đủ 4/4 vế nguyên văn ⇒ 🟢 **xác nhận oracle phản trực giác của `C-HOME-04`** (chuỗi *"trong khu vực của bạn"* giữ nguyên dù tin load toàn quốc). ⚠️ PASS neo vào thời điểm 05:59, cửa sổ đóng khi có người đăng tin. ❌ **1 FAIL — `TC-HOME-007` (ứng viên bug H1, ⛔ CHƯA log bug):** tagline app = `Tiện đường — Giúp đồng nghiệp`, Expected = `Tiện đường — Đồng nghiệp giúp nhau`. ⚠️ Rất có thể **TC/tài liệu lỗi thời** chứ không phải app sai (cùng họ `TC-ORD-006`/`KB-VIBE-01`) ⇒ **chờ QC/BA chốt chuỗi oracle**. Vế *"nhấn banner không điều hướng"* thì PASS. 🚫 **1 BLOCKED — `TC-HOME-002`** ở step 2 (0 tin ⇒ không mở được Chi tiết tin); **3/4 màn con còn lại đã kiểm và ĐÚNG**. ⚠️ **5 TC PASS kèm khai báo lệch step** (`009`/`011`/`012`/`013`/`015`): dùng **đơn có sẵn** thay vì tự đăng tin ở step 1 vì bug **`B1` của VR-004 vẫn chặn đăng tin NEED**; cả 5 ca Expected **không assert trạng thái đơn** ⇒ verdict tin được. 🔧 **3 bẫy locator MỚI T15–T17** + **T2 tái hiện lần 3**: **T15** chấm đỏ chuông là **ViewGroup ẩn danh** (không text/desc/rid) · **T16** **thanh progress 5 bước KHÔNG có node nào** trong tree — cả 2 **bắt buộc đo pixel**, ⛔ không assert bằng `find_element` · **T17** `content-desc` card đơn là **chuỗi gộp 5 dòng** ⇒ dùng `descriptionStartsWith("<Vai>: <Loại hàng>")` · **T2** ca mới `home-news-empty` (`id` 🚫, `-android uiautomator resourceId(...)` ✅). Merge **51/51 = 100%** vào `locators/vibe-locators-latest.md`; **màn Trang chủ lần đầu harvest đầy đủ** (8 màn). 📨 **3 việc route `/analyze-requirements`:** (1) card OFFER *"Nhận giao hàng"* nằm trong section "Đơn của tôi" nhưng **không có nhãn vai** — `SC-HOME-011/012/013` chưa đặc tả; (2) section hiển thị **cả đơn `Đã huỷ`/`Đã giao`** (6 card), mâu thuẫn *"đơn đang hoạt động"*; (3) header Trang chủ **không có node icon vai trò nào** ⇒ nghi `SC-HOME-004` chưa build. 🗂️ **Dữ liệu phát sinh:** toàn bộ thông báo của A **đã đọc** (`TC-HOME-006` dùng nút bulk) ⇒ ⚠️ retest `TC-HOME-005` cần sự kiện sinh thông báo mới; ⛔ không đăng tin / không nhận đơn nào (hero giữ `13`, cộng đồng giữ `317 đơn · 23743 người`). ▶️ **Chạy tiếp:** `/vibe-test --module HOME` — pending tự bốc đúng **15 TC** còn nợ. Gỡ được **tài khoản B + OTP** thì thông ngay **8 TC**; thêm fix `B1` thì thông tiếp **5 TC**. 🟧 **2026-09-19 — module `ASN`, ⛔ 0 VERDICT ⇒ ⛔ KHÔNG mở run `VR`** (đã thử mở `VR-006` rồi **rút lại**: hợp đồng `VR-*` đòi ≥1 TC có evidence per-TC, Stop hook chặn đúng bằng `log_format_broken`; ⛔ không bịa section TC để gate xanh). Phần recon ở **`08_test-runs/vibe/repro/RP-ASN-lo-sdt-sau-ghep-2026-09-19/`** (hợp đồng nhẹ, `--check` ✓ OK). SCOPE_TOTAL **26** = 21 (v1.0) + 13 (v1.1) − 8 ID trùng (lấy bản v1.1); fragment ghi rõ *"Không gỡ TC nào"*. Cả **26/26 chặn tiền đề** ⇒ ledger 24 `⏳ NOT_RUN` + 2 `⛔ N-A`, ⛔ **không khai TC nào là đã phủ**. 🔴 **Chặn gốc không thuộc ASN:** 20 TC chặn bởi *0 tin trên STG + bug `B1`* · 4 TC cần 2–4 thiết bị + **OTP nhập tay** (chỉ 1 emulator) · 2 TC ngoài phạm vi UI (`TC-ASN-024` concurrency tool · `TC-ASN-026` API + audit log) ⇒ `N-A`, giao automation/backend/security. **ASN phụ thuộc seed data nặng nhất dự án — ⛔ đừng lên lịch chạy trước khi fix `B1`.** 🟢 **ĐÍNH CHÍNH clarification của VR-004:** VR-004 ghi *"màn đơn đã ghép **không** lộ SĐT, trái banner cam kết"* — đo lại thấy cụm `NGƯỜI GIAO HÀNG` **CÓ** tên + **SĐT `0947153040`** + nút `Gọi` (MCP verified). ⚠️ Mới kiểm **vai người NHẬN**; `TC-ASN-004` đòi vai **gửi**/**vận chuyển** ⇒ TC vẫn `NOT_RUN`, clarification **mở lại kiểm đúng vai**, ⛔ không đóng bằng quan sát này. 🔧 **Bẫy mới `T18` — nguyên nhân gốc của kết luận sai đó:** màn *Theo dõi đơn* **chỉ cuộn khi bước cuộn NHỎ**; `scroll` mặc định và `swipe` toạ độ **báo thành công nhưng màn đứng yên** ⇒ page source thiếu phần dưới nếp gấp. Dùng `scroll_to_element(scrollDistancePreset="small")`. ⛔ **KHÔNG** dùng `adb shell wm density` để lộ vùng bị che — **app restart về host FoxPro**. `T2` tái hiện lần 4. Merge **20/20 = 100%** vào `locators/vibe-locators-latest.md`; màn *Theo dõi đơn vai người nhận* lần đầu harvest. 👤 **ĐỔI TÀI KHOẢN giữa 2 phiên, không ai báo:** VR-005 = `Đặng Châu Giang` (hero 13) → phiên ASN = **`Đặng Châu Anh`** (hero 3). Cùng 1 đơn hiện `Gửi:` ở phiên trước và `Nhận:` ở phiên này — **suýt log thành bug "nhãn vai tính sai"**; kiểm header mới rõ là 2 tài khoản ⇒ hành vi ĐÚNG và là **đối chứng dương** cho `TC-HOME-011/012/013`. 📌 Bài học: **luôn đọc tên tài khoản ở header trước khi so với phiên trước.** 🚦 Gate = `GATE FAIL` nhưng **0 vi phạm evidence/trích dẫn** — 2 mục đỏ là **giới hạn của gate với phiên 0-verdict** (gate giả định luôn có ≥1 TC chạy; 2 TC bị nhắc là cross-reference). ⛔ Không bịa section TC để gate xanh. 🗂️ Không phát sinh dữ liệu STG. ▶️ **Chạy tiếp chỉ có nghĩa sau khi `B1` được fix:** `/vibe-test --module ASN`. 🔴🔴 **ĐÍNH CHÍNH 2026-09-19 08:40 — `B1` KHÔNG TÁI HIỆN.** QC chất vấn *"tự đăng tin rồi logout đổi account được mà?"* ⇒ kiểm thật: **OFFER ✅ · NEED ✅ đều đăng thành công** (đủ 3 bước, logcat sạch, ⛔ không `REQ_400`); **Đăng xuất ✅ CÓ** ở FoxPro host → Cá nhân (⛔ không nằm trong FoxEco). ⇒ **RÚT LẠI** mọi khai báo *"bug `B1` chặn N TC"* ở dòng `HOME`/`ASN` phía trên — lỗi do **thừa kế kết luận phiên khác làm tiền đề mà không re-verify trong phiên**. ⚠️ ⛔ **Chưa đóng `B1`** — mới chứng minh không tái hiện trên `Đặng Châu Anh`; VR-004 dùng `Đặng Châu Giang`. Giả thuyết ưu tiên test: VR-004 khai **email người nhận = chính tài khoản đang đăng nhập**. 🐞 2 phát hiện phụ mới: **autofill SĐT người nhận trả về MNV** (`0000002352`, app báo *"Số điện thoại không hợp lệ"* — cùng họ `BUG-008`) · **nhãn buổi thật `Sáng (8–12h)`** ≠ QA-obs *"(6–12h)"* ở `TC-ASN-v1.1.md §0` ⇒ sửa fragment. 🗂️ **Đã tạo 2 tin trên STG** (tài khoản `Đặng Châu Anh`): 1 **OFFER** `Tòa V-City → FPT Cầu Giấy` buổi Sáng (= **`SEED-ASN-01`**) + 1 **NEED** cùng tuyến/ngày/buổi ⇒ theo `BR04-01/02` **phải sinh thông báo khớp tuyến**, là việc đầu tiên của phiên ASN sau. 📄 `08_test-runs/vibe/repro/RP-ORD-B1-dang-tin-duoc-lai-2026-09-19/report.md`. 🟢 **VR-007 (2026-09-19) — module `ASN`, 3 TC / 3 PASS · evidence 3/3 · gate 0 vi phạm.** Module ASN từ **2/26** lên **5/26 có verdict cuối**. 🔓 **Hai blocker từng khai ĐỀU BỊ BÁC BỎ:** bug `B1` (*"không đăng được tin"*) và *"OTP nhập tay"* — QC cấp 5 tài khoản + **OTP staging CỐ ĐỊNH** ⇒ **AI tự logout/login** (đổi 2 lượt trong phiên). ⇒ **mọi dòng `NOT_RUN` viện lý do OTP ở VR-001/003/004/005 đều hết hiệu lực.** ✅ PASS: `TC-ASN-013` (**P1** — không gợi ý khớp tuyến cho tin của chính mình; tin NEED trùng **tuyệt đối** tuyến+ngày+buổi với OFFER của chính mình mà app **vẫn loại trừ đúng**) · `TC-ASN-023` (thông báo khớp tuyến trỏ đúng tin) · `TC-ASN-021` (2 tin NEED độc lập, không ghi đè). 🟢 **Đối chứng chéo 013⟷023:** cùng tuyến, chủ tin **KHÔNG** nhận thông báo / người khác **CÓ** ⇒ app loại trừ **theo chủ sở hữu**. ⚠️ **`023` chạm biên `NFR-04`:** độ trễ đo được **(5s, 65s]** — cận trên vượt 60s **do làm tròn nhãn "3 phút trước"**, ⛔ không phải app chậm; 🔁 đề nghị đo lại bằng **2 thiết bị**. 🔑 **Màn đăng nhập FoxPro lần đầu harvest** (merge 34/34 = 100%) — mở khoá nhóm TC **đa tài khoản của MỌI module**; luồng 5 bước ở `04_test-data/valid/USR-accounts.md §0b`. 📨 **2 đề nghị sửa tài liệu:** `TC-ASN-021` Steps ghi loại hàng *"Giấy tờ, hồ sơ"* (nhãn **không tồn tại**, `C-ORD-09` chốt `Tài liệu`) · fragment ASN ghi buổi *"Sáng (6–12h)"*, app thật **`Sáng (8–12h)`**. 🗂️ **5 tin sống trên STG** (1 OFFER + 2 NEED tạo phiên này + 2 tin lúc 08:26–08:38) ⇒ lô sau ⛔ đừng seed lại. 🛑 Dừng vì **hết sức phiên**: **21 TC còn nợ nay là NỢ CÔNG SỨC** — 18 TC chạy được ngay; chỉ `006`/`008` (2–3 thiết bị) + `019` (dev lùi ngày) còn chặn thật. ▶️ `/vibe-test --module ASN`. 🟡 **VR-016 (2026-09-21) FEED** — chỉ 5 TC v1.1: 5/15 có verdict (1P/2F/2B). Chi tiết → `08_test-runs/vibe/coverage/coverage-FEED.md` + `v1.0/FEED-bang-tin/CHANGELOG.md §4`. 🟡 **VR-017 (2026-09-21) ACT** — chỉ 10 TC v1.1, chế độ chỉ đọc: **10/10 v1.1 có verdict** (6P/2F/2B) · module 10/18 (8 CARRIED ngoài phạm vi). 2 bug draft chờ QC review `BUG-030` (`008` chuỗi Hết hạn) · `BUG-031` (`012` thiếu dòng giải thích); `017` QC chốt app đúng ⇒ sửa Expected, FAIL→PASS, `BUG-032` xoá; `015` QC chốt đơn Đã huỷ ở tab Đang diễn ra **không phải bug** ⇒ sửa Expected theo app, FAIL→BLOCKED; `005`/`015` BLOCKED thiếu đơn `RETURNED`. Chi tiết → `08_test-runs/vibe/coverage/coverage-ACT.md`. 🟡 **VR-018 (2026-09-21) CNL** — chỉ 13 TC v1.1, QC cho phép đổi trạng thái đơn: **13/13 v1.1 có verdict** (5P/6F/2B) · module 13/22 (9 CARRIED ngoài phạm vi; header fragment ghi thiếu `014`). 4 ứng viên bug chờ QC: thiếu nút `Yêu cầu hoàn hàng` khi Đang giao (`017/018/019`) · log người gửi huỷ ghi nhãn `Đã huỷ nhận đơn` (`009`) · log ghi tên thay vai (`009/010`) · thiếu lỗi dưới ô lý do (`004`). `020` BLOCKED (báo sự cố = form Microsoft ngoài app, không có đơn INCIDENT) · `021` BLOCKED (cần 2 thiết bị). Chi tiết → `08_test-runs/vibe/coverage/coverage-CNL.md`. |
| 11 | log-bug | **PARTIAL** | 2026-09-18 | v1.1 · **12 bug local** — 8 đã push Jira (`FE-290`…`FE-298`, module USR) · **4 draft chưa push**: `BUG-009`/`BUG-010`/`BUG-011`/`BUG-012` (ứng viên **B1/B2/B6/B5** của `VR-002-ORD`) + `BUG-008` (USR) | `05_bug-reports/draft/` (5 file) · `05_bug-reports/jira/` (8 file) · `bug-index.md` | ⏳ **Còn 1 ứng viên bug ORD chưa log**: **B4** autofill thiếu địa chỉ giao (`TC-ORD-074` P1 — ⚠️ **chờ BA** trả lời mới quyết bug app hay TC sai). 🚫 **B3 (trần ảnh 4/5) + B3′ (mất bộ đếm ở 5/5) ĐÃ BÁC BỎ CẢ HAI** (2026-09-18): B3 là dương tính giả do lazy horizontal list · B3′ là **hành vi đúng**, QC chốt *"đủ 5 file bỏ bộ đếm luôn"* ⇒ **sửa Expected `TC-ORD-070`** (`TC-MASTER-v1.1` + `LATEST` + fragment) thay vì log bug ⇒ `TC-ORD-070`/`071` đều **PASS**, khối ảnh ORD sạch. QC GiangDC2 chốt 2026-09-18: **không push Jira** 4 draft hiện có |

## 9. Notes (quyết định cross-version — KHÔNG phải per-run count log)

**Quyết định phương pháp (2026-09-07):**
1. **Phân tích lại v1.0 từ đầu** bằng bộ skill v1.1 thay vì migrate bản phân tích cũ — vì bản cũ dùng layout `flat` (MEMORY.md 193 KB monolith, drift số đếm nhiều lần) và mang 5 lỗi đã biết (`KP-04 §4`).
2. **Tách 11 module** thay 8 mã domain của đợt cũ: thêm `HOME` · `FEED` · `ACT` để **module ↔ fragment ↔ sheet `export-tc-rp`** thành 1:1. Đợt cũ để `ORD` gánh 3 màn (33 SC / 161 TC) và `ASN` gánh Bảng tin (2 SC / 31 TC).
3. **Scope = toàn bộ 11 module**, không giới hạn 5 luồng Phase 1 mà PM chốt (`KP-03 §3`). ⚠️ `CNL` · `NTF` · `TS` được PM xếp **out of scope Phase 1** — cần **xác nhận lại với PM trước khi execute** (36 SC thuộc 3 module này).
4. **Chạy trên tài liệu hiện có** (BRD v3.2 + PRD tái dựng từ demo). PRD version mới của PM khi có ⇒ chạy `/analyze-requirements --delta --version <next>`, **không** ghi đè v1.0.
5. **Không dùng bộ TC cũ (323 TC) làm regression baseline** — lý do đầy đủ ở §4.
6. **Tên thư mục module = `<CODE>-<slug>`** (đổi 2026-09-07, trước khi chạy `generate-tc`): `USR-tai-khoan` · `HOME-trang-chu` · `FEED-bang-tin` · `ORD-dang-tin` · `ACT-hoat-dong` · `ASN-ghep-noi` · `DLV-giao-nhan` · `GIFT-qua-cam-on` · `CNL-huy-don` · `NTF-thong-bao` · `TS-trust-safety`. Framework tách sẵn `module.code` (token ID) khỏi `module.dir` (thư mục) nên **ID không đổi một ký tự**. ⚠️ **Áp cho mọi version sau** — mở v1.1/v2.0 thì dùng đúng bộ `Dir` này, ⛔ không quay về tên thư mục chỉ có mã. Cột `Dir` canonical ở `Project_rule.md §Module Codes`; lịch sử per-module ở `<Dir>/CHANGELOG.md §1` (dòng `REFACTOR` 2026-09-07).

**Số liệu đông cứng theo mốc 2026-09-07** *(⚠ không phải số current — số current đọc ở `v1.0/MEMORY.md §2`, canonical ở frontmatter module)*:
- 116 REQ · 211 SC (211 NEW) · 35 CL (17 Open · 0 blocker) · 64 RISK · 11 module.
- **111/116 REQ có ≥1 SC**; 5 REQ gap chủ đích (`REQ-DLV-011..014`, `REQ-CNL-007`) — lý do ở `<module>/CHANGELOG.md §3`.
- ⚠️ **211 SC vượt ngưỡng gợi ý 200 SC/version** của skill (`SKILL.md §Common Edge Cases`). Đã áp cả 2 biện pháp mà skill đề xuất: **(a)** split — 11 module thay 8; **(b)** giảm rác — 5 REQ để gap chủ đích thay vì tạo SC, và altitude SC ở form nhiều trường dừng ở mức rule (EP/BVA từng trường để `generate-tc` fan-out, xem `ORD-dang-tin/CHANGELOG.md §2` ràng buộc 8). Con số 211 là **hệ quả của scope "toàn bộ 11 module" + fan-out đúng luật** (riêng ma trận `DLV` 15 ô đã là 15 SC), không phải SC rác. ⇒ Nếu cần giảm: cắt scope 3 module out-of-scope Phase 1 (`CNL` 13 + `NTF` 16 + `TS` 7 = **36 SC**) → còn 175 SC.

**So sánh với đợt phân tích v1.0 CŨ** *(reference-only — nguồn `_handoff-v1.0/02_seed/KP-04`)*:
| | Đợt cũ (skill v1.0, flat) | Lượt này (skill v1.1, module-first v2) |
|---|---|---|
| Module | 8 | **11** |
| REQ | 46 | **116** |
| SC | 92 (91 hiệu lực + 1 DEPRECATED) | **211** |
| Clarification | 25 | **35** (kế thừa 25 + mở mới 10) |
| Bề mặt thiếu SC nặng nhất | Trang chủ 1 SC/32 TC · Bảng tin 2 SC/31 TC · `DLV` 14 SC cho ma trận 15 ô | `HOME` 24 SC · `FEED` 14 SC · `DLV` 30 SC (15 ô = 15 SC) |

**Nợ cross-version cần xử lý trước khi execute:**
- 🔴 **≥9 bug/nghi vấn bug đã biết mà đợt cũ CHƯA log Jira** (`KP-05 §5`): lộ SĐT trước ghép · chủ tin tự nhận đơn · Tên người gửi không read-only + bị xoá trắng · địa chỉ lấy hàng không pre-fill · SĐT tự đổi giữa phiên · app báo lỗi SĐT nó tự auto-fill · `VAL-04` không enforce (2 lỗi) · log LỊCH SỬ khi huỷ (2 lỗi). ⇒ Jira **chưa cấu hình** ⇒ cần bật `Project_rule §Jira Integration` trước khi `/log-bug --push-jira`.
- 🔴 **Vibe-test nên chạy TRƯỚC `generate-tc`** cho 2 module có nguồn mỏng/cũ: `ACT` (6/9 REQ chỉ dựa 1 lượt `QA-obs` 2026-07-27) và `DLV` (15 ô ma trận từ `QA-obs` + Figma 2026-07-24). Nếu app đã đổi nhãn thì 15 SC của `DLV` FAIL hàng loạt vì lý do không phải bug.
- 🟡 **6 CL ưu tiên hỏi BA** (bảng ở `v1.0/MEMORY.md §2`) — đứng đầu là `C-ORD-09` (danh mục "Loại hàng": app không có chip "Tài liệu").
- 🟡 **Câu hỏi mới phát sinh ở lượt này** (chưa từng có ở đợt cũ): rule `§A8` yêu cầu consent *"trước khi đăng/**ghép**"* nhưng luồng ghép chỉ có modal xác nhận lộ SĐT — có phải **gap tuân thủ pháp lý**? (`TS-trust-safety/risk_assessment.md` `RISK-TS-04`).

## 10. Version Cutover — Compaction Rule
> Khi mở version mới (v1.0 → v1.1/v2.0):
> 1. **Archive** append-log cũ sang file lịch sử; §8 Pipeline Status **reset** về NOT_STARTED cho version mới.
> 2. **Không mang** số current của version cũ sang — recompute cho version mới từ frontmatter `counts:` của module.
> 3. Số "đông cứng theo mốc" ở §9 giữ nguyên (có nhãn ngày), ⛔ KHÔNG "sửa cho khớp" version mới.
> 4. Chạy `health-check` xác nhận: 0 drift count · 0 router-tier chứa nội dung leaf · 0 orphan reference.
